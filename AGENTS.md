# 智能体协作规则

## 沟通

- 始终使用中文回复和记录交接信息。
- 任务交付说明必须列出任务编号、契约版本、验证命令、结果和已知限制。

## 仓库边界

- `contracts/` 是所有跨进程字段、枚举、错误和事件的唯一来源。
- Java、Python 和 TypeScript 网络类型必须由 `tools/generate-contracts/` 生成，禁止手工修改生成文件。
- `src/tool_defect/` 在 P1 保持原位；未经兼容测试不得整体搬迁。
- 业务后端不得导入 Python 算法；推理服务不得访问业务数据库；前端不得实现最终处置规则。
- 数据库迁移只归 `services/business-api/` 所有。
- 图片、数据集、模型权重、运行输出和密钥不得提交 Git。现有 `data/manifests/` 与 `data/masks/` 仅作为迁移前冻结基线，不得继续扩充。

## 安全失败

- 技术失败、状态未知、哈希冲突、契约不兼容和缺失前置条件必须明确失败或进入 `HOLD`，不得产生 `PASS`。
- 不得把缺失 Docker、Maven、Node 20.13.1、真实设备或外部服务的验证标记为通过。
- 示例配置不得包含默认密码、访问密钥、令牌或私钥；密钥通过环境或机密挂载注入。

## 修改所有权

- 同一契约版本、数据库迁移序列和生成包版本必须串行修改。
- 跨模块变更先改契约，再生成类型，最后修改消费者。
- 不覆盖其他智能体的未提交改动；需要越界时先协调任务所有者。

## 验证

- P1 离线源码门禁：`make verify-p1-offline`。
- P1 严格门禁：`make verify-p1-strict`；全阶段汇总入口为 `make verify-all`。严格门禁会检查完整工具链并在缺失前置条件时失败。
- 单项统一入口见根目录 `Makefile`；任何入口都不得用无操作成功或静默跳过冒充通过。
