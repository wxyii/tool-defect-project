# 契约验证

`verify_contracts.py` 使用标准库离线验证 v1 JSON Schema、合法/非法示例、
状态迁移、OpenAPI 写接口五类示例以及 AsyncAPI 投递语义。验证器遇到未实现的
模式关键字会失败，避免静默漏检。

`compatibility.py` 将当前契约表面与已提交基线比较；删除端点、方法、必填字段、
响应码或枚举值均视为破坏性变更。只有明确发布新主版本时才可更新基线。
