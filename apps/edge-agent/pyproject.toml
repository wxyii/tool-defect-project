[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tool-defect-edge-agent"
version = "0.1.0"
description = "刀具缺陷采集端、离线队列与中心同步基础"
requires-python = "==3.11.*"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
