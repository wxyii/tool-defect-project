# 刀片缺陷多任务评估套件

本上下文定义五类监督数据集、模型工件和统一评估中的核心术语，避免把父图级结论与分块子图级结果混用。

## 数据集与样本

**原始数据集**：未经过环形预处理的原始刀片图像、掩膜及其父样本清单。
_避免_：原图数据（“原图”只用于说明显示背景时使用）。

**整图预处理数据集**：对每个父样本完成自适应环形或边界归一化处理后形成的单张训练图像与掩膜。
_避免_：环形数据集（范围不够明确）。

**分块数据集**：从整图预处理数据集按圆周窗口生成的重叠子图集合；每个子图通过对应局部掩膜独立确定分类标签，并保留父样本溯源关系。
_避免_：切片数据（“切片”可指生成过程，也可指子图集合）。

**父图**：一个原始或整图预处理样本，是统一测试集中的评估单位。
_避免_：整图（整图也可能只是相对于子图的形态描述）。

**子图**：分块数据集中由一个父图和一个圆周窗口产生的局部样本。
_避免_：补丁、块图。

## 模型与结果

**模型工件**：能够独立复现一次多任务推理的 `model.json` 与 `weights.h5` 文件组合。
_避免_：权重文件（单独的 `weights.h5` 不足以加载模型）。

**父图级主指标**：将每个模型在同一批父样本上的分类与分割结果汇总后形成的正式比较指标；分块模型先把子图结果合并回父图。
_避免_：整图指标（未说明是否经过父图聚合）。

**子图级辅助指标**：只描述分块模型局部预测质量的诊断指标，不作为五个模型的主排名依据。
_避免_：分块准确率（可能被误读为刀片级准确率）。

**检测图**：以模型对应数据集的父图为背景，叠加模型分割预测区域、分类结果和置信度的可视化结果。
_避免_：真实缺陷图（模型预测不等于人工确认）。
