# 推理服务基础

本目录实现 P2-D01 的推理消费者、对象物化、插件编排、单运行槽、
可信模型预热、资源隔离、健康检查和标准结果回调基础。

推理服务只消费冻结契约事件、读取经校验的对象并提交标准结果，不直连
业务数据库。插件不得访问网络、数据库或队列；只有后端接受结果后才能
确认队列消息。

验证命令：

```text
python -m unittest discover -s services/inference-service/tests -v
```
