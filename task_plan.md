# P0–P2 实施计划

## 目标

完整实现 `Docs/15-系统分阶段构建与智能体任务计划.md` 中 P0、P1、P2 的全部任务，并按文档规定的验证方法和通过标准逐项验收。

## 当前阶段

阶段五：联合验收与交付

## 阶段

### 阶段一：需求盘点与基线核查

- [x] 提取 P0–P2 全部任务、依赖、交付物和通过标准
- [x] 检查仓库结构、已有实现、测试与目录级约束
- [x] 建立“任务—现状—缺口—验证”矩阵
- **状态：** 已完成

### 阶段二：P0 工程地基

- [x] 按依赖顺序完成 P0 全部任务
- [x] 执行 P0 任务级验证与阶段门禁
- **状态：** 已完成（资产差异按设计要求冻结为阻断清单）

### 阶段三：P1 核心领域与数据基础

- [x] 按依赖顺序完成 P1 全部任务
- [x] 执行 P1 任务级验证与阶段门禁
- **状态：** 已完成；源码门禁全绿，Docker/Compose 已确认可用，Compose 只读配置校验通过。严格汇总命令因平台审批额度未启动，但各组成项已有独立成功证据。

### 阶段四：P2 核心服务与接口

- [x] P2-A01 插件开发包和统一内存对象
- [x] P2-A02 现有预处理和算法适配器
- [x] P2-A03 模型包、校验和隔离加载
- [x] P2-A04 三组现有双任务模型技术评估
- [x] P2-B01 模块化单体与领域边界
- [x] P2-B02 首批数据库迁移与约束
- [x] P2-B03 S3 兼容存储适配和上传确认
- [x] P2-B04 发件箱、收件箱和可靠消息基础
- [x] P2-C01 SQLite、本地目录和原子落盘
- [x] P2-C02 触发、相机适配端口和模拟器
- [x] P2-C03 同步、退避、对账和心跳客户端
- [x] P2-D01 推理编排、运行槽和服务健康
- [x] P2-W01 前端壳、鉴权、权限和统一请求层
- [x] 复验 P2-A/D、P2-C、P2-W 当前工作树门禁
- [x] 完成 P2-B Docker/Testcontainers 集成验证
- [x] 完成 P2-A04 三组真实候选模型评估
- [x] 执行 G2 阶段门禁并逐条保存证据
- **状态：** 已完成；五条工作流均可独立构建和测试，命令行、插件、数据库、存储、消息、采集、推理、可信模型与前端契约证据全部通过。

### 阶段五：联合验收与交付

- [x] 运行 P0–P2 全量测试、静态检查和门禁（已执行，外部前置条件按安全失败保留）
- [x] 核对任务覆盖矩阵与文件冲突
- [x] 整理交付说明和验证证据
- **状态：** 已完成；P0–P2 共 22 项任务和 G0/G1/G2 均已按任务口径交付，已知历史资产限制继续显式保留。

## 已知限制清单

1. P0 冻结基线明确要求保持缺失状态的旧分类与旧双任务 Git LFS 权重仍不在工作树；原始 `python -m unittest discover -s tests` 会有 9 项资产错误。不得用 P2 三组重新训练候选替代不同结构/哈希的历史制品。
2. 三组 P2-A04 模型仅登记为 `TEST_CANDIDATE`，34 图技术评估不能形成生产质量或发布声明。
3. 最终复验回合未启动 `make verify-p1-strict` 汇总命令：Docker 套接字需要受控权限，而平台审批额度已用尽。其源码门禁、Compose 只读配置检查以及真实 Docker/Testcontainers 后端门禁已分别成功，但不得据此声称该汇总命令本身通过。
4. `make verify-all` 会包含上述严格汇总入口以及 P0 已知缺少旧权重的核心全量测试，因此没有把它标记为全绿；任务级和阶段级证据以 `progress.md` 中逐项结果为准。

## 关键问题

1. P0–P2 分别包含哪些任务，任务间的精确依赖关系是什么？
2. 哪些交付物已经存在且达到标准，哪些需要补齐或重构？
3. 文档中的命令是否可直接运行，若环境缺少依赖应如何提供等价验证？
4. 是否存在嵌套 `AGENTS.md` 或未提交改动需要避让？

## 已作决定

| 决定 | 理由 |
|---|---|
| 以任务文档的逐项通过标准作为唯一完成口径 | 避免把“文件存在”误判为“任务完成” |
| 先盘点再分工作流并行实施 | 降低跨目录冲突并缩短总耗时 |
| 保留并避让用户现有改动 | 遵守共享工作区的非破坏性要求 |
| 跨平台路径校验同时使用 POSIX 与 Windows 路径语义 | 避免在 Linux/macOS 上漏过 Windows 绝对路径和 UNC 路径 |

## 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 基线 `unittest`：1 个失败、9 个错误 | 1 | 9 个错误源于被忽略的分类/双任务权重缺失；绝对路径缺陷已修复并通过定向测试，外部资产测试仍需按基线清单处理 |
| 更新规划记录时补丁上下文不匹配 | 1 | 重新读取当前文件并使用精确上下文更新，没有重复原补丁 |
| 边缘测试首次收集出现循环导入 | 1 | `capture` 包入口不再提前导入依赖硬件端口的协调器，调用方改为从子模块显式导入 |
| 首次读取 Node.js 官方发布索引失败 | 1 | 本机残留代理 `127.0.0.1:7897` 不可用；按网络权限流程重试后成功确认 Node 24.18.0 |
| 前端首次安装因沙箱网络权限失败 | 1 | 中止自动重试后按受控网络权限重新执行，依赖与锁文件生成成功 |
| 前端首次类型检查无法启动 | 1 | `vue-tsc` 3.3.8 与 TypeScript 7.0.2 的包导出不兼容；按其发布元数据锁定 TypeScript 6.0.2 |
| pnpm 11.9.0 无法在 Node 20.13.1 启动 | 1 | 官方兼容矩阵确认 Node 20 支持 pnpm 8/9/10、不支持 11；按用户降级决定同步锁定 pnpm 10.34.5 |
| 本机 Corepack 0.28.0 无法验证新 pnpm 签名 | 1 | 不升级或污染系统安装，改用 `npx --yes pnpm@10.34.5` 运行项目锁定版本 |
| pnpm 10 首次非交互重建依赖目录被中止 | 1 | 显式设置持续集成模式；随后按清单变更使用 `--no-frozen-lockfile` 生成新锁，完成后恢复冻结安装 |
| pnpm 10 持续集成模式拒绝旧锁文件 | 1 | 这是预期漂移保护；仅本次依赖基线变更使用 `--no-frozen-lockfile` 更新 |
| Vue Router 5.2.0 与 Vite 6 存在对等依赖冲突 | 1 | 锁定兼容 Vue 3.5 且不要求 Vite 7/8 的 Vue Router 4.6.4 |
| 查询不存在的 Vue Router `latest-4` 标签返回 404 | 1 | 改为查询完整 4.x 版本列表，确认最新稳定版为 4.6.4 |
| Node 20 工具链首次类型检查报告类型库与弃用错误 | 1 | 移除 TypeScript 6 已弃用的 `baseUrl`；使用仍属于 Node 20 系列且兼容 TypeScript 6/当前 DOM 测试库的 `@types/node` 20.19.43 |
| 第二次前端类型检查报告路径和第三方声明错误 | 1 | 无 `baseUrl` 时将源路径改为显式相对路径；只跳过第三方声明库内部检查，项目源码与生成契约仍严格检查 |
| 第三次前端类型检查发现测试代码类型问题 | 1 | 补入 Node 测试类型、消除重复路由元数据并移除无效断言泛型 |
| 前端首次单元测试有一个测试文件加载失败 | 1 | 浏览器模拟环境会改写 `import.meta.url`；安全源码测试改从测试进程工作目录解析，不依赖个人绝对路径 |
| 通过 `npx` 执行离线冻结安装时仍尝试访问注册表 | 1 | `npx` 自身需要解析临时包；改为直接调用已校验的本地 pnpm 10.34.5 可执行文件 |
| pnpm 离线冻结安装因本地存储缺少 `tslib` 归档而失败 | 1 | 锁文件本身已通过一致性检查；按冻结锁联网恢复依赖且没有解析漂移，后续构建测试重新执行 |
| 搜索文档目录引用时正则中的反引号被 shell 解释 | 1 | 改用单引号包裹不含命令替换语义的正则后成功；后续 shell 查询避免未转义反引号 |
| 读取可选基线摘要时 zsh 对空通配符报错 | 1 | 改为使用已枚举出的精确文件名读取，不再依赖可能为空的 glob |
| 前端开发服务器首次监听本地端口被沙箱拒绝 | 1 | 按权限流程在 127.0.0.1:5173 启动临时服务，完成检查后立即停止 |
| 浏览器等待策略不支持 `networkidle` | 1 | 改用受支持的 `load` 状态并以 DOM、布局尺寸和控制台日志作为确定性检查 |
| Maven 当前镜像下载地址返回 404 | 1 | 改用 Apache 官方归档地址下载同一 3.9.15 发布包 |
| Apache Maven SHA-512 文件只有裸哈希，`shasum -c` 无法解析 | 1 | 直接计算归档 SHA-512 与官方裸哈希比较，完全一致后才解压 |
| Maven 首次编译无法创建默认 `~/.m2/repository` | 1 | 使用任务专属临时本地仓库 `/private/tmp/tool-defect-m2`，不修改用户 Maven 缓存 |
| 沙箱内 Maven 无法解析中央仓库域名 | 1 | 按网络权限流程重试；依赖下载完成后真实编译 86 个 Java 源文件成功 |
| P2-B 智能体收口回合传输中断 | 1 | 共享文件系统改动保留；要求智能体先只读核对已落盘状态后从原任务恢复，避免重复覆盖 |
| 采集端首次 `uv build` 无法读取用户缓存中的内部 `.git` | 1 | 按权限流程允许只读构建缓存后重试；源码包和 wheel 均成功生成到 `/private/tmp`，并清理源码树内临时 egg-info |
| P2-C 首轮 39 项测试未覆盖五个终审安全边界 | 1 | 独立只读审计提供失败复现；新增真实解码/启动复核/状态组合/回调投递/批次上限测试并继续扩展至 82 项 |
| 崩溃续传测试仍按旧语义期待 `OK` | 1 | 启动恢复必须保守进入质量警告；修正测试期望后复验 |
| 新增鉴权暂停测试时测试尾部误置入心跳用例 | 1 | 恢复测试边界并重新运行完整采集端测试 |
| 后端统一入口只执行 Surefire 单元测试 | 1 | 改为 Maven `verify`，让 `*IT` 与 Testcontainers 缺失前置条件显式失败；离线单元验证单独记录 |
| P1 源码门禁发现 HTTP 测试示例令牌触发密钥扫描 | 1 | 改为扫描器认可且语义明确的测试占位符，随后重跑密钥扫描与完整源码门禁 |
| `make verify-data` 仅有不存在实现的失败占位 | 1 | 新增只读统一入口，连续扫描两次并核对冻结锁、决策文件和未登记现场硬编码 |
| 严格环境检查只识别全局 Maven/Node/pnpm | 1 | 改为验证固定 Maven Wrapper，并按项目清单在全部候选路径中寻找精确 Node 20.13.1 与 pnpm 10.34.5 |
| 搜索 Node 版本引用时 shell 引号未闭合 | 1 | 简化为不含嵌套引号的单引号正则后完成扫描 |
| 用可选前端子目录 glob 读取源码时 zsh 对空匹配报错 | 1 | 改用已枚举的精确文件路径读取 |
| 更新进度时误用不存在的表格上下文 | 1 | 立即重读规划文件并按实际列表上下文更新 |
| P2-C 默认抖动可能缩短 `Retry-After` | 1 | 将服务端值设为硬下限，并增加默认 20% 抖动回归 |
| P2-C 轮询响应未校验请求/响应 `capture_id` 关联 | 1 | 生成适配器与同步服务双层拒绝错误关联，并增加重复最终响应幂等测试 |
| P2-W 旧令牌刷新可覆盖后续登录会话 | 1 | 内存会话绑定单调代次；旧刷新无论成功失败均转为 stale，且不得清除或覆盖新会话 |
| P2-W 较早 OIDC 回调晚到可覆盖或清除新登录 | 1 | 回调引导绑定会话代次与组件生命周期；卸载/旧回调只报 stale，不修改新会话 |
| OIDC 退出链路只清本地令牌，未调用已有吊销端点 | 1 | 先同步清除本地会话并导航登录页，再尽力异步吊销捕获的刷新令牌 |
| OIDC 吊销补丁首次上下文未匹配 | 1 | 分别读取运行时、组件和测试尾部后按精确上下文更新 |
| OIDC 运行时新增吊销方法后前端类型检查失败 | 1 | 补齐测试运行时替身，并给吊销 fetch mock 声明真实请求参数类型 |
| SSE 会话代次检查遮蔽真实刷新失败错误 | 1 | 先传播回调与鉴权失败，再用代次检查识别普通网络等待中的账号切换 |
| `make test-backend` 在测试前写入受限 `~/.m2` 失败 | 1 | Maven Wrapper 用户目录和依赖仓库改为仓库内忽略的 `.build/`，确保统一入口能到达真实集成测试 |
| 干净 `.build` Maven 缓存下载命中失效本机代理 | 2 | 停止该路径；后续只使用此前校验过的 `/private/tmp` Maven 分发与依赖缓存，不把网络失败标记为后端测试结果 |
| Maven 固定依赖联网重试因当前额度限制被拒绝 | 1 | 不再尝试联网；仅使用此前已校验的本机只读 Wrapper 缓存与任务专属依赖缓存做离线严格复验 |
| 最终 `uv build --no-build-isolation` 在 macOS 系统配置库崩溃 | 2 | 两次相同 panic 后停止重试；改用同一已锁定 setuptools 后端的离线 `pip wheel --no-build-isolation --no-deps` 成功构建 |
| 当前虚拟环境未安装 `build` 模块 | 1 | 不联网安装新工具，使用已安装 pip/setuptools 的等价 PEP 517 wheel 构建 |
| P2-A04 受控报告还包含两个候选 36/34 样本及顺序不一致 | 1 | 先核对配置与冻结清单，修正为三候选共享同一 34 图顺序；随后使用 `training/` 中哈希匹配的模型完成真实评估 |
| 记录 P2-A04 决策时补丁目标表格定位错误 | 1 | 已确认技术决策表位于 `findings.md`，改用实际上下文更新，不重复原补丁 |
| `verify-all` 提前调用 P3 以后测试入口 | 1 | 按文档“当前阶段”语义收口为 P0–P2 必需项，未来入口仍独立存在且不得空跑成功 |
| `planning-with-files` 停止脚本统计 0 个阶段 | 1 | 脚本只识别英文 `### Phase`/`**Status:**`，与项目强制中文记录不兼容；以本文件复选框和中文状态为准，不伪造完成 |
| 首轮真实 PostgreSQL 集成迁移失败 | 1 | V3 直接引用了被 PostgreSQL 截断/自动生成的长约束名；改为按唯一约束定义精确查找后删除，兼容空库与 V2 向前迁移 |
| 首轮 Spring 容器启动失败 | 1 | `JdbcInboxRepository`/`JdbcOutboxRepository` 为 `final`，无法被 Spring 数据访问异常转换代理；移除类级 `final` 后定向复验 |
| 首轮 MinIO 容器启动失败 | 1 | 测试 KMS 值把 32 位十六进制文本按 Base64 解码成 24 字节；改用 32 字节测试密钥的标准 Base64 表示 |
| 定向迁移复验把 Flyway 建模式记录计入版本数 | 1 | `migrationsExecuted` 已为 3；历史表断言改为只统计 `version IS NOT NULL` 的成功版本迁移，不把新建 schema 的基线记录误算为 V4 |
| Spring Boot 4 应用未自动执行 Flyway | 1 | Boot 4 把 Flyway 自动配置拆为独立模块；后端改用 `spring-boot-starter-flyway`，保留 PostgreSQL 数据库扩展 |
| 禁用业务消息后健康探针仍连接 RabbitMQ | 1 | Rabbit 健康贡献器按同一 `td.messaging.enabled` 开关启停，避免未启用依赖把受限健康端点置为 `DOWN` |
| 非 Web 消息集成上下文仍创建 HTTP 安全链 | 1 | 安全配置限定为 Servlet Web 应用，消息工作进程测试不再要求不存在的 `HttpSecurity` |
| MinIO 返回标题大小写的对象元数据键 | 1 | S3 适配边界按 HTTP 语义把元数据键规范化为小写，并拒绝大小写碰撞；业务校验不再依赖供应商返回键的大小写 |
| 健康隔离测试把默认拒绝 401 误判为端点暴露 | 1 | 应用端口对未知未认证路径可能先经安全过滤器返回 401；断言接受 401/404，并继续要求管理端口独立、无细节且为 UP |
| RabbitMQ 入站消息从错误属性读取持久化模式 | 1 | 出站单测继续检查 `deliveryMode`；真实消费后的集成测试改查 AMQP 入站映射的 `receivedDeliveryMode` |
| RabbitMQ 集成测试拒绝框架确认关联头 | 1 | Spring 为 mandatory return/发布确认注入 `spring_*` 传输头；测试只豁免该命名空间，非框架业务头仍严格限于冻结字段 |
| 最终 `verify-p1-strict` 权限审批未启动命令 | 1 | Codex 平台提示当前用量额度已达上限；不绕过 Docker 套接字权限，改执行无提权源码门禁，并保留本回合已完成的真实 Docker 后端证据 |

## 暂停点

- 2026-07-29：用户要求在最近一项任务完成后暂停。
- 已完成并验证 Maven Wrapper 3.9.15（仅脚本模式、固定发行包校验和、无 Wrapper JAR）。
- 2026-07-29：用户已要求继续；优先处理 P2-C/W 独立审计所列契约接线缺口，以及 P2-B 测试和容器集成收口。
- 2026-07-29：P2-C 与 P2-W 已完成实现和本地门禁，当前从 P2-B 最终审计、P1/P2 联合门禁与阻断项归档继续。

## 备注

- 每完成一个阶段，更新本文件、`findings.md` 与 `progress.md`。
- 重大技术决定前重新读取本文件。
- 所有失败命令和修复过程均记录，避免重复无效尝试。
