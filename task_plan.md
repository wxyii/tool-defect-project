# P0–P7 实施与整改计划

## 目标

在保留 P0–P5 历史实施记录的基础上，以 `Docs/15-系统分阶段构建与智能体任务计划.md` 为验收口径，整改 P6、P7 全部任务，并按真实实现、真实基础设施和可复核证据逐项验收。

## 当前阶段

阶段三十九：P7 阻塞审计与仓库内收口

## 整改跟踪规则

- 本文件是 P6/P7 整改状态的唯一跟踪表；`findings.md`、`progress.md` 及各类报告只能作为证据，不能另行声明任务状态。
- 整改任务状态只允许：`未开始`、`进行中`、`阻塞`、`已验证`。
- 只有验收命令全部成功，且证据路径包含真实实现或真实现场记录时，状态才可更新为 `已验证`。
- 模板、模拟器、内存状态机、干跑、占位签字、单元测试或缺少前置条件的跳过结果，均不能单独把任务更新为 `已验证`。
- 技术失败、状态未知、哈希冲突、契约不兼容或缺少真实设备/签字/基础设施时，状态必须保持 `阻塞` 或降为 `阻塞`。
- 每次状态变更必须在下方“状态变更记录”补充日期、变更人和可定位依据；负责人按主责角色登记，落实到个人后以“角色（姓名）”替换。

**本阶段状态：** 进行中

## P6/P7 整改唯一跟踪表

> 计划基准日：2026-07-31。期限按前置依赖顺排；现场窗口、真实设备或签字未按期提供时，负责人必须在到期日前把状态保持为 `阻塞` 并写明新的外部承诺日期，禁止静默顺延。
>
> 表中尚不存在的 `make verify-p6-*`、`make verify-g6`、`make verify-p7-*` 和 `make verify-g7` 必须由对应负责人补成非空跑、非静默跳过的严格入口；新增入口本身属于该任务交付物。任务状态更新为 `已验证` 时，必须同时满足：验收命令全绿、目标证据存在且可追溯、当前阻断条件全部关闭。
>
> 契约版本：`v1`；OpenAPI 与生成包版本：`1.0.0`。跨进程字段变更必须先修改 `contracts/`，再生成三语言类型并验证消费者。

| 任务 | 状态 | 负责人 | 完成期限 | 证据路径 | 验收命令 | 当前阻断与收口要求 |
|---|---|---|---|---|---|---|
| P6-01 历史数据导入与不可变基线 | 阻塞 | 数据治理负责人 | 2026-08-07 | 现有：`jobs/artifact-migrator/controlled-output/summary.json`、`jobs/artifact-migrator/controlled-output/baseline-180/`、`jobs/artifact-migrator/controlled-output/retrain-172/`；收口：同目录正式审批、对象注册、备份和回滚校验记录 | `make verify-p6-01` | `baseline-180` 与汇总仍为 `BLOCKED`，审批为 `DRAFT`；需验证两版本分存、全量数量/大小/哈希/标签/掩膜/家族泄漏、原目录只读及对象备份回滚。 |
| P6-02 生产候选池和数据集构建 | 阻塞 | 数据集负责人 | 2026-08-14 | 现有：`jobs/dataset-builder/controlled-output/production-candidate-v1/`、`services/business-api/src/main/java/com/tooldefect/business/dataset/`；收口：不可变版本、质量审批、版本差异和后端集成测试证据 | `make verify-p6-02` | 本地精确/近似去重、许可、复核、掩膜和版本差异门禁已补；前置 P6-01、真实候选来源许可/复核关闭/质量批准和不可变发布证据仍缺失。 |
| P6-03 可复现训练任务 | 阻塞 | 训练平台主管 | 2026-08-28 | 现有：`jobs/training-pipeline/train.py`、`jobs/training-pipeline/controlled-output/`；收口：真实普通训练、两阶段重训练、恢复、隔离与失败检查点记录 | `make verify-p6-03` | 本地训练编排已复用真实 TensorFlow 两阶段实现并有严格门禁；前置 P6-02、真实已批准数据集/初始化模型/权重、平台资源隔离证明和可复核运行证据仍缺失。 |
| P6-04 统一评估、门槛和极坐标重新标定 | 阻塞 | 算法评估负责人 | 2026-09-04 | 现有：`jobs/model-evaluator/controlled-output/p2-baseline/`、`jobs/model-evaluator/recalibrate_polar.py`；收口：生产门槛版本及质量、工艺、算法三方签字记录 | `make verify-p6-04` | 固定 34 图、极坐标 v2、旧版拒绝和重复容差的本地检查已补；前置 P6-03、生产门槛版本及质量/工艺/算法三方签字仍缺，报告继续禁止生产声明。 |
| P6-05 模型登记、审批和签名 | 阻塞 | 模型注册与供应链安全负责人 | 2026-09-11 | 现有：`services/business-api/src/main/java/com/tooldefect/business/model/`、`services/inference-service/src/inference_service/model_runtime/verifier.py`；收口：模型包、软件物料清单、签名验真、质量/技术独立审批及不可变别名记录 | `make verify-p6-05` | 本地后端状态保护和签名包验证器已补；前置 P6-04 及真实模型包、SBOM、受信公钥、独立审批和不可变别名证据仍缺，当前门禁缺 5 项文件返回 2。 |
| P6-06 影子、灰度、正式发布和回滚 | 阻塞 | 模型发布负责人 | 2026-09-18 | 现有：`services/business-api/src/main/java/com/tooldefect/business/deployment/`、`services/inference-service/src/inference_service/model_runtime/slot.py`；收口：双槽验签、预热、影子/灰度观测、正式切换和真实回滚记录 | `make verify-p6-06` | 本地状态机、后端数据库保护和运行槽基础已补；真实双槽运行、预热、灰度窗口/最小样本、正式切换和回滚记录仍缺，当前门禁缺 7 项文件返回 2。 |
| P6-07 质量、数据集、训练和模型管理页面 | 阻塞 | 前端负责人 | 2026-09-25 | 现有：`apps/web-console/src/features/quality/`、`apps/web-console/src/features/datasets/`、`apps/web-console/src/features/models/`；收口：受版本控制的训练、审批、灰度和回滚页面及前端测试 | `make verify-p6-07` | 本地页面门禁已通过，但前置 P6-02 至 P6-06 未关闭，且真实模型/部署证据缺失；仍需完成全阶段前置、真实后端联调和现场闭环，不能仅凭前端门禁改为“已验证”。 |
| P6-08 模型生命周期端到端验收 | 阻塞 | 集成测试负责人 | 2026-10-02 | 现有：`tests/end-to-end/mlops/test_model_lifecycle.py`；收口：连接真实后端、训练、推理、数据库和对象存储的全链路证据 | `make verify-data verify-models test-integration test-e2e test-security` | 前置 P6-02 至 P6-07 未关闭；现有 MLOps 用例是内存状态机，不能作为通过证据。需证明任一生产结果可反查原图、预处理、算法、模型、数据集、训练、审批和部署版本。 |
| G6 阶段门禁 | 阻塞 | 技术负责人 | 2026-10-02 | 收口：`Docs/reports/P6-gate-acceptance.json` 及 P6-01 至 P6-08 全部不可变证据链接 | `make verify-g6` | P6 目前 0/8 已验证；入口必须汇总数据不可变、训练复现、三模型登记/淘汰、职责分离、签名、影子、灰度、回滚及在线推理不依赖可变根目录。 |
| P7-01 生产技术决策锁定 | 阻塞 | 生产架构负责人 | 2026-10-09 | 现有：`Docs/decisions/production-decision-closure.json`、`Docs/decisions/site-parameter-decisions.json`、`deploy/environments/production/site-config.yaml`、`tools/p7/`；收口：全部批准决策与预检输出 | `make verify-p7-01` | 五个预检脚本、结构化证据契约和严格入口已补；当前门禁零实现错误、明确返回 2。前置 G6 未关闭，18 项待现场签字，另缺技术清单、生产环境、签名模型、真实冒烟和 31 项预检结果。 |
| P7-02 真实相机和 PLC 适配 | 阻塞 | 采集与硬件负责人 | 2026-10-23 | 现有：`apps/edge-agent/src/edge_agent/adapters/vendor/`、`apps/edge-agent/scripts/hardware_acceptance.py`、`tools/p7/hardware.py`；收口：非模板现场配置、驱动清单和连续运行报告 | `make verify-p7-02` | 默认禁用/显式硬件启用、USB 打开、光电重复触发、序列/失败率/P95/稳定性和六类外部场景证据门禁已补，边缘测试 116/116。前置 P7-01、已选厂商 SDK 和真实设备报告仍缺，门禁返回 2。 |
| P7-03 数据迁移与生产恢复演练 | 阻塞 | 数据迁移与恢复负责人 | 2026-10-23 | 现有：`jobs/production-migration/`、`jobs/production-recovery/`、`tools/p7/migration.py`；收口：生产源目标对账、隔离恢复和实际恢复记录 | `make verify-p7-03` | 干跑/跳过/抽样/宽泛回滚/假恢复成功路径已封闭，33 项定向测试通过；门禁零实现错误返回 2。前置 P7-01 与 P6-01 未关闭，且缺生产迁移、六类资产全量对账、隔离恢复、RPO/RTO 和签署证据。 |
| P7-04 全量非功能验收 | 阻塞 | 非功能验收负责人 | 2026-11-13 | 现有：三类自动化回归、`tools/p7/nonfunctional.py`、报告模板；收口：非模板现场报告 | `make verify-p7-04` | 严格入口已执行故障 16/16、安全 18/18、性能 17/17 且零跳过；真实来源、签署阈值、故障注入、安全探测、容量/长期稳定性、告警恢复和四方批准门禁已补。前置 P7-02/P7-03 与现场报告仍缺，门禁返回 2、零实现错误。 |
| P7-05 质量试运行和模型门槛签字 | 阻塞 | 质量负责人 | 2026-11-27 | 现有：报告模板、`tools/p7/quality.py`；收口：真实工位/班次/批次试运行报告和四方签字 | `make verify-p7-05` | 结构化门禁已覆盖分层范围、分母闭合、置信区间、选择偏差、七类质量指标、新旧模型配对、研究集不得替代及质量/工艺/算法/发布四名独立批准；42 项相关测试通过。前置 P6-06/P7-02/P7-04 与真实试运行报告仍缺，门禁返回 2、零实现错误。 |
| P7-06 用户、运维和应急演练 | 阻塞 | 运维负责人 | 2026-11-20 | 现有：九类角色手册、20 场景目录、`tools/p7/operations.py`；收口：真实联系人和值班表、未参与开发人员签署的真实设备演练记录 | `make verify-p7-06` | 缺失六场景和五份角色手册已补，严格门禁验证外部用户、职责分离、原因、二次确认、审计及全部场景覆盖；46 项相关测试与 P5 手册门禁通过。前置 P7-04、真实联系人和现场演练仍缺，门禁返回 2、零实现错误。 |
| P7-07 上线就绪评审 | 阻塞 | 发布负责人 | 2026-12-04 | 现有：安全失败的上线清单与 `NO_GO/BLOCKED` 决定；收口：外部真实 `go-live-checklist.json`、`release-decision-record.json`、回滚目标、值班表和全部风险处置记录 | `make verify-p7-07` | 严格汇总已拒绝占位、模拟、未绑定证据、开放高风险、无效豁免、未预热回滚目标、值班空档及缺失四方签署；当前 78 个真实前置阻断、零实现错误并返回 2。 |
| G7 阶段门禁 | 阻塞 | 上线评审负责人 | 2026-12-04 | 现有：`Docs/reports/P7-gate-acceptance.json` 的真实 `BLOCKED` 基线；收口：引用 P7-01 至 P7-07 全部真实现场证据的已签 G7 记录 | `make verify-g7` | 严格验证九类 G7 要求、七项任务绑定、原始日志与四方签署；当前 100 个真实前置阻断、零实现错误并返回 2，P7 仍为 0/7 已验证。 |

### 状态变更记录

| 日期 | 任务 | 状态 | 变更人 | 依据 |
|---|---|---|---|---|
| 2026-07-31 | P6-01 至 P6-08、G6、P7-01 至 P7-07、G7 | 阻塞 | 规划维护者 | 依据阶段三十四独立复核：P6 0/8、P7 0/7，G6/G7 均未通过；统一为四态并登记逐项阻断。 |
| 2026-07-31 | 阶段三十五：建立整改唯一跟踪表 | 已验证 | 规划维护者 | 17 行整改记录的状态、负责人、期限、证据路径和验收命令结构校验通过；`git diff --check -- task_plan.md` 通过。 |
| 2026-07-31 | 阶段三十八：P6 阻塞再审计与可内生收口 | 阻塞 | 规划维护者 | 仓库内冻结数据、安全测试和后端完整门禁已修复并复验；P6-01 至 P6-06、P6-08 与 G6 仍因真实生产证据缺失安全返回 2，P6 权威状态保持 0/8。 |
| 2026-07-31 | 阶段三十九：P7 仓库内阻断收口 | 阻塞 | 规划维护者 | P7-01 至 P7-07 与 G7 严格入口及仓库内实现缺口已关闭；P7-07/G7 分别以 78/100 个真实生产前置阻断、零实现错误返回 2，未把外部证据缺失冒充通过。 |

### 阶段三十九：P7 阻塞审计与仓库内收口（2026-07-31）

- [x] 盘点 P7-01 至 P7-07、G7 的实现、证据、统一入口与真实失败原因
- [x] 关闭 P7-01 至 P7-03 可在仓库内解决的决策预检、硬件适配和迁移恢复缺口
- [x] 关闭 P7-04 至 P7-06 可在仓库内解决的非功能、质量试运行和运维演练缺口
- [x] 补齐 P7-07/G7 严格汇总，拒绝模板、干跑、模拟器、跳过项和缺失前置条件
- [x] 运行定向测试、P7 单项门禁、G7 与全阶段汇总，并按真实结果更新唯一跟踪表
- **安全边界：** 不生成现场签字、真实硬件运行、生产迁移、质量试运行或上线批准作为假证据；这些前置条件缺失时任务保持“阻塞”。
- **契约边界：** 当前契约版本 `v1`、OpenAPI 与生成包版本 `1.0.0`；若发现跨进程字段缺口，必须先改 `contracts/` 再生成三语言类型。
- **错误记录：** 首次读取 P7-03 资产时假定存在 `jobs/production-migration/README.md`，`awk` 返回 2；目录实际无 README，后续改为先按已列出的真实文件读取，不重复该命令。
- **错误记录：** 新增 P7 预检代码后首次使用系统 Python 3.9 的 `py_compile`，其仍尝试写入沙箱外 Apple Python 缓存并因权限返回 1；后续改用仓库 Python 执行内存 `compile()` 与单元测试，不重复写该缓存。
- **错误记录：** 五个预检入口首跑中配置检查返回 1，定位到生产模板写 `storage.protocol=S3`，与现有安全配置事实 `S3_COMPATIBLE` 不一致；已统一为 `S3_COMPATIBLE`，其余四个入口均按缺现场证据安全返回 2。
- **错误记录：** `make verify-p7-01` 首跑因直接执行 `tools/p7/verify.py` 时仓库根不在模块搜索路径，触发 `ModuleNotFoundError: tools`；已在入口最前显式加入解析后的仓库根，定向测试本身 7/7 不受影响。
- **错误记录：** P7-02 首轮 `make test-edge` 执行 116 项时 1 项错误，新增测试需要向 `hardware_acceptance.main` 注入参数，但入口尚无 `argv` 形参；已改为可注入参数且命令行行为保持不变，待复跑。
- **审计结论：** P7 七项与 G7 均存在仓库内可修复缺口，且所有任务仍缺真实现场前置。关键缺陷包括五个缺失预检脚本、硬件验收占位/零退出、迁移干跑假完成与无作用域回滚、非功能/质量合成数据假门禁、演练场景缺项以及条件上线弱化阻断。
- **P7-01 收口结果：** 五个缺失预检脚本已实现并接入 `make verify-p7-01`；7 项定向测试通过。严格入口当前汇总 67 个外部阻断、零实现错误并返回 2，脚本引用检查已 PASS；任务因 G6、18 项现场签字和真实生产证据缺失继续“阻塞”。
- **P7-02 收口结果：** 修复永久守卫、USB 不打开设备、光电只触发一次、待确认仍零退出、报告覆盖和稳定性占位；新增真实配置/设备清单/审批哈希、六类外部场景与严格报告验证。`make test-edge` 116/116，`make verify-p7-02` 零实现错误并按 P7-01 与真实报告缺失返回 2。
- **P7-07/G7 收口结果：** 新增 `tools/p7/release.py`、P7-07/G7 聚合入口、严格 G7 基线记录与发布验证测试；最终上线证据必须逐项绑定哈希，风险、豁免、回滚、值班、七项任务、九类门禁和四方签署全部安全失败。最终 59 项 P7 相关测试通过；`make verify-p7-07` 与 `make verify-g7` 分别返回 2，统计 78/100 个阻断且均为零实现错误。
- **最终验证结果：** P7 单元 59/59、边缘 116/116、故障 16/16、安全 18/18、性能 17/17、发布/追踪 19/19、手册、布局、P1 离线和 P1 严格门禁通过；`git diff --check` 通过。`make test-core` 的 3 个内部失败已清零，仍有 9 个因禁止提交的分类/多任务模型权重缺失而错误；因此 `make verify-all` 不能标记通过，且 G7 继续因真实现场证据缺失返回 2。
- **错误记录：** P7-03 严格入口首次补丁按错误的单行 `.PHONY` 上下文修改 Makefile，`apply_patch` 校验失败且未产生部分修改；已读取实际续行结构并改用精确上下文。
- **错误记录：** 值班覆盖补强的首次多文件补丁把终端拼接输出中的相邻 `evidence_path` 误当成同一字典内重复键，精确上下文校验失败且未产生部分修改；改为先读取带行号的真实片段，再拆分补丁。
- **错误记录：** 首次补充 P7-07 证据说明时给两个 v2 模式名误加了 `p7` 片段；对照验证器后已改为实际接受的 `tool-defect-go-live-checklist/v2` 与 `tool-defect-release-decision-record/v2`，并登记三个受控挂载环境变量。
- **错误记录：** 首次 `pnpm install --offline --frozen-lockfile` 因 pnpm 尝试在不可写的 `/Users/jinghong/_tmp_*` 创建临时文件而返回 255；未修改锁文件，后续只把临时/存储位置显式限定到工作区或 `/private/tmp` 后重试。
- **错误记录：** 改用精确 Node 20.13.1 + pnpm 10.34.5 后安装仍在 `/Users/jinghong/_tmp_*` 返回 255，说明根因不是版本而是 pnpm 向上识别了用户目录工作区或配置写入位置；停止盲目重试，先核对工作区根。
- **错误记录：** 在正确的 `packages/typescript-contracts/` 包目录以精确工具链离线安装时，锁文件校验通过，但本地存储缺 `typescript-5.9.2.tgz`，返回 `ERR_PNPM_NO_OFFLINE_TARBALL`；需要获准联网下载这一锁定依赖后再继续。
- **错误记录：** 第二次 `make verify-all` 在进入 `test-core` 后转为后台执行会话，包装层只返回未定义退出码且未暴露内部会话编号；沙箱内 `pgrep` 又因无法读取进程列表返回 3。已确认前置环境至数据门禁通过，后续改为运行独立目标并用受控进程检查确认是否仍在执行，不把部分输出记为全量完成。
- **错误记录：** 独立 `make test-core` 完整执行 192 项后有 3 失败、9 错误：9 个错误均为禁止提交的分类/多任务 `weights.h5` 缺失；1 个失败是旧测试仍期待 `PENDING_SITE_SIGNOFF` 而权威发布状态已安全改为 `BLOCKED/NO_GO`；2 个失败是需求矩阵计数从 352 增至 355 后冻结摘要未更新。只修复后两类仓库内漂移，模型权重缺失继续明确失败。
- **P7-03 收口结果：** 迁移/恢复编排已采用三态安全失败，拒绝无批次回滚、部分写入、未批准源、干跑完成、抽样代替全量和非生产恢复成功；严格门禁要求六类资产数量/字节/散列、源保留、六场景、RPO/RTO 与签署证据。33/33 定向测试通过；当前 70 个外部阻断、零实现错误并返回 2。
- **错误记录：** `make verify-p7-04` 首跑在 `test-faults` 失败，严格 unittest 子进程未把仓库根加入 `sys.path`，导致两个模块均无法导入 `tools`；已修复统一执行器，三个测试目录均零跳过通过。
- **P7-04 收口结果：** 新增结构化现场非功能证据门禁，区分确定性桩回归与真实故障/安全/性能证据，并要求签署阈值、零跳过、原图零丢失、告警触发恢复及四方独立批准。`make verify-p7-04` 完成 51 项自动化回归后按现场证据缺失返回 2，零实现错误。
- **P7-05 收口结果：** 新增机器可验证的质量试运行门禁，要求真实生产范围、完整分母/区间/抽样偏差、分层覆盖、七类质量指标、配对模型与四方独立签署，并明确拒绝 34 张研究集替代。`make verify-p7-05` 按前置与现场报告缺失返回 2，零实现错误。
- **P7-06 收口结果：** 新增正常、拒权、死信、完整回滚、证书吊销和紧急账号六场景及算法/发布/管理员/审计/安全手册；严格门禁要求九类外部用户、真实联系人和值班表、20 场景全覆盖以及原因/二次确认/职责分离/审计证据。`make verify-p7-06` 按两个现场证据缺失返回 2，零实现错误。
- **错误记录：** 修正上线清单时依据长输出误判 `GL-111` 有重复 `notes`，首个补丁上下文校验失败且未产生部分修改；精确读取确认文件无重复字段，已撤销该错误结论，仅更新真实过期的场景数量。
- **错误记录：** 随后的多文件更正补丁把 `progress.md` 标题误作为 `findings.md` 上下文，再次校验失败且未产生修改；已拆分为精确文件补丁完成更正。

### 阶段三十六：P6-01 阻塞收口（2026-07-31）

- [x] 核对 P6-01 迁移实现、受控输出、审计测试和统一入口
- [x] 增加不可变对象注册、备份/回滚验证和非空 `verify-p6-01` 入口
- [ ] 在缺少真实对象存储、正式审批或可恢复备份时保持 `BLOCKED`，不伪造通过
- [ ] 以真实命令结果更新 P6-01 状态；未满足完整现场标准时保持“阻塞”
- **当前边界：** 先收口可在本地安全验证的实现与门禁；正式审批、真实对象存储和备份回滚现场证据仍是外部前置条件。
- **错误记录：** 首轮 `make verify-p6-01` 返回 2；旧受控输出缺少 `source-snapshot.json`、对象注册、备份/恢复和正式审批，验证器同时重复输出逐行快照缺失。已改为缺快照单次报告并截断展示错误，未放宽失败条件。
- **错误记录：** 报告聚合补丁首轮出现两处 Python 缩进错误，门禁分别返回 1；已按错误行修正并重新运行，当前验证器能正常执行并安全返回 2。
- **错误记录：** 登记器首次使用仓库外临时输出时错误调用 `relative_to(REPO_ROOT)`，随后又把同哈希不同来源路径误报为对象冲突；已改为路径展示兼容仓库外目录，并按内容哈希合并来源路径。

### 阶段三十七：P6-02 数据集准入与不可变版本收口（2026-07-31）

- [x] 增加候选样本准入事实：来源许可、复核关闭、质量批准、普通/困难标记、采集与复核追踪标识
- [x] 增加精确/近似去重、跨划分哈希、标签冲突、家族泄漏、掩膜尺寸/像素内容和版本差异审计
- [x] 拒绝覆盖已有受控输出，生成双侧哈希清单、隔离清单、溯源和不可变标记
- [x] 增加非空 `verify-p6-02`，重新解码并复算最终清单，不把构建报告直接当作事实
- [x] 增加 4 项定向测试，覆盖旧 `sample_id` 映射、正样本空掩膜、近似跨划分、独立审批后严格复算和不可覆盖
- [ ] 不生成正式质量审批；当前真实候选仍缺许可/复核/质量证据，`make verify-p6-02` 必须保持阻塞
- **验证结果：** 定向测试 4/4 通过；`make verify-layout` 通过；`git diff --check` 通过；`make verify-p6-02` 返回 2，现有旧候选包缺 `diff-report.json`、`quarantine.json`，且旧审批仍为 `DRAFT`。
- **当前边界：** P6-01 对象/备份/正式审批前置条件仍未关闭；P6-02 唯一跟踪表状态继续为“阻塞”，不能仅凭本地临时有效样本把任务改为“已验证”。

#### P6-03/P6-04 本地实现与严格门禁（2026-07-31）

- [x] 将训练入口改为复用真实 TensorFlow 两阶段重训练；禁止删除受控输出、文本伪检查点和模拟指标
- [x] 锁定数据集版本/清单哈希、初始化模型哈希、配置、环境、代码工作树、随机种子、恢复契约和资源隔离记录
- [x] 增加非空 `verify-p6-03`，增加训练作业 4 项安全失败测试
- [x] 增加 P6-04 `verify-p6-04`，要求三候选固定 34 样本、极坐标 v2/旧版拒绝、重复运行容差、版本化阈值和三方独立签字
- [x] 重标定输出拒绝覆盖，新增 P6-04 门槛验证器 2 项测试
- [ ] 当前不伪造资源平台证明、生产阈值或质量/工艺/算法签字；`make verify-p6-03` 与 `make verify-p6-04` 必须保持阻塞
- **验证结果：** 重训练模块 4/4、P6-03 作业测试 4/4、P6-04 门槛测试 2/2、极坐标回归 17/17 通过；两个严格入口分别返回 2；`make verify-layout` 和 `git diff --check` 通过。
- **当前边界：** 真实数据集审批、初始化模型制品、资源隔离平台证明和生产门槛三方签字尚未提供；P6-03/P6-04 唯一跟踪表继续为“阻塞”。

#### P6-05/P6-06 契约、后端与数据库收口（2026-07-31）

- [x] 在 `contracts/` 先补齐模型登记、验证哈希、部署回滚目标、审批角色和 `If-Match` 的 v1 形状，并通过生成器更新 Python/Java/TypeScript 契约包
- [x] 模型登记后端强制训练运行、数据集、评估报告、门槛摘要、SBOM、签名密钥和制品定位；验证与发布审批人独立，终态更新受数据库触发器保护
- [x] 部署后端按实际 `model_deployment` 表列接线，要求当前/回滚版本均为 `APPROVED`，两角色审批写入独立审批表，使用 `If-Match` 和 `record_version` 防并发覆盖
- [x] 增加模型供应链与部署状态保护迁移 `V8__model_supply_chain_and_deployment_guards.sql`；加载失败、单审批、同版本回滚和未预热激活均保持失败
- [x] Java 主源码使用缓存依赖执行 `javac --release 25 -proc:none` 编译检查通过；本机 Maven Wrapper 可执行离线单测与真实 Failsafe 集成，但完整打包仍受插件缓存缺件阻断
- [ ] 不生成真实签名模型包、SBOM、独立审批、双槽预热、灰度指标或回滚记录；`make verify-p6-05`、`make verify-p6-06` 必须继续对缺失外部证据返回非零
- **验证结果：** P6-05/P6-06 验证器测试各 2/2；Java 主源码使用缓存依赖执行 `javac --release 25 -proc:none` 通过；`make verify-contracts-source`、`make verify-layout` 通过。`make test-backend` 仍受隔离 Maven 仓库无法解析 Spring Boot 父 POM 阻断；本机缓存离线 Wrapper 已完成 68/68 单测与架构测试，绕过打包直接执行的真实 Failsafe 集成为 11/11 通过。完整 `verify` 仍在打包阶段因缺少 `org.codehaus.plexus.archiver.FileSet` 失败，未把 Maven 全流程标为通过。
- **当前边界：** 后端实现已与 v1 契约和数据库列对齐，但真实模型供应链和推理平台运行证据仍缺失；P6-05/P6-06 唯一跟踪表状态保持“阻塞”，下一步进入 P6-07 前端消费者与 P6-08/G6 集成验收。

#### P6-07/P6-08/G6 前端与全链路收口（2026-07-31）

- [x] 按 v1 生成契约接线模型登记、验证、部署审批、`If-Match`、回滚和异步训练页面
- [x] 前端增加双角色审批、记录版本并发保护、危险操作二次确认、队列态不冒充完成和生产切换职责边界
- [x] 增加 P6-07 页面测试与非空 `verify-p6-07` 入口；完整 Node 20.13.1/pnpm 门禁通过
- [x] 增加非空 `verify-p6-08` 与 `verify-g6`，明确拒绝内存状态机、模拟证据、缺失生命周期链路和缺失阶段汇总
- [ ] 不把前端本地通过、37 项内存端到端场景或拆分自动化子集当作真实 P6 通过；缺少真实后端、训练、推理、数据库、对象存储和不可变链路证据时保持“阻塞”
- **验证结果：** `make verify-p6-07` 通过（类型检查、6 个测试文件/23 项测试、生产构建）；`make verify-p6-08` 返回 2，缺少真实组件证据目录；`make verify-g6` 返回 2，缺少 `Docs/reports/P6-gate-acceptance.json` 及 P6-01 至 P6-08 的全部 `PASS` 证据。`make verify-data` 返回 2，冻结资产组与稳定清单哈希不一致；分拆的 `make verify-models` 返回 0 但明确 `production_claim_allowed=false`，`make test-integration` 为 5/5，`make test-e2e` 为 37/37 内存场景，`make test-security` 为 18 项中 1 项跳过。
- **当前边界：** P6-07 仅完成本地消费者和页面门禁，因 P6-02 至 P6-06 前置及真实后端证据未闭合，唯一跟踪表仍为“阻塞”；P6-08/G6 不得由内存场景、模拟器、缺失前置条件或跳过测试升级为“已验证”。

#### P6-05/P6-06/P6-08 真实后端复验与历史记录安全边界（2026-07-31）

- **真实后端复验：** Docker 29.6.2 下使用 PostgreSQL、RabbitMQ、MinIO Testcontainers，`DatabaseMigrationIT` 7/7、`S3StorageIT` 1/1、`ApplicationHealthIT` 1/1、`ReliableMessagingIT` 2/2，共 11/11 通过；该结果只证明后端基础设施集成，不替代真实模型、对象注册、审批、双槽、灰度和回滚证据。
- **本地实现修复：** CSRF 过滤器改为只挂入人员安全链并禁用全局 Servlet 注册，避免拦截 edge JWT/内部回调；RabbitMQ mandatory return 增加异步退回回调等待，确保不可路由消息不被误报为确认成功；历史模型/部署空供应链字段可被读取但状态推进明确进入 HOLD，不能审批、激活、部署或回滚。
- **新增验证：** 模型/部署状态机测试 7/7 通过，覆盖独立审批、影子零流量、回滚目标和历史空证据安全失败；契约保持 `v1`、OpenAPI/生成包 `1.0.0`，未新增跨进程字段。
- **当前边界：** `make verify-p6-01` 至 `make verify-p6-06`、`make verify-p6-08` 和 `make verify-g6` 仍按缺失真实证据返回阻塞；`make verify-p6-07` 本地通过，但全阶段状态仍为 P6 0/8、G6 阻塞。完整 Maven `verify` 的唯一当前环境阻断是本地插件缓存缺 `org.codehaus.plexus.archiver.FileSet`，真实 Failsafe 已通过专用绕过打包命令复验。

#### P6-05/P6-06 接口权限收紧（2026-07-31）

- [x] 将数据集创建/审批、模型登记/验证、部署创建/审批/回滚和查询接口加入人员安全链的最小权限匹配；未命中权限不再只凭“已认证”放行。
- [x] 部署审批根据请求角色绑定 `dataset:approve` 或 `model:deploy:approve`，并从本地会话 `LocalIdentity.userId` 取稳定业务身份，避免使用 record `toString()` 造成审批主体错误。
- [x] 不改变 v1 网络字段；后端离线 Maven 单测/架构测试 68/68 通过，`javap` 已确认新增权限和身份分支进入编译类；`mvn clean` 仍因本机缺 `maven-clean-plugin` 的 `org.codehaus.plexus.util.Os` 失败，不能把 clean 构建标为通过。
- **当前边界：** 该修复只关闭接口级越权缺口，不产生真实模型包、SBOM、独立审批、双槽预热、灰度或回滚现场证据；P6-05/P6-06 及其前置仍保持“阻塞”。

#### P6-02/P6-07 数据集契约与后端集成复验（2026-07-31）

- [x] 先修改 `contracts/openapi/tool-defect-api-v1.json`，登记数据集版本列表、版本详情和版本差异的 v1 网络形状，再通过 `make generate-contracts` 更新三语言生成包。
- [x] 新增 V9 数据库迁移，建立候选清单登记表、审批状态约束、已批准清单不可变触发器，并把候选清单、用途和 `record_version` 接入数据集版本；不对历史行猜测回填。
- [x] 后端创建工作流严格要求已登记且已批准候选清单，拒绝缺失、跨数据集、未批准、非 UUID 请求者和审批人本人发起的发布；创建和详情响应按契约返回。
- [x] 后端版本列表使用不透明游标和稳定排序，版本差异按样本键复算新增、删除、修改和未变更计数；前端数据集服务改用生成契约类型，不再复制网络字段。
- [x] 补充数据集工作流测试，并完成真实 PostgreSQL/Flyway 迁移验证；前端类型、测试和生产构建门禁通过。
- [ ] 不把本地候选构建、后端基础设施集成或前端本地门禁当成生产候选批准；真实对象存储、许可/复核/质量审批、训练/模型/部署证据仍缺失，唯一跟踪表状态保持“阻塞”。
- **契约与验证结果：** 契约版本 `v1`、OpenAPI/生成包版本 `1.0.0`，生成源哈希为 `dc4fcbf59b91260bed31e409bc78fb82d56a4a401512c8d9b2ed5fe49772ed0d`；`make verify-contracts-source`、`make verify-layout`、`git diff --check` 通过；离线 Maven 单元/架构测试 70/70 通过；此前 V9 后真实 Failsafe 集成 11/11 通过（DatabaseMigrationIT 7/7、S3StorageIT 1/1、ApplicationHealthIT 1/1、ReliableMessagingIT 2/2）；`make verify-p6-07` 通过（类型检查、6 个测试文件/23 项测试、生产构建）。
- **严格门禁结果：** `make verify-p6-01` 返回 2（冻结数据、对象注册、备份/回滚和正式审批证据缺失）；`make verify-p6-02` 返回 2（缺 `diff-report.json`、`quarantine.json`）；`make verify-p6-03` 返回 2（缺真实训练制品和运行锁）；`make verify-p6-04` 返回 2（缺评估、极坐标和生产门槛包）；`make verify-p6-05` 返回 2（缺 5 项供应链证据）；`make verify-p6-06` 返回 2（缺 7 项部署证据）；`make verify-p6-08` 返回 2（缺真实组件证据目录）；`make verify-g6` 返回 2（缺阶段汇总及八项 PASS 证据）。这些非零结果均为安全失败，不是通过。
- **工具链限制：** `mvn clean compile` 仍因本地离线缓存缺 `org.codehaus.plexus.util.Os` 失败；普通离线 `mvn test` 已通过，不能将缺失的 clean 插件或真实生产证据隐瞒为通过。

#### 阶段三十八：P6 阻塞再审计与可内生收口（2026-07-31）

- [x] 恢复 `planning-with-files` 现场并确认唯一跟踪表仍为 P6 0/8、G6 阻塞
- [x] 逐项复跑 P6-01 至 P6-08、G6 及其基础门禁，形成当前阻断清单
- [x] 修复仓库内可安全闭合的技术阻断，不伪造对象存储、签字、真实训练或现场运行证据
- [x] 复验完整门禁并更新证据、错误记录与状态；未满足真实验收标准的任务继续保持“阻塞”
- **首轮严格入口结果：** P6-01 返回 2、365 项错误（含大量源路径大小写漂移及缺少源快照/对象注册/备份回滚/正式审批）；P6-02 返回 2、缺 `diff-report.json` 与 `quarantine.json`；P6-03 返回 2、缺 14 项真实训练制品；P6-04 返回 2、缺 3 项评估与门槛制品；P6-05 返回 2、缺 5 项供应链制品；P6-06 返回 2、缺 7 项部署制品；P6-08 返回 2、缺真实证据目录；G6 返回 2、缺阶段报告与八项任务证据。所有非零均按安全失败保留，尚未重复执行。
- **P6-01 根因发现：** 冻结 CSV 使用小写图片目录，真实图片目录首字母大写；旧迁移在大小写不敏感文件系统上未识别漂移，现有受控包又早于来源快照与新版审批字段。旧包不得覆盖，后续只能以新版本验证迁移修复；180 样本基线的 4 个跨划分哈希问题和 1 个家族泄漏继续保持阻断。
- **P6-02 根因发现：** 当前缺失的差异/隔离文件只是旧模式包的首层错误；冻结 `retrain.csv` 不含许可、复核、质量、难例、采集和复核追踪事实，且沿用大小写漂移路径。不得用默认值补造真实准入证据，P6-02 继续等待真实候选清单和独立质量审批。
- **基础门禁错误：** `make verify-data` 返回 2，冻结资产数量/字节/聚合哈希和稳定摘要漂移；`make test-security` 虽返回 0，却因缺 `config/audit-schema.json` 跳过 1 项。前者须定位真实漂移，后者须改为显式失败或补齐真实前置，均不得维持假绿。
- **基础门禁定位：** 冻结资产漂移只发生在 `design_documents`（文件数不变、总字节增加 257）；安全统一入口当前完全透传 `unittest` 返回码，无法拒绝跳过。下一步审计文档相对冻结提交的精确差异，并为统一测试入口增加“零跳过”执行语义。
- **路径治理限制：** 工作树为 `Docs/`、Git 索引为 `docs/`，按工作树大小写查询历史提交会得到空结果；冻结文档差异必须用索引路径复核，未经协调不得顺手执行目录大小写迁移。
- **冻结门禁修复决策：** 257 字节是提交 `b7667d2` 的合法设计更新；资产扫描应像源码/配置/测试一样从 `97c88cb` 读取冻结文档，并规范显示为 `Docs/`，而不是刷新锁或回退现行文档。试算已精确重现锁定摘要。
- **已关闭技术阻断：** `make verify-data` 已恢复通过；新增严格 unittest 执行器后，跳过/预期失败会返回 2，3 项执行器单测通过，`make test-security` 当前对缺失审计前置明确失败。
- **审计测试决策：** 不创建违反契约唯一来源的 `config/audit-schema.json`；改为验证现有 V1 `audit_log` 字段、V2 只追加触发器和启用配置。真实防篡改归档与现场签字不由静态测试冒充。
- **基础复验结果：** `verify-data`、`verify-models`、`test-integration` 5/5、`test-e2e` 37/37、`test-security` 18/18 和 `verify-p6-07` 23/23 加生产构建均返回 0，且目录测试零跳过；模型报告仍禁止生产声明，集成/端到端仍不是 P6-08 真实组件证据。
- **后端完整门禁错误：** 首次 `make test-backend` 在沙箱内因 Maven Central DNS 失败返回 2；获准联网后的第二种尝试已完成 78/78 单测和 JAR 打包，但 Spring Boot 重打包下载 `commons-lang3:3.16.0` 时远端中断 TLS，完整 `verify` 返回 2且未进入 Failsafe。第三次必须改用校验后的精确制品补齐与离线复验，不重复相同联网命令。
- **制品补齐错误：** Maven Central 不提供该 JAR 的 `.sha512` 伴随文件，首次校验元数据请求返回 404；不再重复该 URL，改取官方 `.sha1`，下载 JAR 的 SHA-1 `3eb54effe40946dfb06dc5cd6c7ce4116cd51ea4` 与官方值精确一致。
- **后端完整门禁收口：** 校验后的 `commons-lang3:3.16.0` 已写入项目隔离缓存；统一入口 `make test-backend` 随后完成 78/78 单元与架构测试及 11/11 真实 PostgreSQL、MinIO、应用健康、RabbitMQ 容器集成测试，零失败、零错误、零跳过并构建成功，数据库迁移验证到 V11。沙箱内 Docker 套接字权限失败不计通过，最终容器复验在受控提权环境执行。
- **最终复验结果：** `verify-p6-01` 至 `verify-p6-06`、`verify-p6-08`、`verify-g6` 均按缺失真实证据返回 2；`verify-p6-07` 返回 0。五项目基础命令整体返回 0，其中冻结数据摘要为 `37301fdc...c396ba`、集成 5/5、内存端到端 37/37、安全 18/18 且零跳过；模型技术报告仍为 `production_claim_allowed=false`。契约源、布局、差异检查及冻结资产/严格执行器定向测试 12/12 通过。
- **当前边界：** 大量 P6/P7 文件为既有未提交改动，续办期间只做可归因的增量修改，不覆盖或回退他人工作；外部证据缺失继续按安全失败处理。

## 历史阶段记录（非 P6/P7 状态跟踪表）

### 阶段一：需求盘点与基线核查

- [x] 提取 P0–P2 全部任务、依赖、交付物和通过标准
- [x] 检查仓库结构、已有实现、测试与目录级约束
- [x] 建立“任务—现状—缺口—验证”矩阵
- **历史结论：** 已完成

### 阶段二：P0 工程地基

- [x] 按依赖顺序完成 P0 全部任务
- [x] 执行 P0 任务级验证与阶段门禁
- **历史结论：** 已完成（资产差异按设计要求冻结为阻断清单）

### 阶段三：P1 核心领域与数据基础

- [x] 按依赖顺序完成 P1 全部任务
- [x] 执行 P1 任务级验证与阶段门禁
- **历史结论：** 已完成；源码门禁全绿，Docker/Compose 已确认可用，Compose 只读配置校验通过。严格汇总命令因平台审批额度未启动，但各组成项已有独立成功证据。

### 阶段四：P2 核心服务与接口

- [x] P2-A01 插件开发包和统一内存对象
- [x] P2-A02 现有预处理和算法适配器
- [x] P2-A03 模型包、校验和隔离加载
- [x] P2-A04 三组现有双任务模型技术评估
- [x] P2-B01 模块化单体与领域边界
- [x] P2-B02 首批数据库迁移与约束
- [x] P2-B03 S3 兼容存储适配和上传确认
- [x] P2-B04 发件箱、收件箱和可靠消息基础
- [x] P2-C01 SQLite、本地目录和原子落盘
- [x] P2-C02 触发、相机适配端口和模拟器
- [x] P2-C03 同步、退避、对账和心跳客户端
- [x] P2-D01 推理编排、运行槽和服务健康
- [x] P2-W01 前端壳、鉴权、权限和统一请求层
- [x] 复验 P2-A/D、P2-C、P2-W 当前工作树门禁
- [x] 完成 P2-B Docker/Testcontainers 集成验证
- [x] 完成 P2-A04 三组真实候选模型评估
- [x] 执行 G2 阶段门禁并逐条保存证据
- **历史结论：** 已完成；五条工作流均可独立构建和测试，命令行、插件、数据库、存储、消息、采集、推理、可信模型与前端契约证据全部通过。

### 阶段五：联合验收与交付

- [x] 运行 P0–P2 全量测试、静态检查和门禁（已执行，外部前置条件按安全失败保留）
- [x] 核对任务覆盖矩阵与文件冲突
- [x] 整理交付说明和验证证据
- **历史结论：** 已完成；P0–P2 共 22 项任务和 G0/G1/G2 均已按任务口径交付，已知历史资产限制继续显式保留。

### 阶段六：P3 自动检测纵向闭环

- [x] P3-01 至 P3-07
- [x] G3 阶段门禁
- **历史结论：** 已完成；自动检测、采集安全降级、工位页面、契约集成和八类端到端场景均已通过。

### 阶段七：P4 后端人工复核闭环

- [x] P4-01 复核任务池、认领和租约
- [x] P4-02 复核提交、修订、二审和最终处置
- [x] P4-03 人工掩膜上传与标注完整性
- **历史结论：** 已完成；V5 迁移、复核应用服务、数据库范围与并发约束、人工掩膜确认和不可变审计均通过单元及真实容器集成验证。

### 阶段八：P4 前端、安全和人工闭环

- [x] P4-04 复核任务池和工作台
- [x] P4-05 完整角色、数据范围和审计链
- [x] P4-06 人工闭环端到端验收
- [x] G4 阶段门禁
- **历史结论：** 已完成；复核工作台、角色职责分离、对象访问审计、16 项纵向场景和文档规定的四入口联合门禁全部通过。

### 阶段九：P4 现状盘点与契约核查

- [x] 恢复 P3 未提交工作树和既有验证证据
- [x] 提取 P4-01 至 P4-06 的依赖、交付物、验证方法和通过标准
- [x] 核对 02/04/05/06/07/09/12 号设计文档、冻结契约、数据库基线和当前实现缺口
- [x] 建立 P4 “任务—现状—缺口—验证”矩阵
- **历史结论：** 已完成

### 阶段十：P4 联合验收与交付

- [x] 执行 v1 契约兼容、确定生成和三语言编译
- [x] 执行后端单元、PostgreSQL、MinIO、RabbitMQ 与应用健康集成验证
- [x] 执行 `make test-backend test-web test-e2e test-security`
- [x] 执行布局、契约集成、P1 离线源码回归和差异格式检查
- [x] 更新任务矩阵、错误记录、验证证据与已知限制
- **历史结论：** 已完成；P4-01 至 P4-06 和 G4 均达到任务文档的通过标准。

### 阶段十一：P5 需求盘点与可靠性基线核查

- [x] 提取 P5-01 至 P5-07 的前置依赖、交付物、验证方法和通过标准
- [x] 核对 01/07/10/11/12/14 号设计文档与当前 P0–P4 实现
- [x] 建立 P5 “任务—现状—缺口—验证”矩阵
- **历史结论：** 已完成

### 阶段十二：P5 可靠性与可观测性基础

- [x] P5-01 重试、补偿、对账、死信和孤儿处置
- [x] P5-02 结构化日志和全链路追踪
- [x] P5-03 指标、看板、服务目标和告警
- **历史结论：** 已完成

### 阶段十三：P5 安全、生命周期和恢复

- [x] P5-04 身份、网络、密钥和模型供应链安全
- [x] P5-05 生命周期、容量、备份和恢复
- **历史结论：** 已完成

### 阶段十四：P5 韧性验证与运行手册

- [x] P5-06 故障、性能和长期稳定性测试
- [x] P5-07 分级运行手册
- [x] G5 阶段门禁
- **历史结论：** 已完成

### 阶段十五：P5 联合验收与交付

- [x] 执行 P5 任务级验证、全量回归与阶段门禁
- [x] 更新覆盖矩阵、验证证据、错误记录和已知限制
- [x] 核对工作树边界并整理任务交付说明
- **历史结论：** 已完成；P5-01 至 P5-07 和 G5 自动化门禁全部通过，真实现场目标与人员演练按计划保留到 P7。

## 已知限制清单

1. P0 冻结基线明确要求保持缺失状态的旧分类与旧双任务 Git LFS 权重仍不在工作树；原始 `python -m unittest discover -s tests` 会有 9 项资产错误。不得用 P2 三组重新训练候选替代不同结构/哈希的历史制品。
2. 三组 P2-A04 模型仅登记为 `TEST_CANDIDATE`，34 图技术评估不能形成生产质量或发布声明。
3. 最终复验回合未启动 `make verify-p1-strict` 汇总命令：Docker 套接字需要受控权限，而平台审批额度已用尽。其源码门禁、Compose 只读配置检查以及真实 Docker/Testcontainers 后端门禁已分别成功，但不得据此声称该汇总命令本身通过。
4. `make verify-all` 会包含上述严格汇总入口以及 P0 已知缺少旧权重的核心全量测试，因此没有把它标记为全绿；任务级和阶段级证据以 `progress.md` 中逐项结果为准。
5. P3 端到端门禁使用模拟设备、真实采集/推理生产模块和契约级中心替身；真实 PLC、相机及生产外部服务仍属于后续现场验收范围，未被标记为已验证。
6. 推理工作进程为抵御确认丢失保留最近 4096 条已接受结果清单；更早的极端延迟重复消息仍由后端幂等与单结果约束阻断重复业务事实，但可能重新执行推理。
7. P4 纠错修订、优先级调整和训练准入决定已在应用服务、数据库及自动化测试中实现；06 号设计文档未定义这些管理动作的公开 v1 端点，因此本阶段没有擅自扩展浏览器管理界面。
8. P4 端到端测试以确定性契约场景覆盖人工状态链，真实 PostgreSQL、MinIO、RabbitMQ 和 Spring 容器由独立后端集成门禁覆盖；真实 OIDC 提供方、现场浏览器和生产对象存储仍属于部署环境验收。
9. 布局验证继续报告两个既有治理警告：183 个迁移前冻结数据文件在 P6 注册前不得变更；Git 索引中的 `docs` 大小写迁移需由协调任务单独执行。
10. P5 容量、节拍、端到端延迟、恢复时间和恢复点目标没有现场签字；所有计算目标均保持 `PENDING_SIGNOFF`，确定性负载结果不得替代 P7 真实环境证据。
11. P5 已完成六组件隔离联合恢复和独立机器执行器演练，但没有连接生产数据库、生产对象存储、真实设备或生产模型；未参与开发人员的现场演练仍属于 P7-06。
12. 生产安全交付为可验证的部署基线和拒绝策略；真实 OIDC、证书签发/吊销、已签名镜像摘要、外部机密提供者和生产网络策略须在 P7 真实基础设施上验收。
13. 本机未缓存 Prometheus 3.5.0 镜像，因此没有执行官方 `promtool`；监控资产已通过 Ruby YAML 解析、离线结构门禁和六项告警触发/恢复演练，生产镜像验收仍需补官方工具复验。

## 关键问题

1. P0–P2 分别包含哪些任务，任务间的精确依赖关系是什么？
2. 哪些交付物已经存在且达到标准，哪些需要补齐或重构？
3. 文档中的命令是否可直接运行，若环境缺少依赖应如何提供等价验证？
4. 是否存在嵌套 `AGENTS.md` 或未提交改动需要避让？

## 已作决定

| 决定 | 理由 |
|---|---|
| 以任务文档的逐项通过标准作为唯一完成口径 | 避免把“文件存在”误判为“任务完成” |
| 先盘点再分工作流并行实施 | 降低跨目录冲突并缩短总耗时 |
| 保留并避让用户现有改动 | 遵守共享工作区的非破坏性要求 |
| 跨平台路径校验同时使用 POSIX 与 Windows 路径语义 | 避免在 Linux/macOS 上漏过 Windows 绝对路径和 UNC 路径 |
| P3 继续消费冻结的 v1 契约，不为内部实现细节扩展网络字段 | 现有 12 个自动检测操作已覆盖纵向闭环，避免无必要的契约漂移 |
| 已接受结果清单采用有界保留而非立即删除 | 覆盖“后端接受但消息确认丢失”的重复投递窗口，同时限制工作进程本地元数据增长 |

## 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 整改表首轮结构校验把错误记录中的 `P7-07` 行误计为任务 | 1 | 不重复原范围；将解析区间收紧到“P6/P7 整改唯一跟踪表”与“状态变更记录”之间，再验证 17 行字段和四态枚举 |
| 枚举 P6/P7 证据时使用了不存在的 `apps/inference-service/` 路径 | 1 | 不重复猜测路径；后续从仓库根用 `rg --files` 定位真实推理服务目录，并只登记实际存在的证据路径 |
| `planning-with-files` 完成检查脚本只识别到 1 个阶段并返回未完成 | 1 | 脚本不能解析本项目中文阶段结构，且 P6/P7 实施任务确实仍未完成；审计阶段本身以阶段三十四复选框和最终矩阵为完成依据 |
| 枚举可选 `config/` 目录时 `rg` 因目录不存在返回 2 | 1 | 已确认这正是安全测试跳过的缺失前置条件；后续只检查已存在精确路径，不重复该查询 |
| P7-07 规定的 `make verify-all` 严格门禁失败 | 1 | 环境检查未识别 TypeScript 编译器并立即停止；不重复整条命令，结合已分拆执行的数据、网页、后端和 P7 自动化入口判定 |
| 离线缓存 + Docker 权限下后端 `verify` 真实失败 | 1 | 61 项单元/架构测试通过；11 项集成中 5 失败、1 错误，问题集中在 V7 迁移断言/重复权限、跨站请求伪造错误码和健康端点预期；本轮只审计不修复 |
| `make test-backend` 未进入编译：缺 Spring Boot 4.1.0 父 POM | 2 | 首次为沙箱域名解析失败，联网重试为 TLS 握手中断；停止重复联网路径，改查此前隔离缓存并执行离线验证 |
| 文档规定的 G6 命令在 `verify-data` 阶段失败 | 1 | 记录冻结资产组数量/字节/聚合哈希及稳定清单摘要不一致；不重复整条命令，后续分别执行其余独立目标并诊断数据门禁 |
| P6/P7 复核执行 `make test-web` 在类型检查阶段失败 | 1 | 保留 11 组 P6 请求层、数据集、训练、模型和质量类型错误原始结果；不重复同一命令，继续执行独立门禁定位其他任务状态 |
| 读取统一入口清单时查询了不存在的 `tools/verify-layout/targets.json` | 1 | 已确认全部目标映射直接定义在 `run_target.py`，后续只读取实际文件，不重复查询该路径 |
| 基线 `unittest`：1 个失败、9 个错误 | 1 | 9 个错误源于被忽略的分类/双任务权重缺失；绝对路径缺陷已修复并通过定向测试，外部资产测试仍需按基线清单处理 |
| 更新规划记录时补丁上下文不匹配 | 1 | 重新读取当前文件并使用精确上下文更新，没有重复原补丁 |
| 边缘测试首次收集出现循环导入 | 1 | `capture` 包入口不再提前导入依赖硬件端口的协调器，调用方改为从子模块显式导入 |
| 首次读取 Node.js 官方发布索引失败 | 1 | 本机残留代理 `127.0.0.1:7897` 不可用；按网络权限流程重试后成功确认 Node 24.18.0 |
| 前端首次安装因沙箱网络权限失败 | 1 | 中止自动重试后按受控网络权限重新执行，依赖与锁文件生成成功 |
| 前端首次类型检查无法启动 | 1 | `vue-tsc` 3.3.8 与 TypeScript 7.0.2 的包导出不兼容；按其发布元数据锁定 TypeScript 6.0.2 |
| pnpm 11.9.0 无法在 Node 20.13.1 启动 | 1 | 官方兼容矩阵确认 Node 20 支持 pnpm 8/9/10、不支持 11；按用户降级决定同步锁定 pnpm 10.34.5 |
| 本机 Corepack 0.28.0 无法验证新 pnpm 签名 | 1 | 不升级或污染系统安装，改用 `npx --yes pnpm@10.34.5` 运行项目锁定版本 |
| pnpm 10 首次非交互重建依赖目录被中止 | 1 | 显式设置持续集成模式；随后按清单变更使用 `--no-frozen-lockfile` 生成新锁，完成后恢复冻结安装 |
| pnpm 10 持续集成模式拒绝旧锁文件 | 1 | 这是预期漂移保护；仅本次依赖基线变更使用 `--no-frozen-lockfile` 更新 |
| Vue Router 5.2.0 与 Vite 6 存在对等依赖冲突 | 1 | 锁定兼容 Vue 3.5 且不要求 Vite 7/8 的 Vue Router 4.6.4 |
| 查询不存在的 Vue Router `latest-4` 标签返回 404 | 1 | 改为查询完整 4.x 版本列表，确认最新稳定版为 4.6.4 |
| Node 20 工具链首次类型检查报告类型库与弃用错误 | 1 | 移除 TypeScript 6 已弃用的 `baseUrl`；使用仍属于 Node 20 系列且兼容 TypeScript 6/当前 DOM 测试库的 `@types/node` 20.19.43 |
| 第二次前端类型检查报告路径和第三方声明错误 | 1 | 无 `baseUrl` 时将源路径改为显式相对路径；只跳过第三方声明库内部检查，项目源码与生成契约仍严格检查 |
| 第三次前端类型检查发现测试代码类型问题 | 1 | 补入 Node 测试类型、消除重复路由元数据并移除无效断言泛型 |
| 前端首次单元测试有一个测试文件加载失败 | 1 | 浏览器模拟环境会改写 `import.meta.url`；安全源码测试改从测试进程工作目录解析，不依赖个人绝对路径 |
| 通过 `npx` 执行离线冻结安装时仍尝试访问注册表 | 1 | `npx` 自身需要解析临时包；改为直接调用已校验的本地 pnpm 10.34.5 可执行文件 |
| pnpm 离线冻结安装因本地存储缺少 `tslib` 归档而失败 | 1 | 锁文件本身已通过一致性检查；按冻结锁联网恢复依赖且没有解析漂移，后续构建测试重新执行 |
| 搜索文档目录引用时正则中的反引号被 shell 解释 | 1 | 改用单引号包裹不含命令替换语义的正则后成功；后续 shell 查询避免未转义反引号 |
| 读取可选基线摘要时 zsh 对空通配符报错 | 1 | 改为使用已枚举出的精确文件名读取，不再依赖可能为空的 glob |
| 前端开发服务器首次监听本地端口被沙箱拒绝 | 1 | 按权限流程在 127.0.0.1:5173 启动临时服务，完成检查后立即停止 |
| 浏览器等待策略不支持 `networkidle` | 1 | 改用受支持的 `load` 状态并以 DOM、布局尺寸和控制台日志作为确定性检查 |
| Maven 当前镜像下载地址返回 404 | 1 | 改用 Apache 官方归档地址下载同一 3.9.15 发布包 |
| Apache Maven SHA-512 文件只有裸哈希，`shasum -c` 无法解析 | 1 | 直接计算归档 SHA-512 与官方裸哈希比较，完全一致后才解压 |
| Maven 首次编译无法创建默认 `~/.m2/repository` | 1 | 使用任务专属临时本地仓库 `/private/tmp/tool-defect-m2`，不修改用户 Maven 缓存 |
| 沙箱内 Maven 无法解析中央仓库域名 | 1 | 按网络权限流程重试；依赖下载完成后真实编译 86 个 Java 源文件成功 |
| P2-B 智能体收口回合传输中断 | 1 | 共享文件系统改动保留；要求智能体先只读核对已落盘状态后从原任务恢复，避免重复覆盖 |
| 采集端首次 `uv build` 无法读取用户缓存中的内部 `.git` | 1 | 按权限流程允许只读构建缓存后重试；源码包和 wheel 均成功生成到 `/private/tmp`，并清理源码树内临时 egg-info |
| P2-C 首轮 39 项测试未覆盖五个终审安全边界 | 1 | 独立只读审计提供失败复现；新增真实解码/启动复核/状态组合/回调投递/批次上限测试并继续扩展至 82 项 |
| 崩溃续传测试仍按旧语义期待 `OK` | 1 | 启动恢复必须保守进入质量警告；修正测试期望后复验 |
| 新增鉴权暂停测试时测试尾部误置入心跳用例 | 1 | 恢复测试边界并重新运行完整采集端测试 |
| 后端统一入口只执行 Surefire 单元测试 | 1 | 改为 Maven `verify`，让 `*IT` 与 Testcontainers 缺失前置条件显式失败；离线单元验证单独记录 |
| P1 源码门禁发现 HTTP 测试示例令牌触发密钥扫描 | 1 | 改为扫描器认可且语义明确的测试占位符，随后重跑密钥扫描与完整源码门禁 |
| `make verify-data` 仅有不存在实现的失败占位 | 1 | 新增只读统一入口，连续扫描两次并核对冻结锁、决策文件和未登记现场硬编码 |
| 严格环境检查只识别全局 Maven/Node/pnpm | 1 | 改为验证固定 Maven Wrapper，并按项目清单在全部候选路径中寻找精确 Node 20.13.1 与 pnpm 10.34.5 |
| 搜索 Node 版本引用时 shell 引号未闭合 | 1 | 简化为不含嵌套引号的单引号正则后完成扫描 |
| 用可选前端子目录 glob 读取源码时 zsh 对空匹配报错 | 1 | 改用已枚举的精确文件路径读取 |
| 更新进度时误用不存在的表格上下文 | 1 | 立即重读规划文件并按实际列表上下文更新 |
| P2-C 默认抖动可能缩短 `Retry-After` | 1 | 将服务端值设为硬下限，并增加默认 20% 抖动回归 |
| P2-C 轮询响应未校验请求/响应 `capture_id` 关联 | 1 | 生成适配器与同步服务双层拒绝错误关联，并增加重复最终响应幂等测试 |
| P2-W 旧令牌刷新可覆盖后续登录会话 | 1 | 内存会话绑定单调代次；旧刷新无论成功失败均转为 stale，且不得清除或覆盖新会话 |
| P2-W 较早 OIDC 回调晚到可覆盖或清除新登录 | 1 | 回调引导绑定会话代次与组件生命周期；卸载/旧回调只报 stale，不修改新会话 |
| OIDC 退出链路只清本地令牌，未调用已有吊销端点 | 1 | 先同步清除本地会话并导航登录页，再尽力异步吊销捕获的刷新令牌 |
| OIDC 吊销补丁首次上下文未匹配 | 1 | 分别读取运行时、组件和测试尾部后按精确上下文更新 |
| OIDC 运行时新增吊销方法后前端类型检查失败 | 1 | 补齐测试运行时替身，并给吊销 fetch mock 声明真实请求参数类型 |
| SSE 会话代次检查遮蔽真实刷新失败错误 | 1 | 先传播回调与鉴权失败，再用代次检查识别普通网络等待中的账号切换 |
| `make test-backend` 在测试前写入受限 `~/.m2` 失败 | 1 | Maven Wrapper 用户目录和依赖仓库改为仓库内忽略的 `.build/`，确保统一入口能到达真实集成测试 |
| 干净 `.build` Maven 缓存下载命中失效本机代理 | 2 | 停止该路径；后续只使用此前校验过的 `/private/tmp` Maven 分发与依赖缓存，不把网络失败标记为后端测试结果 |
| Maven 固定依赖联网重试因当前额度限制被拒绝 | 1 | 不再尝试联网；仅使用此前已校验的本机只读 Wrapper 缓存与任务专属依赖缓存做离线严格复验 |
| 最终 `uv build --no-build-isolation` 在 macOS 系统配置库崩溃 | 2 | 两次相同 panic 后停止重试；改用同一已锁定 setuptools 后端的离线 `pip wheel --no-build-isolation --no-deps` 成功构建 |
| 当前虚拟环境未安装 `build` 模块 | 1 | 不联网安装新工具，使用已安装 pip/setuptools 的等价 PEP 517 wheel 构建 |
| P2-A04 受控报告还包含两个候选 36/34 样本及顺序不一致 | 1 | 先核对配置与冻结清单，修正为三候选共享同一 34 图顺序；随后使用 `training/` 中哈希匹配的模型完成真实评估 |
| 记录 P2-A04 决策时补丁目标表格定位错误 | 1 | 已确认技术决策表位于 `findings.md`，改用实际上下文更新，不重复原补丁 |
| `verify-all` 提前调用 P3 以后测试入口 | 1 | 按文档“当前阶段”语义收口为 P0–P2 必需项，未来入口仍独立存在且不得空跑成功 |
| `planning-with-files` 停止脚本统计 0 个阶段 | 1 | 脚本只识别英文 `### Phase`/`**Status:**`，与项目强制中文记录不兼容；以本文件复选框和中文状态为准，不伪造完成 |
| 首轮真实 PostgreSQL 集成迁移失败 | 1 | V3 直接引用了被 PostgreSQL 截断/自动生成的长约束名；改为按唯一约束定义精确查找后删除，兼容空库与 V2 向前迁移 |
| 首轮 Spring 容器启动失败 | 1 | `JdbcInboxRepository`/`JdbcOutboxRepository` 为 `final`，无法被 Spring 数据访问异常转换代理；移除类级 `final` 后定向复验 |
| 首轮 MinIO 容器启动失败 | 1 | 测试 KMS 值把 32 位十六进制文本按 Base64 解码成 24 字节；改用 32 字节测试密钥的标准 Base64 表示 |
| 读取 P3 检测工作流测试时使用了不存在的文件名 | 1 | 不猜测测试类名，后续先用 `rg --files` 枚举真实路径再读取 |
| 更新 P4 规划记录时补丁包含不精确历史上下文 | 1 | 先用 `rg` 定位精确行，再拆分为小范围补丁更新 |
| 搜索后端测试配置时包含不存在的 `src/test/resources` | 1 | 后续只搜索已由 `rg --files` 确认存在的路径，配置基线以主资源和测试内动态属性为准 |
| P4 首次离线编译引用的临时 Maven 3.9.15 路径已不存在 | 1 | 不重复调用失效路径；先枚举当前 Wrapper 分发和可用缓存，再选择现存工具链 |
| P4 复核单测首次运行时 Mockito 在 JDK 25 无法自附加代理 | 1 | 不重复原运行方式；改用无需运行时代理附加的子类 MockMaker，再重新执行定向测试 |
| Context7 查询 Mockito 官方配置时连接失败 | 1 | 不重复网络查询；改为检查已锁定本地 Mockito 制品中的官方插件常量和扩展机制 |
| P4 人工掩膜首次测试编译有 3 处存储替身签名漂移 | 1 | 生产源码 145 个文件已编译；按编译器指出的构造器、仓储新端口和对象头字段逐项更新既有测试替身 |
| P4 首轮完整后端单测发现 review↔storage 模块环和 API 层越界 | 1 | 46 项业务测试中仅 2 项架构门禁失败；把标注端口所有权移到 storage 应用层，并将控制器校验异常改为 API 层契约异常 |
| 记录 P4 工作区契约发现时补丁标题上下文不精确 | 1 | 重新读取 `findings.md` 尾部，按实际“P4 前端设计方向”内容追加，不重复原补丁 |
| P4 工作区后端多文件补丁尾部包含错误导入上下文 | 1 | 确认补丁未部分生效；移除无关尾部并按接口、仓储、服务、控制器分组应用 |
| 读取 P4 后端配置时猜测了安全与异常处理文件路径 | 1 | 用 `rg --files` 找到实际的 shared/infrastructure 与 review/infrastructure 路径，后续只使用枚举结果 |
| 在业务后端子目录运行搜索时重复使用仓库根相对路径 | 1 | 搜索报错不影响随后的 Maven 编译；后续根据 `workdir` 使用 `src/...` 路径 |
| P4 前端样式补丁假设了不存在的旧式媒体查询 | 1 | 样式文件实际使用 `@media (width <= 960px)`；读取尾部后按真实锚点分开追加基础与响应式规则 |
| P5 可靠性服务大补丁包含空更新段 | 1 | 补丁校验阶段整体拒绝且未产生部分写入；移除空段后按新增文件重新应用成功 |
| P5 采集遥测补丁尾部锚点不匹配 | 1 | 工具整体拒绝且未部分写入；读取真实文件尾部后拆成导入/构造器、运行循环和辅助函数三个小补丁成功 |
| P5 生产安全验证器把正则空白转义写成字面量 | 1 | 首轮门禁无法解析实际 Compose 服务；修正三个解析正则后重新执行，不放宽任何安全断言 |
| P5 后端单测仍期待旧的通用领域异常 | 1 | 生产代码已把畸形消息明确分类为不可重试异常；同步收紧三项发布器断言，确保其会进入终止态而非重试 |
| P5 手册机器执行器首次直接运行找不到仓库命名空间 | 1 | 脚本入口显式把仓库根加入模块搜索路径；不改变演练断言，随后从统一入口重跑 |
| P5 后端严格门禁首次无法访问 Docker 套接字 | 1 | 57 项单元测试已通过，4 个集成类按安全失败；受控权限重跑后 PostgreSQL、对象存储、RabbitMQ 和 V6 迁移 11/11 通过 |
| 本机缺少 Prometheus 3.5.0 镜像 | 1 | 不联网拉取额外制品；使用 Ruby 解析全部监控 YAML，并由离线门禁与六项告警演练验证结构和行为，保留 `promtool` 现场限制 |
| P5 最终核心回归仍有 9 个历史权重缺失错误 | 1 | 109 项中其余 100 项通过；错误与 P0 冻结阻断清单完全一致，不伪造或提交不同哈希模型替代 |
| P5 指标复验首次使用了已失效的 Maven Wrapper 哈希目录 | 1 | 先枚举实际分发路径，再用现存 Maven 3.9.15 和隔离依赖缓存完成 59 项单测与 11 项容器集成测试 |
| P4 前端首轮类型检查报告 14 项严格类型错误 | 1 | 3 项来自 TypeScript 6 的 `ArrayBufferLike`/`BlobPart` 收紧，11 项来自可选链未排除 Canvas 上下文 `undefined`；改为显式复制 ArrayBuffer 与分步判空 |
| 复核工作区单测尝试模拟具体服务类失败 | 1 | 当前无自附加 MockMaker 只支持接口；测试改用真实 `DetectionQueryService` 加窄仓储替身，保持 JDK 25 无代理附加策略 |
| 复核工作区定向测试使用了不符合领域约束的追踪标识 | 1 | 换成固定 32 位小写十六进制测试追踪标识，不放宽生产校验 |
| 拼接输出边界让我误判复核记录 SQL 有重复列 | 1 | 用带行号的单文件精确片段复核，确认列清单正确，未修改生产 SQL |
| 首次搜索迁移表定义猜测了文件名 | 1 | 用 `rg --files` 确认实际 V1–V4 文件名后读取，不再使用猜测路径 |
| 定向数据库验证使用 `-DskipTests` 导致 Failsafe 也被跳过 | 1 | 该次成功不计验证结果；改用空 Surefire 选择并关闭“无指定测试失败”，只让 `-Dit.test=DatabaseMigrationIT` 执行 |
| 沙箱内数据库集成测试无法访问 Docker 套接字 | 1 | 按权限流程受控重跑，Docker 29.6.2 与 PostgreSQL 18.4 成功启动 |
| V5 首次真实迁移引用了不存在的追加守卫函数 | 1 | V2 的实际公共函数名是 `td_reject_fact_mutation`；V5 复用该已存在函数后重跑全部迁移场景 |
| 定向迁移复验把 Flyway 建模式记录计入版本数 | 1 | `migrationsExecuted` 已为 3；历史表断言改为只统计 `version IS NOT NULL` 的成功版本迁移，不把新建 schema 的基线记录误算为 V4 |
| Spring Boot 4 应用未自动执行 Flyway | 1 | Boot 4 把 Flyway 自动配置拆为独立模块；后端改用 `spring-boot-starter-flyway`，保留 PostgreSQL 数据库扩展 |
| 禁用业务消息后健康探针仍连接 RabbitMQ | 1 | Rabbit 健康贡献器按同一 `td.messaging.enabled` 开关启停，避免未启用依赖把受限健康端点置为 `DOWN` |
| 非 Web 消息集成上下文仍创建 HTTP 安全链 | 1 | 安全配置限定为 Servlet Web 应用，消息工作进程测试不再要求不存在的 `HttpSecurity` |
| MinIO 返回标题大小写的对象元数据键 | 1 | S3 适配边界按 HTTP 语义把元数据键规范化为小写，并拒绝大小写碰撞；业务校验不再依赖供应商返回键的大小写 |
| 健康隔离测试把默认拒绝 401 误判为端点暴露 | 1 | 应用端口对未知未认证路径可能先经安全过滤器返回 401；断言接受 401/404，并继续要求管理端口独立、无细节且为 UP |
| RabbitMQ 入站消息从错误属性读取持久化模式 | 1 | 出站单测继续检查 `deliveryMode`；真实消费后的集成测试改查 AMQP 入站映射的 `receivedDeliveryMode` |
| RabbitMQ 集成测试拒绝框架确认关联头 | 1 | Spring 为 mandatory return/发布确认注入 `spring_*` 传输头；测试只豁免该命名空间，非框架业务头仍严格限于冻结字段 |
| 最终 `verify-p1-strict` 权限审批未启动命令 | 1 | Codex 平台提示当前用量额度已达上限；不绕过 Docker 套接字权限，改执行无提权源码门禁，并保留本回合已完成的真实 Docker 后端证据 |
| P3 首次 Maven 增量编译未启动 Java 编译 | 1 | 隔离 Wrapper 缓存缺分发包且命中失效代理 `127.0.0.1:7897`；停止原命令，改查找历史已校验的 `/private/tmp` Maven 分发与依赖缓存做离线编译 |
| P3 推理清单首轮测试 4 项失败 | 1 | 重构摘要函数时误删仍被本地安全令牌使用的 `hashlib` 导入；恢复导入后 22/22 通过 |
| P3 前端首轮类型检查 1 项失败 | 1 | TypeScript 不从数组长度推断索引非空；显式读取首项并判断 `undefined` 后完整门禁通过 |
| P3 后端严格门禁首次受 Docker 套接字权限阻断 | 1 | 按权限流程在沙箱外重跑，Docker 29.6.2 与 Testcontainers 可用 |
| P3 后端严格门禁首次应用上下文失败 | 1 | 新 JDBC 查询仓储和事务服务误声明为 `final`，Spring 无法生成数据访问/事务代理；移除 `final` 后重跑 |
| P3 集成测试首轮使用了非冻结操作名并未解析参数引用 | 1 | 改为 OpenAPI 中真实 `operationId`，并解析公共参数 `$ref` 后通过 |
| P3 端到端首轮轮询间隔低于冻结下限 | 1 | 中心替身按契约返回 100ms，八个场景随后全部通过 |
| 已接受结果清单删除会使重复消息重新推理 | 1 | 增加已接受标记与 4096 条有界保留；重复消息直接返回同一摘要，崩溃窗口仅重放回调 |
| P3 最终源码门禁仍按 P2 阶段拒绝联合测试入口 | 1 | 将布局验证器当前必需集合提升为 P0–P3，仅继续拒绝 P5/P7 的故障与性能门禁 |
| P4 全量后端集成上下文缺少禁用存储时的标注端口 | 1 | `@ConditionalOnMissingBean` 用于组件扫描类时把候选自身计入判断；改为与 `td.storage.enabled` 同源的显式条件，禁用存储装配安全失败实现、启用存储装配真实实现 |

## 暂停点

- 2026-07-29：用户要求在最近一项任务完成后暂停。
- 已完成并验证 Maven Wrapper 3.9.15（仅脚本模式、固定发行包校验和、无 Wrapper JAR）。
- 2026-07-29：用户已要求继续；优先处理 P2-C/W 独立审计所列契约接线缺口，以及 P2-B 测试和容器集成收口。
- 2026-07-29：P2-C 与 P2-W 已完成实现和本地门禁，当前从 P2-B 最终审计、P1/P2 联合门禁与阻断项归档继续。

## 备注

- P6/P7 每完成一个整改阶段，只在本文件更新任务状态；`findings.md` 与 `progress.md` 可追加原始证据和执行日志，但不得形成第二套状态表。
- 重大技术决定前重新读取本文件。
- 所有失败命令和修复过程均记录，避免重复无效尝试。

## P3 实施续篇

### 阶段六：P3 现状盘点与契约复核

- [x] 完整提取 P3-01 至 P3-07 的依赖、交付物、验证方法和通过标准
- [x] 核查现有契约、后端、推理、采集端、前端实现与未提交改动
- [x] 建立 P3“任务—现状—缺口—验证”矩阵
- **历史结论：** 已完成

### 阶段七：P3 纵向闭环实现

- [x] P3-01 采集初始化、图片确认和检测提交
- [x] P3-02 检测任务、执行尝试和结果接受
- [x] P3-03 推理完整编排
- [x] P3-04 版本化处置规则引擎
- [x] P3-05 采集端在线与断网恢复集成
- [x] P3-06 工位页、检测列表和详情
- **历史结论：** 已完成

### 阶段八：P3 联合验收与交付

- [x] P3-07 自动检测端到端与契约回归
- [x] 执行任务级验证、G3 阶段门禁与相关前序回归
- [x] 更新追踪矩阵、交接记录、错误清单和已知限制
- **历史结论：** 已完成

## P6 历史审计记录（非状态跟踪表）

### 阶段十六：P6 需求盘点与基线核查

- [x] 完整提取 P6-01 至 P6-08 的前置依赖、交付物、验证方法和通过标准
- [x] 核查现有 jobs/、后端、前端、数据库、契约与核心算法的 P6 相关实现
- [x] 建立 P6 "任务—现状—缺口—验证"矩阵
- **历史结论：** 已完成

### 阶段十七：P6-01 历史数据导入与不可变基线

- [ ] P6-01 历史数据导入与不可变基线
- **历史结论：** 部分完成、任务未通过；18/18 审计测试通过，但总汇总和 baseline-180 为 `BLOCKED`，两套审批均为 `DRAFT`，未形成对象注册、备份和回滚验证证据

### 阶段十八：P6-02 生产候选池和数据集构建

- [ ] P6-02 生产候选池和数据集构建
- **历史结论：** 部分完成、任务未通过；存在构建器和后端骨架，但候选审批仍为 `DRAFT`，近似去重、来源许可、未关闭复核、真实质量审批和不可变发布闭环缺失

### 阶段十九：P6-03 可复现训练任务

- [ ] P6-03 可复现训练任务
- **历史结论：** 未完成；当前脚本只写文本伪检查点并删除历史输出，没有真实普通/两阶段训练、同运行恢复、初始化模型和资源锁，受控报告为 `BLOCKED`

### 阶段二十：P6-04 统一评估、门槛和极坐标重新标定

- [ ] P6-04 统一评估、门槛和极坐标重新标定
- **历史结论：** 部分完成、任务未通过；极坐标版本 2 的 17/17 测试通过，三候选技术评估可运行，但 `production_claim_allowed=false`，质量、工艺和算法负责人未完成生产门槛签字

### 阶段二十一：P6-05 模型登记、审批和签名

- [ ] P6-05 模型登记、审批和签名
- **历史结论：** 未完成；后端只有状态记录骨架，允许缺失训练/数据集/评估证据，没有软件物料清单、签名验真和独立质量/技术审批闭环

### 阶段二十二：P6-06 影子、灰度、正式发布和回滚

- [ ] P6-06 影子、灰度、正式发布和回滚
- **历史结论：** 未完成；没有推理双槽下载验签、预热、激活接线、灰度指标/观察窗口/最小样本量门禁或有效上一稳定模型回滚

### 阶段二十三：P6-07 质量、数据集、训练和模型管理页面

- [ ] P6-07 质量、数据集、训练和模型管理页面
- **历史结论：** 未完成；`make test-web` 在类型检查阶段失败，前后端路径和字段偏离冻结契约，训练页面被 Git 忽略，部署审批/灰度/回滚界面缺失

### 阶段二十四：P6-08 模型生命周期端到端验收

- [ ] P6-08 模型生命周期端到端验收
- **历史结论：** 未完成；37/37 Python 场景通过，但 MLOps 场景为独立内存状态机，不调用真实后端/训练/推理/数据库，规定五目标命令又在 `verify-data` 失败

### 阶段二十五：G6 阶段门禁验收

- [ ] 执行 G6 阶段门禁与全量回归
- **历史结论：** 未通过；`verify-data`、`test-web` 和后端真实容器门禁失败，三个候选仍禁止生产声明，真实训练、签名、独立审批、灰度与回滚未闭合

## P6 关键约束

1. 历史和生产数据均以不可变版本管理，原目录在注册、备份和回滚验证前只读保留。
2. 训练和评估可复现，三个历史模型已独立登记或明确淘汰。
3. 模型发布职责分离，制品签名、影子、灰度和回滚有效。
4. 在线推理不依赖根目录 `outputs` 或 `artifacts` 中的可变"当前模型"。
5. 数据库迁移仅由 `services/business-api/` 所有；不得提交图片、数据集、模型权重、运行输出或密钥。
6. 同一版本契约、数据库迁移编号和生成包版本必须串行修改。

## P7 历史审计记录（非状态跟踪表）

P7 大量依赖真实硬件和现场决策，遵守安全失败原则：缺失真实设备的事项标记为 `PENDING_HARDWARE`，不伪造通过。

### 阶段二十六：P7 需求盘点与基线核查

- [x] 完整提取 P7-01 至 P7-07 条款与 G7 门禁
- [x] 识别 18 项待现场签字决策、真实硬件依赖与可自动化部分
- [x] 核查 Docs/decisions/、deploy/、adapters/、runbooks/、tests/ 现有资产
- **历史结论：** 已完成

### 阶段二十七：P7-01 生产技术决策锁定

- [ ] P7-01 生产技术决策锁定
- **历史结论：** 阻断；22 项决策中 18 项 `PENDING_SITE_SIGNOFF`，生产配置仍含大量待签值，起飞前清单引用的 4 个校验脚本不存在

### 阶段二十八：P7-02 真实相机和 PLC 适配

- [ ] P7-02 真实相机和 PLC 适配
- **历史结论：** 阻断；20/20 单元测试证明待硬件安全守卫，但没有真实相机、PLC、传感器、驱动版本或连续运行验收，且 P7-01 前置依赖未满足

### 阶段二十九：P7-03 数据迁移与生产恢复演练

- [ ] P7-03 数据迁移与生产恢复演练
- **历史结论：** 阻断；22/22 测试仅覆盖纯函数、干跑和内存报告，生产迁移与真实隔离恢复未执行，恢复记录仍为空模板

### 阶段三十：P7-04 全量非功能验收

- [ ] P7-04 全量非功能验收
- **历史结论：** 阻断；自动化故障 16/16、性能 17/17、安全 17 通过加 1 跳过，但没有按已签字参数执行现场脚本，非功能报告仍是待签模板

### 阶段三十一：P7-05 质量试运行和模型门槛签字

- [ ] P7-05 质量试运行和模型门槛签字
- **历史结论：** 阻断；没有真实工位、班次、批次和生产样本报告，质量/工艺/算法/发布四方未签字，现有 34 图只允许技术检查

### 阶段三十二：P7-06 用户运维和应急演练

- [ ] P7-06 用户运维和应急演练
- **历史结论：** 阻断；仅有手册和场景模板，紧急联系人仍为 `PENDING_SITE_FILL`，没有未参与开发人员的真实设备演练记录

### 阶段三十三：P7-07 上线就绪评审 + G7 门禁

- [ ] P7-07 上线就绪评审
- [ ] G7 阶段门禁验收
- **历史结论：** 未通过；发布记录为 `PENDING_SITE_SIGNOFF`，四方签字和值班表待定，回滚模型待确认，`make verify-all` 失败且真实设备、质量、节拍、容量、安全和恢复均未签字

### 阶段三十四：P6/P7 全任务完成情况独立复核

- [x] 恢复既有规划记录并确认未提交工作树边界
- [x] 从 15 号任务计划逐项提取 P6/P7 权威验收口径
- [x] 核对每项任务的依赖、交付物、实现证据和安全失败状态
- [x] 执行任务级验证、G6/G7 门禁及必要回归
- [x] 形成逐项完成度矩阵并修正不成立的既有完成声明
- **历史结论：** 已完成；P6 0/8、P7 0/7 达到完整通过标准，G6/G7 均未通过

## P7 关键约束

1. 真实硬件依赖项(P7-02/P7-04 设备和节拍验证)不得用模拟器结果替代。
2. 示例配置不得包含默认密码、访问密钥或私钥。
3. 所有未关闭的上线阻断决策必须显式标记为 `PENDING_SIGNOFF` 或 `PENDING_HARDWARE`。
4. 生产签名仅由质量、工艺、算法和发布负责人共同签署。
5. 34 张研究测试集不能替代现场全场景质量试运行(P7-05)。

## P3 关键约束

1. 跨模块字段先修改 `contracts/`，再通过 `tools/generate-contracts/` 生成三语言类型，禁止手改生成文件。
2. 技术失败、未知状态、哈希冲突、契约不兼容和缺失前置条件必须失败或进入 `HOLD`，不得产生 `PASS`。
3. 后端不导入 Python 算法，推理服务不访问业务数据库，前端不实现最终处置规则。
4. 数据库迁移仅由 `services/business-api/` 所有；不得提交图片、数据集、模型权重、运行输出或密钥。

## P6 当前收口记录（2026-07-31）

- **任务状态：** P6-01 至 P6-08 仍为 `阻塞`，G6 仍为 `阻塞`；本轮没有把本地实现或模拟测试改写成生产通过。
- **契约版本：** 网络契约保持 `v1`，OpenAPI/生成包版本 `1.0.0`；训练状态回报先改契约再生成三语言类型，当前源哈希为 `698d11ac5202bb62df31a09ed1c46be7280d905bc497c612fa9ab557e35b26cc`。
- **本轮实现：** 补齐训练状态回报与失败码、V10 状态转换触发器；共享生命周期资格读取器消除模型—训练循环依赖；模型注册强制成功训练和冻结数据集清单；部署激活强制运行时验签/预热/健康证据；普通回滚请求失败关闭，只有带目标模型验签/预热/健康摘要的内部编排器可回滚，V11 增加数据库证据列和触发器；修复质量/训练 JDBC 代理类的 `final` 运行时缺陷。
- **验证命令与结果：**
  - `make verify-contracts-source`：通过。
  - `make verify-layout`：通过（仅保留冻结数据与大小写索引警告）。
  - `git diff --check`：通过。
  - `services/business-api/mvnw -o -B -Dmaven.repo.local=/Users/jinghong/.m2/repository -f services/business-api/pom.xml test`：78/78，通过。
  - `services/business-api/mvnw -o -B -Dmaven.repo.local=/Users/jinghong/.m2/repository -f services/business-api/pom.xml -Dtest=NoSuchUnitTest -Dsurefire.failIfNoSpecifiedTests=false -Dit.test=DatabaseMigrationIT,S3StorageIT,ApplicationHealthIT,ReliableMessagingIT -DfailIfNoTests=false failsafe:integration-test failsafe:verify`：11/11，通过，使用真实 Docker 29.6.2、PostgreSQL 18.4、RabbitMQ 4.1.4、MinIO。
  - `make verify-p6-07`：通过，Node 20.13.1/pnpm、23 项前端测试和生产构建通过。
  - `make verify-p6-01`、`verify-p6-02`、`verify-p6-03`、`verify-p6-04`、`verify-p6-05`、`verify-p6-06`、`verify-p6-08`、`verify-g6`：均返回 2 并报告 `BLOCKED`；分别缺少真实迁移快照/备份、候选差异与隔离清单、训练产物与资源证明、评估与三方门槛签字、模型供应链证据、双槽灰度回滚证据、真实全链路目录和 G6 汇总报告。
- **已知限制：** 仓库仍没有可核验的生产对象注册和备份恢复记录、已批准候选数据集、真实模型权重与评估签字、受信签名包/SBOM、双槽运行与灰度观察记录、真实回滚记录及 P6-08/G6 全链路证据；这些前置条件缺失时必须保持 `BLOCKED/HOLD`。
