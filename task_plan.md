# 任务计划：手工批量检测与管理简化 R5

## 目标

依据 `Docs/16-手工批量检测与管理简化需求规格.md` 与 `Docs/17-手工批量检测整改实施计划.md` 完成 R5 上传、结果、历史和三按钮核心用户流程，并以仓库规定的门禁验证交付结果。

## 当前阶段

R5 已启动；正在核对入口条件、任务范围与现有实现

## 阶段

### 阶段 0：R5 入口条件与现状核对

- [ ] 确认 `RG3`、`RG4` 已关闭并提取 `R5-*` 与 `RG5` 验收条件
- [ ] 核对 `DOC-16 1.1.1`、`DOC-17 1.1.1` 与 `v2 / 2.0.0` 契约基线
- [ ] 盘点前端上传、进度、结果、历史、反馈组件及相应后端接口
- [ ] 将发现记录到 `findings.md`
- **状态：** 进行中

### 阶段 1：R5 实施设计

- [ ] 明确文件校验、直传、提交和逐项失败隔离边界
- [ ] 明确实时状态更新、轮询降级、刷新恢复和历史筛选边界
- [ ] 明确三按钮提交、幂等、权限及可访问性要求
- **状态：** 未开始

### 阶段 2：R5 核心用户流程实施

- [ ] 按 `DOC-17` 完成全部 `R5-*` 任务
- [ ] 增量验证每项修改
- **状态：** 未开始

### 阶段 3：`RG5` 门禁验证

- [ ] 执行 `DOC-17` 规定的 R5 自动化门禁
- [ ] 记录浏览器人工验收证据及缺失前置条件
- [ ] 执行仓库相关汇总门禁且不以跳过冒充通过
- **状态：** 未开始

### 阶段 4：交付

- [ ] 核对任务编号、契约版本、验证命令、结果和已知限制
- [ ] 更新三份规划文件并提交本轮交付说明
- **状态：** 未开始

### 历史阶段 1：R4 入口条件与现状核对

- [x] 确认 `RG2`、`RG3` 已通过并提取 `R4-01` 至 `R4-06`
- [x] 核对第二版推理事件和结果模式已冻结
- [x] 盘点推理服务、预处理、质量检查、业务处置与边缘代理现状
- [x] 核对第一版兼容消费者、升级清单和用量指标
- [x] 将发现记录到 `findings.md`
- **状态：** 已完成

### 历史阶段 2：R4 实施设计

- [x] 明确单图片项消息、幂等消费、回调和重试边界
- [x] 明确逐项质量检查版本、固定样例、异常与安全失败语义
- [x] 明确手工和产线复用同一单图链路及第一版兼容边界
- **状态：** 已完成

### 历史阶段 3：R4 单图推理与产线适配实施

- [x] 完成 `R4-01` 第二版单图片项消费者、单原图解码和结果回调
- [x] 完成 `R4-02` 第二版单图预处理并移除主视角依赖
- [x] 完成 `R4-03` 版本化逐项质量检查
- [x] 完成 `R4-04` 质量拒绝、技术失败、不确定和成功的安全映射
- [x] 完成 `R4-05` 第二版产线单项适配
- [x] 完成 `R4-06` 第一版兼容适配、消费者清单和用量指标
- [x] 增量验证每项修改
- **状态：** 已完成

### 历史阶段 4：`RG4` 门禁验证

- [x] 验证手工和产线图片项复用同一单图推理链路
- [x] 验证十项批次中一项质量拒绝时其余九项完成且批次部分完成
- [x] 验证全部失败、部分完成、全部完成和不确定结果聚合
- [x] 验证第二版消息与路径不存在多视角语义，第一版边缘端保持兼容
- [x] 执行 `make test-edge`、`make test-inference`、`make test-integration`、`make test-e2e`、`make test-faults`
- [x] 记录项目所有者关于跳过真实相机与触发设备验收的明确豁免；该项未执行且不标记为通过
- **状态：** 已关闭；自动化门禁通过，硬件项按批准范围豁免

### 历史阶段 5：R4 交付

- [x] 核对任务编号、契约版本、验证命令、结果和已知限制
- [x] 更新三份规划文件并提交本轮交付说明
- **状态：** 已完成

## 关键问题

1. 第二版单图路径能否完全不依赖主视角或多视角字段？已由契约、范围扫描和实际解码帧属性验证。
2. 重复消息是否会重复运行算法或形成冲突业务事实？已由文件回调清单、确定性事件标识和业务收件箱闭合。
3. 质量拒绝、技术失败和不确定是否可能产生通过结论？已由 V18 约束、聚合函数、真实 PostgreSQL 用例和故障门禁验证为否。
4. 产线端如何保持第一版兼容并逐步升级？已保留第一版同步器，新增独立第二版适配器、消费者状态和升级/用量基线。
5. 真实硬件是否已具备验收条件？否；配置模板、相机、触发器、设备清单和批准证据均缺失。项目所有者明确决定本轮跳过，因此解除阶段阻断并记录为“已豁免、未执行”。

## 已作决策

| 决策 | 理由 |
|---|---|
| 使用仓库根目录三份 Markdown 文件记录全过程 | 用户明确要求使用 `planning-with-files`，且便于跨会话恢复 |
| 先完成需求与现状核对，再修改代码 | 遵守契约优先、修改所有权和安全失败规则 |
| R0 不修改网络契约，契约版本保持 `v1 / 1.0.0` | 目标 `v2 / 2.0.0` 属于 R1；R0 只闭合文档、决策、追踪和基线 |
| 第一版从未部署时，运行遥测和观察窗口标为不适用 | 项目所有者已确认第一版从未实际使用；这不是把缺失遥测解释为零调用，而是确认从未存在运行消费者 |
| R1 在现有 R0 未提交成果上增量实施 | 这些改动属于当前整改链路，必须保留，禁止覆盖或回退 |
| 第二版目标契约固定为 `v2 / 2.0.0`，第一版保持 `v1 / 1.0.0` | DOC-16 与 DOC-17 已批准双版本并行边界 |
| R2 在现有 R0/R1 未提交成果上增量实施 | 当前整改链路尚未提交，必须保留并避让其他所有者改动 |
| 在入口条件核对完成前不宣称 `RG2` 通过 | R2 要求备份、迁移演练、消费者归零证据及真实 PostgreSQL/Testcontainers 证据；观察窗是否适用须由部署事实决定 |
| `V16` 使用可重复执行的数据库回填函数 | Flyway 迁移只执行一次不能证明回填幂等；函数可由迁移作业和测试重复调用并对账 |
| 旧 `capture_id`、`image_id`、结果和复核引用只作为外键复用，不复制或改写 | 满足历史引用不变并减少双写漂移面 |
| 历史多原图采集不创建伪造单项，记录稳定失败并保持只读 | 禁止自动选主图；未知映射必须安全失败或 `HOLD` |
| 模型来源用数据库检查约束强制完整且互斥 | 应用校验不足以保护迁移、脚本和并发写入 |
| 第一版从未部署时免除 R2 运行观察窗 | 项目所有者明确指示不考虑批准运行观察窗；冻结消费者清单、确定性双写/影子读集成测试及数据库门禁作为 `RG2` 证据 |
| R4 第二版消费者采用持久回调清单，接受后保留有界回放记录 | 重复投递不重复解码和运行算法；接受状态丢失时仍可安全重发同一确定性消息，由业务收件箱去重 |
| 质量拒绝形成 `QUALITY_REJECTED`，技术失败形成 `FAILED`，不确定保持 `INCONCLUSIVE` | 三类非正常结果均不可被聚合为业务通过；全质量拒绝或技术失败批次明确为 `FAILED` |
| `RG4` 按项目所有者批准后的范围关闭 | 真实硬件仍缺少前置条件；项目所有者明确决定跳过。本项记录为“已豁免、未执行”，不伪造硬件通过证据，并在 `RG9` 上线验收前恢复 |

## 遇到的错误

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 解除 R4 硬件豁免后尝试执行不存在的 `make verify-traceability` 目标 | 1 | 未修改源码；改用仓库实际的 `tests.test_traceability` 单测入口验证追踪锁 |
| R4 首次 `make test-faults`：21 项中新增源码断言 1 项失败，测试误把数据库终态枚举写成不存在的 `TECHNICAL_FAILED` | 1 | 按 V18 实际模型改为断言技术失败终态 `FAILED`、质量拒绝终态 `QUALITY_REJECTED` 及聚合安全公式，不改生产语义 |
| R4 第二次 `make test-faults`：禁止通过的全文断言误命中合法质量检查状态 `PASS` | 2 | 将断言收窄为禁止把图片项业务状态写成 `PASS`，保留质量检查单项通过语义 |
| R4 第三次 `make test-faults`：聚合公式断言漏写第二个字段的 `summary.` 限定符 | 3 | 按迁移中的完整表达式收窄断言后重跑；不改生产代码 |
| R4 最终差异审查发现产线图片直接置为 `AVAILABLE` 时未写入对象检查得到的宽高 | 1 | 扩展内部仓储图片值对象，服务仅接受正宽高并落库；新增真实 PostgreSQL 幂等与宽高完整性用例后完整后端门禁通过 |
| R4 单图解码首次回归：新增夹具漏传物化对象大小，且直接改变第一版解码器默认视角属性导致既有兼容测试失败 | 1 | 恢复第一版解码行为；新增独立第二版单图解码适配器，仅向后续第二版链路暴露图片种类，并补齐夹具大小 |
| R4 后端真实容器第二轮：22 项集成中剩余消息补绑重试 1 失败、R4 图片夹具完整性 1 错误 | 1 | 为可用图片补齐宽高；把失败事件重试时间明确置于数据库当前时间之前，消除应用/数据库时钟边界后重跑 |
| R4 首次真实 `make test-backend`：92 项单元通过；22 项集成中 2 失败、1 错误，分别为两处迁移计数仍期待 V17，以及新增测试批次号超过 32 字符 | 1 | `V18` 本身在空库和升级库执行成功；更新测试计数为 18/16，并把测试批次号收敛到字段长度后重跑 |
| 沙箱内首次 `make test-backend` 无权访问 Docker 套接字，7 个集成类明确失败 | 1 | 不把缺失前置标为通过；按权限规则在沙箱外重跑，成功启动 PostgreSQL、MinIO 与 RabbitMQ 后取得真实结果 |
| R4 首次业务后端编译失败：`JacksonException` 是 `RuntimeException` 子类，不能同时出现在 multi-catch | 1 | 收窄为捕获 `RuntimeException`，保留非重试异常优先透传后重新编译 |
| R4 首次 `make test-inference`：34 项中 2 项错误，新增测试调用 `SchemaEngine.validate` 时缺少 `instance_path` | 1 | 生产代码已完成对应路径；读取校验器真实签名后修正测试接线，不重复错误调用 |
| `tests.test_traceability` 固定期望 355 条、旧摘要，但当前锁和生成结果为 463 条 | 1 | 记录为 R0 前置追踪测试漂移；R0-04 扩展后同步改为结构性断言和新锁摘要 |
| 新追踪验证发现 14 条 P0 要求没有验收场景映射 | 1 | 生成器已明确列出缺口；补充或扩展与需求语义一致的 AT 场景后重新验证 |
| 合并更新测试、追踪说明和冲突记录时上下文未命中 | 1 | 文件中目标段落存在换行差异；拆分补丁并读取精确上下文后重试 |
| 第一版消费者迁移字段进入 `consumer-contract-v1` 后改变第一版生成源哈希 | 1 | 恢复第一版网络模式，新增独立 `consumer-migration-r1.schema.json`；重新生成后第一版 8 个源文件无差异 |
| `verify_v2_scope.py` 首次扫描遍历依赖和构建目录，未在合理时间结束 | 1 | 终止本次运行；改用带目录剪枝的源码遍历，显式排除 `node_modules`、`target`、`dist`、`build` 等产物 |
| 范围门禁以“文件中任意位置存在 V2”判定整个历史迁移测试为第二版，误报第一版冻结字段 | 1 | 收窄为逐行第二版表面判定；第二版契约与生成目录仍保持全量严格扫描 |
| Python 契约包编译器只扫描顶层 `*.py`，第二版子包未实际编译 | 1 | 改为递归 `rglob("*.py")`，重新执行离线和严格三语言编译 |
| `planning-with-files` 完成脚本只识别英文阶段/状态，中文计划被统计为 0 个阶段 | 1 | 保持仓库强制中文记录；改用未勾选项和中文未完成状态扫描，结果无命中，并记录该技能脚本本地化限制 |
| 沙箱内 `docker info` 无权访问用户 Docker 套接字 | 1 | 记录为环境待复核，不误判 Docker 缺失；在执行真实集成门禁时申请受控权限复核 |
| `rg` 备份证据扫描中的 `compose*.yml` 被 zsh 当作无匹配通配符拒绝 | 1 | 不重复原命令；后续使用 `rg --files` 提供显式文件列表或直接扫描已存在目录 |
| 向 `DatabaseMigrationIT` 插入 R2 场景时使用了不存在的方法名作为补丁上下文 | 1 | 读取实际相邻测试名，改在 `pgDumpRestoresSchemaHistoryAndBusinessRows` 前用精确上下文插入 |
| 业务后端单元测试 88 项中架构边界测试失败 1 项：API 直接依赖共享领域异常 | 1 | 不放宽门禁；将退役能力异常移到共享应用层，保持 API → application 的依赖方向后重跑 |
| 用 `unittest` 执行 pytest 风格的 `test_r2_data_verification.py`，未发现测试并返回 5 | 1 | 不重复运行器；改用仓库 `.venv` 中的 pytest 执行该测试 |
| 仓库 `.venv` 未安装 pytest，无法执行 pytest 风格新增测试 | 1 | 不安装新依赖；将测试改为标准库 `unittest.TestCase`，保持离线可执行 |
| 根 `.gitignore` 的 `training/` 规则误将业务后端训练源码排除，直接修改无法形成可交付变更 | 1 | 恢复忽略文件改动；改用已跟踪的共享 HTTP 过滤器统一拦截取消写路径，并记录该既有忽略规则限制 |
| 停写过滤器误用未引入的 `com.fasterxml.jackson.databind.ObjectMapper` 导致编译失败 | 1 | 使用项目现有 `tools.jackson.databind.ObjectMapper` 和统一 `StandardErrorFactory.write`，不新增依赖 |
| 停写过滤器测试将根 `JsonNode` 当作集合提取字段，导致 91 项中 1 项空指针 | 1 | 先解析根节点，再直接断言根节点的 `code` 字段；生产过滤器无需修改 |
| 首次真实 `make test-backend`：18 项集成测试中 1 失败、8 错误 | 1 | 已分解为 64 位失败摘要、测试 `record_version`、权限排序和应用上下文级联根因；分别定向修复后重跑完整门禁 |
| 第二次 `make test-backend` 仅剩从 V2 升级的执行数量仍期望 13，实际为 14 | 2 | 更新为 `V3—V16` 共 14 个迁移，并再次运行完整门禁 |
| 更新升级迁移计数时误用变量名 `v2`，实际测试变量为 `latest` | 1 | 读取精确行后使用实际变量上下文修改 |
| 最终 `make verify-p1-offline` 的旧安全测试仍要求已取消的数据集/训练权限 | 1 | 按 R2 权限语义改为全角色撤权、专用模型审批权限及系统操作员授权断言后重跑完整离线门禁 |
| 使用双引号执行包含反引号的 `rg` 模式，zsh 将 `RG2` 误作命令替换 | 1 | 命令未写文件；改用无反引号模式和精确文件扫描，并记录本次工具调用错误 |
| 修改 `AT-COMP-003` 后追踪冻结摘要与 9 项追踪单测失配 | 1 | 按生成器实际摘要更新验收场景哈希和 DOC-16 源文件哈希，再重跑锁验证与追踪单测 |
| 查找幂等执行器时使用了不存在的 `IdempotentExecutor.java` 与 `ApplicationResponse.java` 文件名 | 1 | 未修改文件；改用 `rg` 定位实际的 `IdempotencyService` 及其内嵌 `Response` 类型 |
| 合并补丁同时修改 YAML、属性类和新增接口时把属性类上下文错误匹配到 YAML | 1 | 补丁整体未应用；拆分为按文件精确补丁后继续 |
| 在业务后端子目录执行计划重读时使用根目录相对路径，`sed` 找不到 `task_plan.md` | 1 | Maven 编译仍独立执行；后续从仓库根目录读取计划并使用 `-f services/business-api/pom.xml` 验证 |
| R3 首次单元回归 91 项中架构边界失败 2 项：应用层依赖基础设施属性类，接口层依赖领域异常 | 1 | 不放宽架构门禁；新增应用层设置值对象并把跨层异常移动到应用层，由配置适配器完成映射 |
| 合并权限补丁时把控制器方法上下文错误归入安全配置文件 | 1 | 补丁整体未应用；拆分迁移、权限矩阵、安全配置和控制器四个补丁 |
| 首次 `make test-backend` 在 91 项单元测试阶段失败 1 项：系统管理员精确权限集合未包含 R3 三个原子权限 | 1 | 同步精确权限基线后重跑；本次未进入集成测试，不标记后端门禁通过 |
| R3 增加 `V17` 后 `make verify-data` 仍把迁移全集硬编码为 `V1—V16` | 1 | 保留对 R2 `V16` 的专属内容验证，把全序列检查改为连续至当前最高版本，并新增 R3 `V17` 必需数据不变量校验 |
| 放宽手工检测配置条件以暴露禁用能力后，完整后端门禁在 MinIO 集成上下文失败：事务服务为 `final`，Spring 无法创建 CGLIB 代理 | 1 | 保留能力接口的可观测禁用语义；移除应用服务的 `final` 限制并重跑完整门禁，不把首次运行记为通过 |

## R0 关闭判定（R1 入口）

- `R0-01`：源码产物完成。
- `R0-02`：源码产物完成。
- `R0-03`：源码产物完成。
- `R0-04`：生成、锁和测试完成。
- `R0-05`：源码扫描完成；项目所有者确认第一版从未实际使用，运行实例、用户、调用和历史记录数量均为零。
- `R0-06`：取消清单完成；源码对象继续按后续任务退役，运行零调用条件已满足。
- `RG0`：已通过，可以进入 R1 契约阶段。

## R1 当前关闭判定

- `R1-01`—`R1-08`：源码产物与规定门禁完成。
- `RG1`：本地通过，可进入 R2 数据模型阶段。
- 第一版 8 个既有生成源文件无差异；第二版以独立 `v2` 命名空间并存。
- 第二版消费者仍处于 `PLANNED` 或 `TESTING`，本阶段未宣称运行接入。

## 备注

- 完成每个阶段后同步更新状态。
- 每两次查看或搜索操作后，将关键发现写入 `findings.md`。
- 所有失败与缺失前置条件必须明确记录，不得按通过处理。
