# 任务计划：手工批量检测与管理简化 R8（R7 并行运行）

## R8 当前目标与并行边界

依据 `Docs/16-手工批量检测与管理简化需求规格.md` 与 `Docs/17-手工批量检测整改实施计划.md`，完成 R8 模型直接上传、验证、启用和回退闭环；保持第二版 `v2 / 2.0.0`，不把内部数据集版本或训练运行作为外部模型成功前置。

R7 当前仍由其他工作流运行。本轮仅在 R8 所有权范围内增量修改；R7 正在修改的契约源、生成包、`V20` 迁移及其相关文件视为并发未提交改动，未经交接不得覆盖、回退、重排迁移序号或把中间状态判定为完成。

## R8 阶段

### 阶段 0：入口条件、并发边界与现状核对

- [x] 提取 `R8-01`—`R8-10`、`RG8`、模型包安全门禁和规定验证命令
- [x] 核对 `RG2`、`RG6` 当前证据，以及隔离桶、验证运行身份、资源限额、固定测试集、生产切换门槛和外部来源规范批准事实；`RG6` 与 R8 专属批准事实缺失保持 `HOLD`
- [x] 确认当前生产活动模型唯一化状态与歧义处理方式；静态核对未发现生产活动唯一数据库约束，需在迁移中补强并保持 `HOLD`
- [x] 盘点现有模型控制器、工作流、资格读取器、仓储、数据库迁移、对象存储、运行时和前端模型页，划定 R8 所有权
- [x] 盘点 R7 当前未提交改动和契约/迁移版本，确认 R8 不与其产生串行冲突
- [x] 将入口、边界、依赖和已知限制写入 `findings.md`
- **状态：** `completed_with_hold`；源码设计可继续，面向用户启用和数据库/契约串行变更受 R7 交接、`RG6` 和 R8 批准前置阻断

### 阶段 1：R8 契约、数据与供应链设计

- [x] 盘点并初步对齐上传会话、外部来源快照、验证证据、模型版本、启用申请、批准和回退的公共字段；新增跨进程字段仍待 R7 契约交接后串行修改 `contracts/`
- [x] 明确生成链、消费者边界和现有模型验证事件表面；确认生成包只能由 `tools/generate-contracts/` 产出
- [x] 明确隔离对象前缀、票据、配额、哈希、保留期、验证队列、清单/SBOM/签名/白名单和失败状态的设计缺口
- [x] 明确 `LEGACY_INTERNAL`/`EXTERNAL_UPLOAD` 来源互斥、历史模型可追溯、唯一生产活动模型和不同管理员确认事务的目标不变量
- [x] 明确模型运行时无业务凭据、无任意网络、只读根文件系统、资源上限、固定图片试运行和清理补偿边界
- **设计决策：** 在 R7 交接前只实施不触碰 `contracts/`、生成包和迁移序列的纯模型包安全验证；第二版公共字段和数据库事实在交接后串行闭合。
- **状态：** `in_progress`；进入 R8-02 纯验证逻辑和测试设计，R8-01/R8-04/R8-05 的跨层接入等待串行变更窗口

### 阶段 2：R8 实施

- [ ] `R8-01`：实现上传会话、隔离对象票据、完成确认、外部来源说明、幂等/配额/哈希/权限/到期和审计
- [x] `R8-02`：实现安全解包、清单、签名/SBOM、框架/插件白名单和恶意模型包拒绝（独立验证入口已完成；尚未接入根门禁和运行时）
  - [x] R8-02 子项：ZIP 成员边界、路径/特殊文件/压缩炸弹、清单文件集合、内层 SHA-256、Ed25519 签名和验证后安全解包
  - [x] R8-02 子项：SBOM 内容、框架/插件白名单与隔离验证作业接入
- [ ] `R8-03`：实现隔离加载、输入输出校验、预热、固定图片试运行、无凭据/无任意网络和失败清理
- [ ] `R8-04`：实现模型验证状态、来源快照、来源互斥、唯一活动模型、启用申请和回退目标数据库事实
- [ ] `R8-05`：实现不同管理员批准后的原子启用、崩溃/超时/预热失败补偿和安全回退
- [ ] `R8-06`：实现模型管理页面的文件选择、上传进度、验证证据、申请/批准启用和回退展示；上传不得显示为启用
- [ ] `R8-07`：固定历史检测实际模型引用并补齐上传/验证/启用/失败/回退不可变审计
- [ ] `R8-08`：解除第二版对成功训练运行/冻结数据集的硬依赖，保留第一版历史读取和兼容窗口
- [ ] `R8-09`：新增真实 `make verify-model-supply` 及模型供应链正负测试
- [ ] `R8-10`：新增真实 `make verify-online-without-training` 隔离集成环境和全链路安全失败证据
- [ ] 每项修改后执行增量验证，记录错误，不覆盖 R7 或其他所有者未提交改动
- **当前进展：** R8-02 的独立安全包验证子项已完成；业务接入、SBOM、运行时和门禁仍未完成。
- **状态：** `in_progress`

### 阶段 3：`RG8` 门禁验证

- [ ] 执行 `make verify-model-supply`
- [ ] 执行 `make verify-online-without-training`
- [ ] 执行 `make test-backend`
- [ ] 执行 `make test-inference`
- [ ] 执行 `make test-integration`
- [ ] 执行 `make test-security`
- [ ] 执行 `make test-faults`
- [ ] 执行 `make test-e2e`
- [ ] 核对隔离环境中无数据集构建器/训练服务、无其凭据、无相关网络连接；缺失真实外部前置保持 `HOLD`
- **状态：** `pending`

### 阶段 4：R8 交付

- [ ] 核对 `R8-01`—`R8-10`、契约版本、数据库迁移序列、验证命令、结果和已知限制
- [ ] 更新三份规划文件并形成 R8 阶段性交付说明
- [ ] 只有所有 `RG8` 规定证据齐全且 `RG6` 启用前置满足时才关闭 `RG8`；否则明确 `completed_with_hold`
- **状态：** `pending`

## R8 已知错误

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 用 zsh 通配符读取不存在的 `docker-compose*.yml`/`compose*.yml` 时返回 `no matches found` | 1 | 未重复原命令；改用 `rg --files` 先列出实际配置文件，再按显式路径读取 |
| R8 模型包测试清单只声明业务文件，未包含 `checksums.sha256` 与 `signature.sig`，导致有效包被判定为清单不一致 | 1 | 修正测试包清单闭包，声明除 `manifest.json` 外的全部成员后再重跑；未放宽生产校验 |
| R8 供应链 CLI 单测返回 `HOLD` 但初始断言未打印脱敏输出，无法定位配置失败原因 | 1 | 将断言附加 CLI 输出后重跑；不重复隐藏错误 |
| R8 供应链 CLI 在 SBOM 校验时把 SBOM 子策略当成完整策略读取，触发 `KeyError` 并进入 `HOLD` | 1 | 按函数参数边界改用 `format`、`spec_versions` 和 `require_components` 直接读取，保留未知配置失败语义 |

## R7 并行状态

- R7 仍在运行；其契约、生成包和 `V20__r7_sample_library_and_exports.sql` 等未提交改动由 R7 工作流负责。
- 本计划保留下方 R7 原计划作为交接上下文；R8 不宣称 R7 已完成，也不在 R7 尚未交接时修改其迁移或生成文件。

## 目标

依据 `Docs/16-手工批量检测与管理简化需求规格.md` 与 `Docs/17-手工批量检测整改实施计划.md` 启动并完成 R8 阶段；先核对入口门禁、模型供应链和并发修改边界，再按契约优先、不可变事实和安全失败规则实施、验证与交付。R7 保持并行运行，不纳入本轮完成判定。

## 当前阶段

R8 入口核对已开始：`RG2` 前序记录为通过；`RG6` 因 `RG5` 真实浏览器证据缺失仍为 `HOLD`，且 R8 专属隔离桶、验证身份、资源限额、固定测试集、生产切换门槛和外部来源批准事实尚未取得。本轮允许推进源码和自动化验证，但面向用户启用、`RG6`/`RG8` 关闭仍不得宣称通过。

## R7 阶段

### 阶段 0：入口条件与现状核对

- [x] 提取 `R7-01`—`R7-07`、`RG7`、导出包最小内容和规定验证命令
- [x] 核对 `RG2`、`RG6` 状态及 R7“可开发/可启用”边界；保留 `RG6` 的 `HOLD`
- [x] 盘点管理员反馈、复核历史、对象存储、审计、权限和异步作业现状
- [x] 盘点样本候选持久化技术债、最新迁移序号、第二版契约/生成类型和现有前端表面
- [ ] 确认导出对象前缀、保留期、配额、审批阈值；当前配置尚未提供批准事实，保持 `HOLD`
- [x] 将入口和现状发现记录到 `findings.md`
- **状态：** `completed_with_hold`；R7 可以继续开发，面向用户启用和配置放量仍受 `RG6` 及导出参数确认阻断

### 阶段 1：R7 契约、数据和实施设计

- [x] 明确管理员六类反馈、修订链、并发和数据范围边界：管理员全授权范围读取，反馈/决策按主体和资源加幂等键，历史事实只追加
- [x] 明确候选加入/去重、纳入/排除/修订、来源反馈版本和审计事实：候选保存不可变来源快照，决策单独追加并更新显式投影
- [x] 明确导出作业、清单/哈希/失败项、对象写入、部分失败、重试和终态：作业先排队，worker 按候选逐项隔离，任一失败不得完整成功
- [x] 明确短时下载票据、到期、越权、重复下载和孤儿包清理：票据只存哈希，签发/下载/过期事件审计，包和票据按保留期清理
- [x] 明确外部交接仅支持手工回执，不增加外部回调、轮询、消息订阅或服务凭据
- [x] 明确 R7 数据库迁移、权限、契约生成和前端消费者边界：追加 `V20`，契约先改后生成，R7 页面只调用第二版样本 API
- **设计决策：** 导出请求事件使用只含位置的目标对象引用；完成事件使用完整清单引用。原图/结果图按登记引用和 SHA-256 校验，版本字段无法取得时单项失败。
- **状态：** `completed`；契约、生成物、V20 迁移、权限和实施边界已闭合

### 阶段 2：R7 实施

- [x] `R7-01`：管理员筛选和六类不可变反馈、修订与审计
- [x] `R7-02`：候选加入幂等去重、纳入/排除/修订和来源关联
- [x] `R7-03`：异步样本导出、对象写入、清单、哈希、失败隔离和重试
- [x] `R7-04`：短时下载票据、下载审计、过期和孤儿包清理
- [x] `R7-05`：管理页面筛选、标记、候选批量选择、导出状态和失败项
- [x] `R7-06`：导出格式、用途声明、外部交接手工回执、短时票据和清理说明
- [x] `R7-07`：R7 专项正负测试与规定门禁复验
- **当前复核：** R7-01—R7-07 的契约、V20、worker、完成事件投影、前端页面和安全表面均已具备；R7-05 已补充服务端游标分页，R7-06 已补充管理员手工外部接收回执，均保持事实展示和安全失败边界。
- **已完成接线：** 独立样本完成事件消费者复用收件箱事务，严格校验清单/压缩包引用、计数/候选集合并更新导出投影；worker 新增完成事件构造器，事件不携带二进制。
- **已增加真实投影回归：** `DatabaseMigrationIT` 覆盖部分失败、包/清单摘要写入、候选导出关联和同一完成事件重复处理。
- **门禁结果：** `make test-backend` 通过 101 项单测、26 项真实容器集成测试；`make verify-sample-export`、`make test-integration`、`make verify-data`、`make test-security`、`make test-faults`、`make test-e2e`、`make test-web` 和 `make verify-p1-offline` 均通过。
- **增量验证：** 生成物由 `tools/generate-contracts/` 重新生成并通过契约源校验；首轮投影回归发现清单媒体类型查询别名遗漏，修复后重跑通过；未覆盖其他所有者未提交改动。
- **状态：** `completed`

### 阶段 3：`RG7` 门禁验证

- [x] 执行 `make verify-sample-export`
- [x] 执行 `make test-backend`
- [x] 执行 `make test-integration`
- [x] 执行 `make test-security`
- [x] 执行 `make test-e2e`
- [x] 验证导出成功不创建数据集版本、训练运行、训练事件或内部流水线调用
- [x] 记录真实对象存储、外部交接和浏览器前置；缺失项保持 `HOLD`
- **状态：** `completed_with_hold`；RG7 规定自动化标准已通过，现场对象/外部交接和 RG6 用户启用前置仍按缺失证据保持 HOLD

### 阶段 4：R7 交付

- [x] 核对任务编号、契约版本、数据库迁移序列、验证命令、结果和已知限制
- [x] 更新三份规划文件并形成 R7 阶段性交付说明
- [x] 只有所有规定证据齐全时才关闭 `RG7`；当前自动化证据齐全，但启用前置缺失，明确为 `completed_with_hold`
- **状态：** `completed_with_hold`

## 历史阶段：R6

R6 身份、权限、迁移、前端和手册实施已完成；其自动化门禁通过，但 `RG5` 真实浏览器证据缺失导致 `RG6` 保持 `HOLD`。以下保留 R6 原始阶段记录。

## 阶段

### 阶段 0：R6 入口条件与现状核对

- [x] 提取 `R6-*`、`RG6`、入口条件和规定验证命令
- [x] 核对 R5 未闭合项是否构成 R6 阻断，并明确记录 `HOLD` 或批准依据
- [x] 核对契约版本、生成类型与现有工作树边界
- [ ] 复核 `RG5` 真实浏览器证据
- [x] 核对 R0-05 基线和逐账号高权限映射适用性：冻结运行基线为零用户，映射批准证据为不适用
- [x] 盘点 R6 涉及模块、测试和既有实现
- [x] 将关键发现记录到 `findings.md`
- **状态：** `completed_with_hold`；R6 任务、门禁、模块和入口资料已核对，唯一未闭合项为 RG5 真实浏览器证据

### 阶段 1：R6 实施设计

- [x] 明确契约、数据、服务、前端及外部依赖边界：复用现有本地会话表面，V19 增加人员角色字段与映射事实，第二版前端移除数据集/训练入口
- [x] 明确权限、幂等、失败隔离和 `HOLD` 语义：仅确认两类角色获得人员原子权限；未确认账号无第二版权限；重复确认同结果幂等，冲突和未知状态失败/HOLD
- [x] 明确增量验证与回归范围：身份单测、数据库迁移/对账、前端认证/路由、安全源码、全量四项 RG6 命令；真实联合验收仍受 RG5 入口阻断
- **状态：** 已完成；进入 R6-01/R6-02/R6-03/R6-04/R6-05/R6-07 增量实施，R6-06 手册随后同步

### 阶段 2：R6 实施

- [x] `R6-01`：两类人员角色领域、V19 角色/权限基线和契约约束
- [x] `R6-02`：角色映射预览、逐项确认、冲突/未确认清单和幂等
- [x] `R6-03`：会话仅返回已确认两类角色，角色/状态变更撤销活动会话
- [x] `R6-04`：用户管理仅显示两类角色并保留显示名称、启停、重置密码
- [x] `R6-05`：移除数据集/训练前端路由、菜单和模型页旧选择器；旧兼容服务保持不可达
- [x] `R6-07`：管理员权限和模型专属审批权限对齐，撤销旧数据集/训练人员授权
- [x] `R6-06`：更新管理员、生产员工和应急操作手册
- [x] 为正向、负向和安全失败路径补充自动化覆盖
- [x] 每项修改后执行增量验证
- **状态：** `completed`；R6-01—R6-07 的实现和自动化回归已完成，旧兼容代码的物理清理留在 R9

### 阶段 3：`RG6` 门禁验证

- [x] 执行 DOC-17 规定的 R6 自动化门禁
- [x] 执行相关仓库离线汇总门禁，不以跳过冒充通过
- [x] 记录真实外部前置条件、人工证据和已知限制
- **状态：** `HOLD`；四项自动化命令和 P1 离线门禁通过，但 RG5 真实浏览器证据缺失，不能关闭 RG6

### 阶段 4：交付

- [x] 核对任务编号、契约版本、验证命令、结果和已知限制
- [x] 更新三份规划文件并提交 R6 阶段交付说明
- [x] 按用户明确请求将开发数据库中原 `SYSTEM_OPERATOR` 的 `admin` 账号显式确认迁移为 `ADMINISTRATOR`，更新密码哈希并验证登录/会话
- **状态：** `completed_with_hold`；已交付 R6 实施结果，等待 RG5 真实证据后再做 RG6 关闭判定

### 历史阶段：R5 入口条件与现状核对

- [x] 确认 `RG3`、`RG4` 已关闭并提取 `R5-*` 与 `RG5` 验收条件
- [x] 核对 `DOC-16 1.1.1`、`DOC-17 1.1.1` 与 `v2 / 2.0.0` 契约基线
- [x] 盘点前端上传、进度、结果、历史、反馈组件及相应后端接口
- [x] 将发现记录到 `findings.md`
- **状态：** 已完成

### 阶段 1：R5 实施设计

- [x] 明确文件校验、直传、提交和逐项失败隔离边界
- [x] 明确实时状态更新、轮询降级、刷新恢复和历史筛选边界
- [x] 明确三按钮提交、幂等、权限及可访问性要求
- **状态：** 已完成

### 阶段 2：R5 核心用户流程实施

- [x] `R5-01`：完成点击/拖拽、多图预览、删除、数量、使用阶段与可访问错误
- [x] `R5-02`：完成能力读取、并发直传、稳定幂等重试、确认、单次提交与恢复
- [x] `R5-03`：完成后端计数驱动的结果排序、突出/折叠、自动刷新与受控结果读取
- [x] `R5-04`：完成权限范围内的历史列表、游标分页、详情、返回导航与深链接
- [x] `R5-05`：完成逐图即时反馈、不可变修订提示、稳定幂等重试和 `HOLD` 提示
- [ ] `R5-06`：十图、部分质量拒绝、结果和反馈的自动化覆盖已完成；真实浏览器联合链路待执行
- [x] 增量验证每项修改
- **状态：** 源码和自动化覆盖已完成；真实联合验收待执行

### 阶段 3：`RG5` 门禁验证

- [x] 执行 `DOC-17` 规定的 R5 自动化门禁
- [ ] 记录浏览器人工验收证据及缺失前置条件
- [x] 执行仓库相关汇总门禁且不以跳过冒充通过
- **状态：** 五项自动化门禁通过；人工验收未执行，`RG5` 未关闭

### 阶段 4：交付

- [x] 核对任务编号、契约版本、验证命令、结果和已知限制
- [x] 更新三份规划文件并提交本轮阶段性交付说明
- **状态：** 阶段性进度已交付；待完成 `R5-06` 后再关闭阶段

### 历史阶段 1：R4 入口条件与现状核对

- [x] 确认 `RG2`、`RG3` 已通过并提取 `R4-01` 至 `R4-06`
- [x] 核对第二版推理事件和结果模式已冻结
- [x] 盘点推理服务、预处理、质量检查、业务处置与边缘代理现状
- [x] 核对第一版兼容消费者、升级清单和用量指标
- [x] 将发现记录到 `findings.md`
- **状态：** 已完成

### 历史阶段 2：R4 实施设计

- [x] 明确单图片项消息、幂等消费、回调和重试边界
- [x] 明确逐项质量检查版本、固定样例、异常与安全失败语义
- [x] 明确手工和产线复用同一单图链路及第一版兼容边界
- **状态：** 已完成

### 历史阶段 3：R4 单图推理与产线适配实施

- [x] 完成 `R4-01` 第二版单图片项消费者、单原图解码和结果回调
- [x] 完成 `R4-02` 第二版单图预处理并移除主视角依赖
- [x] 完成 `R4-03` 版本化逐项质量检查
- [x] 完成 `R4-04` 质量拒绝、技术失败、不确定和成功的安全映射
- [x] 完成 `R4-05` 第二版产线单项适配
- [x] 完成 `R4-06` 第一版兼容适配、消费者清单和用量指标
- [x] 增量验证每项修改
- **状态：** 已完成

### 历史阶段 4：`RG4` 门禁验证

- [x] 验证手工和产线图片项复用同一单图推理链路
- [x] 验证十项批次中一项质量拒绝时其余九项完成且批次部分完成
- [x] 验证全部失败、部分完成、全部完成和不确定结果聚合
- [x] 验证第二版消息与路径不存在多视角语义，第一版边缘端保持兼容
- [x] 执行 `make test-edge`、`make test-inference`、`make test-integration`、`make test-e2e`、`make test-faults`
- [x] 记录项目所有者关于跳过真实相机与触发设备验收的明确豁免；该项未执行且不标记为通过
- **状态：** 已关闭；自动化门禁通过，硬件项按批准范围豁免

### 历史阶段 5：R4 交付

- [x] 核对任务编号、契约版本、验证命令、结果和已知限制
- [x] 更新三份规划文件并提交本轮交付说明
- **状态：** 已完成

## 关键问题

1. 第二版单图路径能否完全不依赖主视角或多视角字段？已由契约、范围扫描和实际解码帧属性验证。
2. 重复消息是否会重复运行算法或形成冲突业务事实？已由文件回调清单、确定性事件标识和业务收件箱闭合。
3. 质量拒绝、技术失败和不确定是否可能产生通过结论？已由 V18 约束、聚合函数、真实 PostgreSQL 用例和故障门禁验证为否。
4. 产线端如何保持第一版兼容并逐步升级？已保留第一版同步器，新增独立第二版适配器、消费者状态和升级/用量基线。
5. 真实硬件是否已具备验收条件？否；配置模板、相机、触发器、设备清单和批准证据均缺失。项目所有者明确决定本轮跳过，因此解除阶段阻断并记录为“已豁免、未执行”。

## 已作决策

| 决策 | 理由 |
|---|---|
| 使用仓库根目录三份 Markdown 文件记录全过程 | 用户明确要求使用 `planning-with-files`，且便于跨会话恢复 |
| 先完成需求与现状核对，再修改代码 | 遵守契约优先、修改所有权和安全失败规则 |
| R0 不修改网络契约，契约版本保持 `v1 / 1.0.0` | 目标 `v2 / 2.0.0` 属于 R1；R0 只闭合文档、决策、追踪和基线 |
| 第一版从未部署时，运行遥测和观察窗口标为不适用 | 项目所有者已确认第一版从未实际使用；这不是把缺失遥测解释为零调用，而是确认从未存在运行消费者 |
| R1 在现有 R0 未提交成果上增量实施 | 这些改动属于当前整改链路，必须保留，禁止覆盖或回退 |
| 第二版目标契约固定为 `v2 / 2.0.0`，第一版保持 `v1 / 1.0.0` | DOC-16 与 DOC-17 已批准双版本并行边界 |
| R2 在现有 R0/R1 未提交成果上增量实施 | 当前整改链路尚未提交，必须保留并避让其他所有者改动 |
| 在入口条件核对完成前不宣称 `RG2` 通过 | R2 要求备份、迁移演练、消费者归零证据及真实 PostgreSQL/Testcontainers 证据；观察窗是否适用须由部署事实决定 |
| `V16` 使用可重复执行的数据库回填函数 | Flyway 迁移只执行一次不能证明回填幂等；函数可由迁移作业和测试重复调用并对账 |
| 旧 `capture_id`、`image_id`、结果和复核引用只作为外键复用，不复制或改写 | 满足历史引用不变并减少双写漂移面 |
| 历史多原图采集不创建伪造单项，记录稳定失败并保持只读 | 禁止自动选主图；未知映射必须安全失败或 `HOLD` |
| 模型来源用数据库检查约束强制完整且互斥 | 应用校验不足以保护迁移、脚本和并发写入 |
| 第一版从未部署时免除 R2 运行观察窗 | 项目所有者明确指示不考虑批准运行观察窗；冻结消费者清单、确定性双写/影子读集成测试及数据库门禁作为 `RG2` 证据 |
| R4 第二版消费者采用持久回调清单，接受后保留有界回放记录 | 重复投递不重复解码和运行算法；接受状态丢失时仍可安全重发同一确定性消息，由业务收件箱去重 |
| 质量拒绝形成 `QUALITY_REJECTED`，技术失败形成 `FAILED`，不确定保持 `INCONCLUSIVE` | 三类非正常结果均不可被聚合为业务通过；全质量拒绝或技术失败批次明确为 `FAILED` |
| `RG4` 按项目所有者批准后的范围关闭 | 真实硬件仍缺少前置条件；项目所有者明确决定跳过。本项记录为“已豁免、未执行”，不伪造硬件通过证据，并在 `RG9` 上线验收前恢复 |
| R5 复用统一 `ApiClient` 并支持显式稳定幂等键 | 保持认证、CSRF、请求标识和错误转换一致；网络重试不得生成新的业务请求 |
| R5 页面仅解析和展示第二版后端事实 | 浏览器只计算展示顺序与上传字节进度，不计算质量结论、算法结论或批次终态 |
| R5 在现有 R4 未提交成果上增量实施 | 现有改动属于同一整改链路，必须保留且禁止覆盖、回退或顺手格式化 |

## 遇到的错误

| 错误 | 尝试 | 处理 |
|---|---:|---|
| R6 首次 `make test-backend`：100 项单测和 `DatabaseMigrationIT` 16 项通过，`OverviewAuditQueryIT` 因夹具仍绑定旧 `OPERATOR` 而无法获得 `detection:read`，总览总数为 0 | 1 | 将测试用户/范围绑定夹具迁移到 `PRODUCTION_EMPLOYEE` 并补写 `person_role`；随后定向测试和最终完整后端门禁通过 |
| R6 首次 `make test-web`：类型检查通过，但旧 P6 测试把模型路由也断言为已删除，导致 44 项中 1 项失败 | 1 | 失败仅来自测试断言与 R6 目标不一致；保留模型入口，改为只断言数据集/训练路由不可达，最终 44 项前端测试和构建通过 |
| R6 入口核验发现 `RG5` 明确未关闭 | 1 | 按安全失败规则将 R6 置为 `HOLD`；不修改生产源码、不伪造门禁通过，等待先闭合 R5 或由项目所有者明确修改阶段入口 |
| 写回 R6 `HOLD` 的跨文件合并补丁上下文未命中 | 1 | 补丁整体未应用；读取精确上下文后改为按文件拆分更新 |
| 解除 R4 硬件豁免后尝试执行不存在的 `make verify-traceability` 目标 | 1 | 未修改源码；改用仓库实际的 `tests.test_traceability` 单测入口验证追踪锁 |
| R4 首次 `make test-faults`：21 项中新增源码断言 1 项失败，测试误把数据库终态枚举写成不存在的 `TECHNICAL_FAILED` | 1 | 按 V18 实际模型改为断言技术失败终态 `FAILED`、质量拒绝终态 `QUALITY_REJECTED` 及聚合安全公式，不改生产语义 |
| R4 第二次 `make test-faults`：禁止通过的全文断言误命中合法质量检查状态 `PASS` | 2 | 将断言收窄为禁止把图片项业务状态写成 `PASS`，保留质量检查单项通过语义 |
| R4 第三次 `make test-faults`：聚合公式断言漏写第二个字段的 `summary.` 限定符 | 3 | 按迁移中的完整表达式收窄断言后重跑；不改生产代码 |
| R4 最终差异审查发现产线图片直接置为 `AVAILABLE` 时未写入对象检查得到的宽高 | 1 | 扩展内部仓储图片值对象，服务仅接受正宽高并落库；新增真实 PostgreSQL 幂等与宽高完整性用例后完整后端门禁通过 |
| R4 单图解码首次回归：新增夹具漏传物化对象大小，且直接改变第一版解码器默认视角属性导致既有兼容测试失败 | 1 | 恢复第一版解码行为；新增独立第二版单图解码适配器，仅向后续第二版链路暴露图片种类，并补齐夹具大小 |
| R4 后端真实容器第二轮：22 项集成中剩余消息补绑重试 1 失败、R4 图片夹具完整性 1 错误 | 1 | 为可用图片补齐宽高；把失败事件重试时间明确置于数据库当前时间之前，消除应用/数据库时钟边界后重跑 |
| R4 首次真实 `make test-backend`：92 项单元通过；22 项集成中 2 失败、1 错误，分别为两处迁移计数仍期待 V17，以及新增测试批次号超过 32 字符 | 1 | `V18` 本身在空库和升级库执行成功；更新测试计数为 18/16，并把测试批次号收敛到字段长度后重跑 |
| 沙箱内首次 `make test-backend` 无权访问 Docker 套接字，7 个集成类明确失败 | 1 | 不把缺失前置标为通过；按权限规则在沙箱外重跑，成功启动 PostgreSQL、MinIO 与 RabbitMQ 后取得真实结果 |
| R4 首次业务后端编译失败：`JacksonException` 是 `RuntimeException` 子类，不能同时出现在 multi-catch | 1 | 收窄为捕获 `RuntimeException`，保留非重试异常优先透传后重新编译 |
| R4 首次 `make test-inference`：34 项中 2 项错误，新增测试调用 `SchemaEngine.validate` 时缺少 `instance_path` | 1 | 生产代码已完成对应路径；读取校验器真实签名后修正测试接线，不重复错误调用 |
| `tests.test_traceability` 固定期望 355 条、旧摘要，但当前锁和生成结果为 463 条 | 1 | 记录为 R0 前置追踪测试漂移；R0-04 扩展后同步改为结构性断言和新锁摘要 |
| 新追踪验证发现 14 条 P0 要求没有验收场景映射 | 1 | 生成器已明确列出缺口；补充或扩展与需求语义一致的 AT 场景后重新验证 |
| 合并更新测试、追踪说明和冲突记录时上下文未命中 | 1 | 文件中目标段落存在换行差异；拆分补丁并读取精确上下文后重试 |
| 第一版消费者迁移字段进入 `consumer-contract-v1` 后改变第一版生成源哈希 | 1 | 恢复第一版网络模式，新增独立 `consumer-migration-r1.schema.json`；重新生成后第一版 8 个源文件无差异 |
| `verify_v2_scope.py` 首次扫描遍历依赖和构建目录，未在合理时间结束 | 1 | 终止本次运行；改用带目录剪枝的源码遍历，显式排除 `node_modules`、`target`、`dist`、`build` 等产物 |
| 范围门禁以“文件中任意位置存在 V2”判定整个历史迁移测试为第二版，误报第一版冻结字段 | 1 | 收窄为逐行第二版表面判定；第二版契约与生成目录仍保持全量严格扫描 |
| Python 契约包编译器只扫描顶层 `*.py`，第二版子包未实际编译 | 1 | 改为递归 `rglob("*.py")`，重新执行离线和严格三语言编译 |
| `planning-with-files` 完成脚本只识别英文阶段/状态，中文计划被统计为 0 个阶段 | 1 | 保持仓库强制中文记录；改用未勾选项和中文未完成状态扫描，结果无命中，并记录该技能脚本本地化限制 |
| 沙箱内 `docker info` 无权访问用户 Docker 套接字 | 1 | 记录为环境待复核，不误判 Docker 缺失；在执行真实集成门禁时申请受控权限复核 |
| `rg` 备份证据扫描中的 `compose*.yml` 被 zsh 当作无匹配通配符拒绝 | 1 | 不重复原命令；后续使用 `rg --files` 提供显式文件列表或直接扫描已存在目录 |
| 向 `DatabaseMigrationIT` 插入 R2 场景时使用了不存在的方法名作为补丁上下文 | 1 | 读取实际相邻测试名，改在 `pgDumpRestoresSchemaHistoryAndBusinessRows` 前用精确上下文插入 |
| 业务后端单元测试 88 项中架构边界测试失败 1 项：API 直接依赖共享领域异常 | 1 | 不放宽门禁；将退役能力异常移到共享应用层，保持 API → application 的依赖方向后重跑 |
| 用 `unittest` 执行 pytest 风格的 `test_r2_data_verification.py`，未发现测试并返回 5 | 1 | 不重复运行器；改用仓库 `.venv` 中的 pytest 执行该测试 |
| 仓库 `.venv` 未安装 pytest，无法执行 pytest 风格新增测试 | 1 | 不安装新依赖；将测试改为标准库 `unittest.TestCase`，保持离线可执行 |
| 根 `.gitignore` 的 `training/` 规则误将业务后端训练源码排除，直接修改无法形成可交付变更 | 1 | 恢复忽略文件改动；改用已跟踪的共享 HTTP 过滤器统一拦截取消写路径，并记录该既有忽略规则限制 |
| 停写过滤器误用未引入的 `com.fasterxml.jackson.databind.ObjectMapper` 导致编译失败 | 1 | 使用项目现有 `tools.jackson.databind.ObjectMapper` 和统一 `StandardErrorFactory.write`，不新增依赖 |
| 停写过滤器测试将根 `JsonNode` 当作集合提取字段，导致 91 项中 1 项空指针 | 1 | 先解析根节点，再直接断言根节点的 `code` 字段；生产过滤器无需修改 |
| 首次真实 `make test-backend`：18 项集成测试中 1 失败、8 错误 | 1 | 已分解为 64 位失败摘要、测试 `record_version`、权限排序和应用上下文级联根因；分别定向修复后重跑完整门禁 |
| 第二次 `make test-backend` 仅剩从 V2 升级的执行数量仍期望 13，实际为 14 | 2 | 更新为 `V3—V16` 共 14 个迁移，并再次运行完整门禁 |
| 更新升级迁移计数时误用变量名 `v2`，实际测试变量为 `latest` | 1 | 读取精确行后使用实际变量上下文修改 |
| 最终 `make verify-p1-offline` 的旧安全测试仍要求已取消的数据集/训练权限 | 1 | 按 R2 权限语义改为全角色撤权、专用模型审批权限及系统操作员授权断言后重跑完整离线门禁 |
| 使用双引号执行包含反引号的 `rg` 模式，zsh 将 `RG2` 误作命令替换 | 1 | 命令未写文件；改用无反引号模式和精确文件扫描，并记录本次工具调用错误 |
| 修改 `AT-COMP-003` 后追踪冻结摘要与 9 项追踪单测失配 | 1 | 按生成器实际摘要更新验收场景哈希和 DOC-16 源文件哈希，再重跑锁验证与追踪单测 |
| 查找幂等执行器时使用了不存在的 `IdempotentExecutor.java` 与 `ApplicationResponse.java` 文件名 | 1 | 未修改文件；改用 `rg` 定位实际的 `IdempotencyService` 及其内嵌 `Response` 类型 |
| 合并补丁同时修改 YAML、属性类和新增接口时把属性类上下文错误匹配到 YAML | 1 | 补丁整体未应用；拆分为按文件精确补丁后继续 |
| 在业务后端子目录执行计划重读时使用根目录相对路径，`sed` 找不到 `task_plan.md` | 1 | Maven 编译仍独立执行；后续从仓库根目录读取计划并使用 `-f services/business-api/pom.xml` 验证 |
| R3 首次单元回归 91 项中架构边界失败 2 项：应用层依赖基础设施属性类，接口层依赖领域异常 | 1 | 不放宽架构门禁；新增应用层设置值对象并把跨层异常移动到应用层，由配置适配器完成映射 |
| 合并权限补丁时把控制器方法上下文错误归入安全配置文件 | 1 | 补丁整体未应用；拆分迁移、权限矩阵、安全配置和控制器四个补丁 |
| 首次 `make test-backend` 在 91 项单元测试阶段失败 1 项：系统管理员精确权限集合未包含 R3 三个原子权限 | 1 | 同步精确权限基线后重跑；本次未进入集成测试，不标记后端门禁通过 |
| R3 增加 `V17` 后 `make verify-data` 仍把迁移全集硬编码为 `V1—V16` | 1 | 保留对 R2 `V16` 的专属内容验证，把全序列检查改为连续至当前最高版本，并新增 R3 `V17` 必需数据不变量校验 |
| R5 首次直接执行 `pnpm --dir apps/web-console typecheck` 被严格工具链版本拒绝：当前 Node 24.14.0 / pnpm 11.9.0，不符合锁定 Node 20.13.1 / pnpm 10.34.5 | 1 | 不安装或放宽版本；改用仓库统一 `make test-web` 入口定位配置的受控工具链，缺失时明确记为前置条件失败 |
| R5 首次 `make test-backend`：92 项单元通过，7 个集成类因沙箱禁止访问 Docker 套接字全部未启动 | 1 | 不标记后端门禁通过；按权限规则在沙箱外重跑，真实 PostgreSQL、MinIO、RabbitMQ 环境中 23 项集成通过 |
| R5 结果证据扩展后的真实后端门禁：24 项集成中新增质量数值读取 1 项错误，PostgreSQL `numeric` 不支持 `ResultSet.getObject(..., Double.class)` | 1 | 改用 `getBigDecimal` 后显式转换为有限双精度值；不改变数据库列或网络语义，随后重跑门禁 |
| R5 最终 `make test-web` 类型检查失败：统一客户端声明实现完整 `ApiClientV2`，但本阶段仅接入其中手工检测操作 | 1 | 将编译约束收窄为生成接口的 R5 操作子集 `Pick<ApiClientV2, ...>`；仍由生成接口校验方法签名，不伪造未实现的 R7/R8 操作 |
| 放宽手工检测配置条件以暴露禁用能力后，完整后端门禁在 MinIO 集成上下文失败：事务服务为 `final`，Spring 无法创建 CGLIB 代理 | 1 | 保留能力接口的可观测禁用语义；移除应用服务的 `final` 限制并重跑完整门禁，不把首次运行记为通过 |
| R7 首次追加契约发现时 `apply_patch` 上下文与实际文件表述不一致 | 1 | 补丁未应用、没有文件损坏；读取精确上下文后拆分为小补丁继续 |
| R7 首次契约增量补丁的 `common-v2` 换行上下文未命中 | 1 | 三份契约未被修改；读取精确行号后拆分公共模式、事件模式和 OpenAPI 小补丁，随后成功应用 |
| R7 契约门禁首次发现 `SampleExportRequested.target_object` 不允许既有示例中的可选 `sha256/size_bytes` | 1 | 扩展目标对象模式允许哈希/大小可选存在；保持生成文件由生成器更新后重跑门禁 |
| R7 首次读取后端文件时误用不存在的 `local/tooldefect/business` 路径 | 1 | 通过 `rg --files` 复核后切换到实际 `com/tooldefect/business` 包根，未产生源码修改 |
| R7 一次并行读取输出超过上下文上限 | 1 | 改为按文件和小范围分段读取，不重复依赖被截断的输出 |
| R7 首次 Maven 编译从仓库根执行 `./mvnw`，根目录不存在 wrapper | 1 | 未修改文件；改用 `services/business-api/mvnw -f services/business-api/pom.xml` 的仓库根入口 |
| R7 worker 单测首次计算仓库路径多上移一级，找不到 `jobs/worker.py` | 1 | 将测试 fixture 路径改为 `parents[1]`，验证脚本本身已先通过 |

## R0 关闭判定（R1 入口）

- `R0-01`：源码产物完成。
- `R0-02`：源码产物完成。
- `R0-03`：源码产物完成。
- `R0-04`：生成、锁和测试完成。
- `R0-05`：源码扫描完成；项目所有者确认第一版从未实际使用，运行实例、用户、调用和历史记录数量均为零。
- `R0-06`：取消清单完成；源码对象继续按后续任务退役，运行零调用条件已满足。
- `RG0`：已通过，可以进入 R1 契约阶段。

## R1 当前关闭判定

- `R1-01`—`R1-08`：源码产物与规定门禁完成。
- `RG1`：本地通过，可进入 R2 数据模型阶段。
- 第一版 8 个既有生成源文件无差异；第二版以独立 `v2` 命名空间并存。
- 第二版消费者仍处于 `PLANNED` 或 `TESTING`，本阶段未宣称运行接入。

## 备注

- 完成每个阶段后同步更新状态。
- 每两次查看或搜索操作后，将关键发现写入 `findings.md`。
- 所有失败与缺失前置条件必须明确记录，不得按通过处理。

## R7 实施复核补充（2026-08-03）

- 已确认 `apps/web-console/src/api/client.ts` 的八个 R7 操作注册和方法补丁已成功应用。
- 已发现并修正应用层通过全限定名依赖 `JdbcSampleLibraryRepository` 游标编码器的分层问题；新增 `SampleCursor` 应用边界，基础设施仅负责非法游标到完整性失败的映射。
- 记录错误：首次把游标重构与 JDBC 文件尾部不匹配的上下文放在同一补丁中，补丁整体未应用；随后读取精确上下文并拆分为新增文件、应用服务和 JDBC 三个小补丁，已成功应用。
- 当前前端 R7 管理页、R7-06 外部交接说明和专项静态测试尚未完成；继续保留 R8 并发工作流已有的未提交改动。

| R7 首次 `make test-web`：Vue 模板把 `#` 作为绑定表达式写入 `:href`，`vue-tsc` 在模板第 260 行报 `TS1127` | 1 | 该位置仅用于展示对象登记引用，改为非交互文本，不生成未授权下载链接；随后重新运行前端门禁 |
| R7 第二次 `make test-web`：生成的 `v2/index.ts` 缺少 `JsonObject`，样本服务的只读请求对象和类型守卫也产生 TypeScript 错误 | 1 | 修正 `tools/generate-contracts/generate.py` 后重新生成；用可变组装对象、正确的对象类型守卫和显式数组收窄修复前端服务，再重跑契约与前端门禁 |
| R7 首次 `make test-security`：新增默认关闭断言寻找字面 `enabled: false`，实际配置使用 `${TD_SAMPLE_EXPORT_ENABLED:false}` | 1 | 按实际 Spring 配置表达式断言环境变量默认值，不放宽功能开关语义；重新运行安全门禁 |
| R7 首次 `make test-backend`：101 项单测通过；V20 真实迁移成功，但迁移历史断言仍为 19，且 `final` JDBC 仓储无法被事务 CGLIB 代理，造成 7 个上下文错误 | 1 | 将迁移断言更新为 20；移除 JDBC 仓储 `final` 限制以允许既有事务代理机制，重新运行后端门禁 |

## R7 后端门禁复核（2026-08-03）

- `make test-backend` 第二次通过：101 项单测、25 项 PostgreSQL 18.4/Testcontainers 集成测试，失败/错误/跳过均为 0；覆盖 V20 空库/升级迁移、MinIO、RabbitMQ 和迁移恢复路径。
- 目前 `R7-01`、`R7-02`、`R7-05` 的业务后端、前端和安全表面已具备；`R7-03`/`R7-04` 的 worker 核心、数据库事实和票据/清理结构已具备，但 worker 完成事件到业务作业投影的实际消费接线仍需补齐。
- 尚未执行 `make test-integration` 与 `make verify-data` 的本轮 R7 复验；`RG7` 暂不关闭。
