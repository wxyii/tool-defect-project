# 任务计划：手工批量检测与管理简化 R1

## 目标

依据 `Docs/16-手工批量检测与管理简化需求规格.md` 与 `Docs/17-手工批量检测整改实施计划.md` 完成 R1 双版本契约和生成基础，并以仓库规定的门禁验证交付结果。

## 当前阶段

R1 已完成，`RG1` 本地门禁已通过

## 阶段

### 阶段 1：R1 需求与现状核对

- [x] 确认 `RG0` 已通过并提取 `R1-01` 至 `R1-08`
- [x] 盘点当前第一版契约、生成器、验证器、生成包与测试
- [x] 核对第二版领域字段、状态、接口和事件的批准基线
- [x] 将发现记录到 `findings.md`
- **状态：** 已完成

### 阶段 2：双版本生成与验证设计

- [x] 明确第一版冻结、第二版独立生成和兼容锁布局
- [x] 明确 `verify-v2-scope` 的结构化负向检查范围
- [x] 确认受影响模块与增量验证矩阵
- **状态：** 已完成

### 阶段 3：R1 契约优先实施

- [x] 完成 `R1-01` 双版本生成器和验证器基础
- [x] 完成 `R1-02` 至 `R1-06` 第二版契约、示例、状态和清单
- [x] 完成 `R1-07` 三语言第二版生成包和离线编译测试
- [x] 完成 `R1-08` 第二版范围真实负向门禁
- [x] 增量验证每项修改
- **状态：** 已完成

### 阶段 4：`RG1` 门禁验证

- [x] 执行 `make verify-contracts-source`
- [x] 执行 `make verify-contracts`
- [x] 执行 `make verify-v2-scope`
- [x] 执行 `make test-contract-packages-offline`
- [x] 执行适用的 P1 离线门禁并如实记录限制
- **状态：** 已完成

### 阶段 5：交付

- [x] 核对任务编号、契约版本、验证命令、结果和已知限制
- [x] 更新三份规划文件并提交本轮交付说明
- **状态：** 已完成

## 关键问题

1. 当前生成器和验证器在哪些位置硬编码第一版？
2. 第二版公共模式、开放接口和异步事件的最小完整集合是什么？
3. 如何确保 `v1 / 1.0.0` 基线和生成包字节级不被第二版覆盖？
4. `verify-v2-scope` 如何逐项验证取消能力和历史快照滥用？

## 已作决策

| 决策 | 理由 |
|---|---|
| 使用仓库根目录三份 Markdown 文件记录全过程 | 用户明确要求使用 `planning-with-files`，且便于跨会话恢复 |
| 先完成需求与现状核对，再修改代码 | 遵守契约优先、修改所有权和安全失败规则 |
| R0 不修改网络契约，契约版本保持 `v1 / 1.0.0` | 目标 `v2 / 2.0.0` 属于 R1；R0 只闭合文档、决策、追踪和基线 |
| 第一版从未部署时，运行遥测和观察窗口标为不适用 | 项目所有者已确认第一版从未实际使用；这不是把缺失遥测解释为零调用，而是确认从未存在运行消费者 |
| R1 在现有 R0 未提交成果上增量实施 | 这些改动属于当前整改链路，必须保留，禁止覆盖或回退 |
| 第二版目标契约固定为 `v2 / 2.0.0`，第一版保持 `v1 / 1.0.0` | DOC-16 与 DOC-17 已批准双版本并行边界 |

## 遇到的错误

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `tests.test_traceability` 固定期望 355 条、旧摘要，但当前锁和生成结果为 463 条 | 1 | 记录为 R0 前置追踪测试漂移；R0-04 扩展后同步改为结构性断言和新锁摘要 |
| 新追踪验证发现 14 条 P0 要求没有验收场景映射 | 1 | 生成器已明确列出缺口；补充或扩展与需求语义一致的 AT 场景后重新验证 |
| 合并更新测试、追踪说明和冲突记录时上下文未命中 | 1 | 文件中目标段落存在换行差异；拆分补丁并读取精确上下文后重试 |
| 第一版消费者迁移字段进入 `consumer-contract-v1` 后改变第一版生成源哈希 | 1 | 恢复第一版网络模式，新增独立 `consumer-migration-r1.schema.json`；重新生成后第一版 8 个源文件无差异 |
| `verify_v2_scope.py` 首次扫描遍历依赖和构建目录，未在合理时间结束 | 1 | 终止本次运行；改用带目录剪枝的源码遍历，显式排除 `node_modules`、`target`、`dist`、`build` 等产物 |
| 范围门禁以“文件中任意位置存在 V2”判定整个历史迁移测试为第二版，误报第一版冻结字段 | 1 | 收窄为逐行第二版表面判定；第二版契约与生成目录仍保持全量严格扫描 |
| Python 契约包编译器只扫描顶层 `*.py`，第二版子包未实际编译 | 1 | 改为递归 `rglob("*.py")`，重新执行离线和严格三语言编译 |
| `planning-with-files` 完成脚本只识别英文阶段/状态，中文计划被统计为 0 个阶段 | 1 | 保持仓库强制中文记录；改用未勾选项和中文未完成状态扫描，结果无命中，并记录该技能脚本本地化限制 |

## R0 关闭判定（R1 入口）

- `R0-01`：源码产物完成。
- `R0-02`：源码产物完成。
- `R0-03`：源码产物完成。
- `R0-04`：生成、锁和测试完成。
- `R0-05`：源码扫描完成；项目所有者确认第一版从未实际使用，运行实例、用户、调用和历史记录数量均为零。
- `R0-06`：取消清单完成；源码对象继续按后续任务退役，运行零调用条件已满足。
- `RG0`：已通过，可以进入 R1 契约阶段。

## R1 当前关闭判定

- `R1-01`—`R1-08`：源码产物与规定门禁完成。
- `RG1`：本地通过，可进入 R2 数据模型阶段。
- 第一版 8 个既有生成源文件无差异；第二版以独立 `v2` 命名空间并存。
- 第二版消费者仍处于 `PLANNED` 或 `TESTING`，本阶段未宣称运行接入。

## 备注

- 完成每个阶段后同步更新状态。
- 每两次查看或搜索操作后，将关键发现写入 `findings.md`。
- 所有失败与缺失前置条件必须明确记录，不得按通过处理。
