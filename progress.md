# R0—R6 进度日志

## R6 会话恢复：2026-08-03

- **状态：** 入口重新核对中；暂不宣称 `RG6` 或 `R6` 完成。
- 已执行：读取 `planning-with-files` 技能说明；恢复 `task_plan.md`、`findings.md`、`progress.md`；确认目标文档存在；确认工作树有业务后端与存储适配器相关未提交改动。
- 当前约束：既有计划记录 `RG5` 未关闭，因此本轮先核对 R6 入口与 R6 任务边界；不覆盖其他未提交改动，不把缺失的真实前置条件或门禁证据标为通过。
- 已核对：`R6-01`—`R6-07`、R6 三项入口条件、`RG6` 六项通过标准及 `make test-backend`/`make test-web`/`make test-security`/`make test-e2e` 四个验证命令；DOC-16 对应 `FR-IAM-001`—`010`、`MIG-005`—`006` 和两类角色权限矩阵。
- 已初检：现有未提交改动主要属于 R5 审计摘要和对象存储安全；第二版契约角色枚举已冻结，但前端用户管理仍默认 `OPERATOR`，路由仍存在数据集/训练权限表面。
- 已盘点后端身份初始形态：`SystemRole` 仍为八类旧角色，权限矩阵和引导账号仍使用 `SYSTEM_OPERATOR`；会话过滤器当前仅下发原子权限。
- 已补充盘点：身份服务角色校验和身份响应仍以数据库旧角色为中心；前端 `/datasets`、`/training-runs` 路由及八类角色选择器仍存在。
- 已核对安全/数据层：`SecurityConfiguration` 仍开放旧数据集/训练 v1 路径，模型审批仍接受 `dataset:approve`；既有数据库迁移最高为 `V18`，后续迁移必须串行递增。
- 已核对验证资产：存在后端身份单测、前端认证/安全源码测试及通用安全/端到端测试；R5 自动化历史结果通过，但真实浏览器联合证据仍缺失，不能升级为 `RG5` 通过。
- 计划决策：按用户指令继续 R6 契约/身份/权限实施与离线回归；发布切换和 `RG6` 关闭继续保持 `HOLD`，不把缺失的真实前置标为通过。
- 工作树边界：发现其他所有者/并发改动扩展到手工检测前后端、S3、契约 v2 和验证器；本轮只审查并保留，不覆盖这些非 R6 身份文件。
- 已盘点测试基线：后端/前端测试仍大量断言旧角色和 `OPERATOR` 会话，尚未覆盖两类角色白名单、迁移预览、深链接归零与未确认高权限安全失败。
- 下一步：确认契约生成输出路径、数据库角色/权限表结构和 R6 可复用服务边界，形成实施设计后再改代码。

## R6 身份与权限实施推进：2026-08-03

- 契约源已扩展本地用户显示名称更新操作，并将本地会话、创建用户、角色更新和用户摘要的角色字段收紧为两类人员角色；等待重新生成三语言包。
- 后端已补充显示名称更新、角色迁移确认请求头、冲突状态安全失败和已确认账号角色变更；`V19` 初始迁移将旧操作员建议为生产员工，其余旧角色进入冲突或未确认清单。
- 前端已删除数据集/训练路由与懒加载注册，用户管理改为两类角色、迁移预览/确认、显示名称保存和状态/密码操作；数据集/训练兼容组件暂不物理删除，留待 R9 清理。
- 角色矩阵单测、前端旧角色解析测试、数据库迁移断言和安全源码断言已同步到 R6 语义；尚未执行业务后端/前端/安全/端到端完整门禁。
- `make test-security` 已通过：密钥扫描、部署安全基线和 21 项安全测试通过。
- 首次 `make test-web` 的类型检查通过、Vitest 44 项中 43 项通过；唯一失败是新增测试错误地把保留的模型路由也断言为不存在，已记录并准备修正测试断言。
- 首次 `make test-backend` 的 100 项单测和数据库迁移集成均通过；`OverviewAuditQueryIT` 暴露出历史夹具仍绑定已撤销权限的 `OPERATOR`，已改为 R6 的 `PRODUCTION_EMPLOYEE` 夹具，等待重跑。

## R6 会话：2026-08-03

### 阶段 0：入口条件与现状核对

- **状态：** `HOLD`
- 已执行：
  - 按 `planning-with-files` 技能恢复仓库根目录的三份持久化记录。
  - 确认用户要求开始 R6，并将 `task_plan.md` 当前阶段切换到 R6。
  - 发现 R5 自动化源码门禁已完成，但真实浏览器联合验收与完整历史筛选仍未闭合，`RG5` 当前记录为未关闭。
  - 提取 `R6-01`—`R6-07`、`RG6` 六项标准和四项验证命令。
  - 初步确认第二版公共契约已冻结两类人员角色，但尚未找到逐账号映射决定清单。
  - 读取 R0 冻结运行清单，确认运行用户为零且逐账号映射批准明确标为不适用；该项不阻断 R6。
  - 确认 `RG5` 是 R6 唯一未满足的硬入口；R5 仍缺真实全链路浏览器证据，并有完整历史筛选限制。
  - 按安全失败规则将 R6 置为 `HOLD`，未修改生产源码，也未把缺失验证标记为通过。
  - 执行 `git diff --check`，三份规划文件格式检查通过。
- 下一步：
  - 由项目所有者选择先闭合 R5（含完整历史筛选与真实联合验收），或正式修改 R6 入口条件并记录批准依据。
  - 入口解除后继续盘点 R6 身份、迁移、会话、用户管理、菜单、权限与手册实现。

### R6 入口核验结果

| 检查项 | 证据 | 结果 |
|---|---|---|
| `RG5` 通过 | `task_plan.md`、`findings.md` 的 R5 关闭判定 | 未满足，阻断 |
| `R0-05` 用户与旧角色基线完整 | `Docs/baseline/R0-v1-runtime-inventory.json` | 满足 |
| 每个旧高权限账号已有目标角色决定 | 运行用户总数为 0，`NOT_APPLICABLE_NO_RUNTIME_USERS` | 不适用，不阻断 |
| 第二版契约角色基线 | `contracts/json-schema/common-v2.schema.json` | 已有两类角色枚举 |

### 本轮验证

| 命令 | 结果 | 状态 |
|---|---|---|
| `git diff --check` | 无输出，退出 0 | 通过 |
| `make test-backend`、`make test-web`、`make test-security`、`make test-e2e` | R6 尚未获准实施，未执行 | 未执行，不计通过 |

## R3 会话：2026-08-03

### 阶段 1—5：入口、实施、门禁与交付

- **状态：** 已完成，详见本文件末尾的 R3 验证结果
- 已执行：
  - 完整重读 `planning-with-files` 技能说明并恢复三份持久化记录。
  - 确认 R0—R2 已完成且 `RG2` 已通过，可以开始 R3 核查。
  - 从 `DOC-17` 提取 `R3-01` 至 `R3-05`、`RG3` 标准和入口配置要求。
  - 确认工作树包含整改链路已有未提交改动，后续仅做增量修改。
  - 确认 R3 的四项规定门禁，并盘点现有 `detectionbatch` 与 `storage` 模块。
  - 发现存储基础能力可复用，但手工功能开关、对象前缀、批次配额、媒体类型清单和孤儿策略仍需显式配置。
  - 对齐第二版 7 个 R3 接口与写请求字段，确认手工原图无需伪造第一版采集记录。
  - 决定用 `V17` 追加 R3 持久化事实并修正草稿期聚合语义，不改写已关闭的 `V16`。
  - 首次按推测文件名查找共享幂等执行器失败；已改用源码检索定位到 `IdempotencyService`，错误已记入计划且未重复失败命令。
  - 一次跨文件合并补丁因上下文归属错误未应用；已记录并改为按文件拆分。
  - 首次编译前在业务后端子目录读取根计划文件失败；已记录路径错误，后续统一从仓库根执行验证。
  - 业务后端源码编译通过；首次 91 项单元回归有 2 项架构边界失败，已定位为 R3 新代码的跨层依赖，未标记通过。
  - 修复跨层依赖后，91 项业务后端单元测试全部通过。
  - 扩展第二版人员安全链并新增手工检测原子权限及过渡角色映射；本人/全部数据范围由 `manual-detection:read:all` 明确控制。
  - 首次 `make test-backend` 在 91 项单元测试中因旧精确权限基线缺少 R3 三项权限而失败，尚未进入集成测试；已同步测试基线并重跑通过。
  - 完成 `R3-01`—`R3-05`、四项规定门禁和附加回归，`RG3` 通过。
- 下一步：
  - 进入后续阶段的任务消费、算法执行与结果回调闭环。

## 会话：2026-08-03

### R2 实施与门禁

- **状态：** 已完成；项目所有者明确免除运行观察窗，`RG2` 已通过
- **开始时间：** 2026-08-03
- 已执行：
  - 完整重读 `planning-with-files` 技能说明并恢复三份持久化记录。
  - 确认 `R1-01` 至 `R1-08` 和 `RG1` 已完成，可以开始 R2 核查。
  - 从 `DOC-17` 提取 `R2-01` 至 `R2-08`、数据迁移不变量、`RG2` 标准和三项规定门禁。
  - 将任务计划从 R1 切换为 R2，并明确真实数据库、备份、观察窗和遥测证据不得静默跳过。
  - 确认现有迁移最高为 `V15`、无并行业务后端工作树修改，本轮独占下一序号 `V16`。
  - 盘点第一版核心表、采集仓储、模型仓储、角色权限矩阵、数据门禁与 Testcontainers 迁移测试。
  - 发现 `verify-data` 尚不覆盖 R2 数据不变量；发现沙箱访问 Docker 套接字被拒绝，已记录待受控权限复核。
  - 对齐第二版冻结契约中的统一批次、单项、质量、反馈、样本、导出、模型上传和历史来源模式。
  - 完成 R2 数据模型设计：`V16` 串行迁移、可重复回填函数、稳定失败清单、影子差异、只追加事实和模型来源互斥约束。
  - 确认第一版从未部署的冻结消费者证据可支持默认关闭数据集/训练写开关，但不等同于生产观察窗证据。
  - 新增 `V16`、统一批次领域聚合、回填与影子读仓储，以及相互独立的取消能力/产线写开关；业务后端跳过测试编译通过。
  - 首次业务后端单元测试运行 88 项，87 项通过、1 项架构边界失败；已定位为退役异常所在包错误。
  - 修复依赖方向后业务后端 88 项单元测试全部通过。
  - `make verify-data` 通过，输出 R2 迁移版本 16 且错误列表为空。
  - 受控确认 Docker 29.6.2 可用；首次 `make test-backend` 已在 PostgreSQL 18.4 上成功完成空库 `V1—V16` 迁移，但整体因 R2 摘要长度、测试版本增量、权限顺序及应用上下文级联问题失败，未标记通过。
  - 修复全部集成问题后，`make test-backend` 已通过 91 项单元测试和 19 项 PostgreSQL 18.4/Testcontainers 集成测试，包含隔离 `pg_dump`/`pg_restore` 恢复演练。
  - 最终 P1 离线回归发现 18 项安全测试中 1 项仍断言已取消的数据集/训练权限；该次运行明确记为失败，正在按 R2 权限语义修正测试后重跑。
  - 修正旧安全断言后，`make verify-p1-offline` 通过：18 项安全测试和 41 个软件物料清单组件检查通过。
  - `make test-integration` 通过 5 项测试；`make verify-data` 返回 `PASSED`、迁移版本 16、错误列表为空；`make verify-v2-scope` 通过。
  - R2 的确定性影子差异、回填幂等和失败清单测试已通过。
  - 项目所有者明确指示第一版从未部署时不考虑批准运行观察窗；已同步修订 `DOC-17` 的 `R2-08`、`RG2` 标准和开关阶段，并据此解除 `HOLD`。
  - 同步修订 DOC-16 验收场景与 ADR-0003 后，首次追踪锁验证和 9 项追踪单测因冻结摘要漂移失败；已按生成结果更新验收场景及产品文档哈希，等待复验，不把首次失败记为通过。
  - 更新冻结摘要后追踪锁验证通过，9 项追踪单测全部通过；验收场景数量保持 23，需求数量保持 476，未产生未映射需求。
- 下一步：
  - 进入以 `RG2` 为入口条件的后续阶段。

### R2 验证结果

| 测试 | 命令 | 实际 | 状态 |
|---|---|---|---|
| 业务后端与真实数据库集成 | `make test-backend` | 91 项单元测试、19 项 PostgreSQL 18.4/Testcontainers 集成测试通过；包含空库、升级、幂等回填、互斥来源和隔离恢复演练 | 通过 |
| 集成边界 | `make test-integration` | 5 项通过 | 通过 |
| 数据不变量 | `make verify-data` | `PASSED`；`r2_migration_version=16`；`errors=[]` | 通过 |
| 第二版范围 | `make verify-v2-scope` | 取消清单、生成类型、消费者表面和历史快照边界通过 | 通过 |
| P1 离线回归 | `make verify-p1-offline` | 18 项安全测试、41 个软件物料清单组件检查通过 | 通过（有既有布局警告） |
| 追踪冻结锁 | `.venv/bin/python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json --compact` | `valid=true`；23 个验收场景、476 条设计要求 | 通过 |
| 追踪单测 | `.venv/bin/python -m unittest tests.test_traceability` | 9 项通过 | 通过 |
| 运行观察窗豁免 | 项目所有者决策与冻结消费者清单 | 第一版从未部署，不要求运行观察窗 | 通过（豁免） |

### 阶段 1：需求与现状核对

- **状态：** 已完成
- **开始时间：** 2026-08-03
- 已执行：
  - 完整读取 `planning-with-files` 技能说明与模板。
  - 确认两份目标文档存在。
  - 初始化 R0 的三份持久化规划文件。
  - 提取 R0 六项任务、`RG0` 通过标准和建议验证命令。
  - 确认当前契约 `v1 / 1.0.0`、目标契约 `v2 / 2.0.0`，R0 本身不修改网络契约。
  - 初步盘点 R0 产物：`R0-01` 与 `ADR-0003` 已有基础，`R0-04` 明确尚未实现。
  - 扩展追踪生成器初版，解析出 104 条产品要求、88 条 P0 和 19 个验收场景。
  - 首次覆盖验证真实暴露 14 条 P0 无验收映射。
  - 新增 4 个 AT 场景并扩展现有映射，全部 88 条 P0 已具备任务和验收引用。
  - 新增 ADR-0004 至 ADR-0007，闭合统一批次、多视角退役、两角色映射和模型直传安全边界。
  - 扫描取消范围和第一版多视角、角色、消费者及在线训练依赖的实际源码位置。
- 已创建或修改：
  - `task_plan.md`（新建）
  - `findings.md`（新建）
  - `progress.md`（新建）

### 阶段 2：实施设计

- **状态：** 已完成

### 阶段 3：R0 实施

- **状态：** 已完成
- 已执行：
  - `R0-01`：确认索引已登记文档职责与原始需求来源。
  - `R0-02`：修订 `Docs/02`、`Docs/04` 并形成 `Docs/01—14` 冲突闭合记录。
  - `R0-03`：新增 ADR-0004 至 ADR-0007，保留并同步 ADR-0003。
  - `R0-04`：追踪生成器升级到 `2.0.0`，纳入 DOC-16 产品编号、稳定编号、任务和验收引用。
  - `R0-05`：冻结源码清单和两次一致扫描；项目所有者确认第一版从未实际使用，运行态数量闭合为零。
  - `R0-06`：冻结 7 类取消对象，每类均记录消费者、历史保留和退役任务。
  - 项目所有者确认第一版从未实际使用；新增可审计声明并将运行态基线和取消清单从 `HOLD` 更新为通过。

### 阶段 4：门禁验证

- **状态：** 已完成

### 阶段 5：交付

- **状态：** 已完成（`RG0` 已通过）

## 测试结果

| 测试 | 命令 | 期望 | 实际 | 状态 |
|---|---|---|---|---|
| 旧矩阵摘要 | `python tools/traceability/build_matrix.py --summary` | 输出可重复摘要 | 463 条要求、614 个测试链接 | 通过 |
| 旧矩阵锁验证 | `python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json` | 锁一致 | `valid: true`，退出 0 | 通过 |
| 追踪单元测试基线 | `python -m unittest tests.test_traceability` | 全部通过 | 7 项中 1 项失败：硬编码 355，实际 463 | 失败 |
| R0-04 初版语法 | `python -m py_compile tools/traceability/build_matrix.py` | 无语法错误 | 退出 0 | 通过 |
| R0-04 初版锁验证 | `python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json` | 暴露缺失映射和锁漂移 | 14 条 P0 无 AT 映射，锁为旧模式 | 预期失败 |
| R0-04 最终锁验证 | `python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json --compact` | 矩阵确定且无覆盖缺口 | `valid: true`；476 条设计要求、104 条产品要求、88 条 P0、23 个 AT；最终摘要 616 个测试链接 | 通过 |
| 追踪单元测试 | `python -m unittest tests.test_traceability` | 全部通过 | 8 项通过，退出 0 | 通过 |
| JSON 与差异格式 | `python -m json.tool ...`、`git diff --check` | 无格式错误 | 退出 0 | 通过 |
| 仓库布局 | `make verify-layout` | 布局通过 | 退出 0；有冻结数据和 Git 大小写警告 | 通过（有警告） |
| R0 源码重复扫描 | 两次相同 `shasum` 清单摘要 | 摘要一致 | 两次均为 `67bdd750...37e64a` | 通过 |
| 追踪与基线单元测试 | `python -m unittest tests.test_traceability` | 全部通过 | 9 项通过，退出 0 | 通过 |
| P1 离线源码门禁 | `make verify-p1-offline` | 所有源码前置与离线门禁通过 | 退出 0；18 项安全测试通过，SBOM 41 个组件；有 2 条既有布局警告 | 通过（有警告） |
| `RG0` 解除后锁验证 | `python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json --compact` | 矩阵与锁一致 | `valid: true`，退出 0 | 通过 |
| `RG0` 解除后追踪测试 | `python -m unittest tests.test_traceability` | 未使用声明、基线和追踪全部通过 | 9 项通过，退出 0 | 通过 |
| `RG0` 解除后离线门禁 | `make verify-p1-offline` | 离线源码门禁保持通过 | 退出 0；18 项安全测试、41 个软件物料清单组件检查通过 | 通过（有既有警告） |

## 错误日志

| 时间 | 错误 | 尝试 | 处理 |
|---|---|---:|---|
| 2026-08-03 | 追踪单测硬编码计数与当前锁漂移（355 对 463） | 1 | 纳入 R0-04，改为结构与锁一致性断言 |
| 2026-08-03 | 14 条 P0 要求无验收场景映射 | 1 | 按语义新增或扩展 AT 场景，不降低校验强度 |
| 2026-08-03 | `apply_patch` 更新追踪说明时上下文未命中 | 1 | 原子补丁未产生修改；改为按文件拆分补丁 |

## 五问恢复检查

| 问题 | 答案 |
|---|---|
| 当前在哪里？ | R1 已开始，正在盘点双版本契约与生成基础 |
| 接下来去哪里？ | 完成 `R1-01` 至 `R1-08` 并执行 `RG1` 门禁 |
| 目标是什么？ | 完成两份指定文档定义的 R1 阶段 |
| 已了解什么？ | 见 `findings.md` |
| 已完成什么？ | R0 已闭合；R1 范围、目标版本和门禁已提取 |

## R1 会话：2026-08-03

### 阶段 1：需求与现状核对

- **状态：** 已完成
- 已执行：
  - 按 `planning-with-files` 恢复三份持久化记录。
  - 确认 `RG0` 已通过，R1 入口成立。
  - 提取 `R1-01` 至 `R1-08`、`RG1` 标准和四个硬门禁。
  - 确认目标契约为 `v2 / 2.0.0`，第一版 `v1 / 1.0.0` 必须保持兼容。
  - 确认工作区包含 R0 未提交成果，后续增量实施并避让已有修改。
  - 盘点第一版契约文件、生成器、验证器、兼容检查和 Makefile 门禁。
  - 确认生成与验证流程存在第一版硬编码，兼容快照扫描范围也需先隔离主版本。
  - 提取第二版核心枚举、公共模式、25 个接口和 7 个事件的批准基线。

### 阶段 2：双版本生成与验证设计

- **状态：** 已完成
- 已决定：
  - 第一版源文件和生成文件保持原路径与内容；源哈希按主版本隔离。
  - 第二版生成类型使用独立子命名空间，避免与第一版链接时混用。
  - 第二版兼容基线独立建立，第一版快照只扫描 `*-v1`。
  - `verify-v2-scope` 使用结构化契约与生成物检查，并扫描消费者表面，不以空目标或单纯文本存在判断代替。

### 阶段 3：R1 契约优先实施

- **状态：** 已完成
- 已执行：
  - 生成器源哈希改为按主版本隔离，并新增三语言第二版子命名空间输出。
  - 新增第二版公共模式、25 个目标业务接口和错误响应表面。
  - 新增第二版 7 个异步事件及严格载荷模式。
  - 新增第二版写接口七类示例、状态转换正反例和全部事件正反例。
  - 第一版消费者清单补齐所有者、遥测、契约版本和迁移状态，并使用独立 R1 治理模式验证。
  - 已生成第二版 Python、Java、TypeScript 子命名空间并建立 `v2` 独立兼容基线。

## R1 增量测试结果

| 测试 | 命令 | 实际 | 状态 |
|---|---|---|---|
| Python 工具语法 | `python3 -m py_compile tools/generate-contracts/generate.py tools/verify-contracts/*.py` | 退出 0 | 通过 |
| 第二版 JSON 解析 | `python3 -m json.tool`（全部新增第二版 JSON） | 退出 0 | 通过 |
| 两代契约验证 | `python3 tools/verify-contracts/verify_contracts.py` | v1/v2 模式、示例、状态机、OpenAPI、AsyncAPI、消费者通过 | 通过 |
| 第一版生成冻结 | `git diff --name-only --`（8 个既有生成源） | 无输出 | 通过 |
| 第一版兼容 | `python3 tools/verify-contracts/compatibility.py --self-test` | `v1 兼容检查通过` | 通过 |
| 第二版兼容 | `python3 tools/verify-contracts/compatibility.py --major 2 --self-test` | `v2 兼容检查通过` | 通过 |

## R1 错误日志

| 时间 | 错误 | 尝试 | 处理 |
|---|---|---:|---|
| 2026-08-03 | `verify-v2-scope` 遍历依赖/构建目录导致运行超时 | 1 | 已终止；增加源码遍历剪枝后重跑 |
| 2026-08-03 | 历史数据库迁移测试因文件内存在 `V2` 字样被整体误判为第二版 | 1 | 消费者源码改为逐行识别第二版表面；契约和生成物仍全量扫描 |
| 2026-08-03 | Python 离线包门禁未递归编译第二版子包 | 1 | 改为 `rglob("*.py")` 后重跑全部契约包门禁 |
| 2026-08-03 | `planning-with-files` 完成脚本无法识别中文阶段状态，误报 0 阶段 | 1 | 遵守仓库中文规则，使用 `rg` 检查未勾选项和中文未完成状态；无命中 |

### 阶段 4：`RG1` 门禁验证

- **状态：** 已完成

| 测试 | 命令 | 实际 | 状态 |
|---|---|---|---|
| 契约源码门禁 | `make verify-contracts-source` | 两代模式/示例/接口/事件/消费者、两代兼容锁、确定性生成、Python/Java 编译通过 | 通过 |
| 严格三语言契约门禁 | `make verify-contracts` | Python、Java、TypeScript 两代生成物编译通过 | 通过 |
| 第二版范围门禁 | `make verify-v2-scope` | 取消清单、生成类型、显式第二版消费者表面、历史快照边界及负向自测通过 | 通过 |
| 离线契约包门禁 | `make test-contract-packages-offline` | Python/Java 两代递归编译通过 | 通过 |
| P1 离线回归 | `make verify-p1-offline` | 18 项安全测试、41 组件软件物料清单及其余离线门禁通过 | 通过（有既有布局警告） |
| 差异格式 | `git diff --check` | 退出 0 | 通过 |

### 阶段 5：交付

- **状态：** 已完成
- 任务编号：`R1-01`—`R1-08`。
- 契约版本：第一版 `v1 / 1.0.0`；第二版 `v2 / 2.0.0`。
- `RG1`：本地门禁通过。
- 已知限制：未执行 `make verify-p1-strict`；第二版消费者尚未运行接入；保留既有冻结数据和 Git 大小写警告。

## R1 五问恢复检查

| 问题 | 答案 |
|---|---|
| 当前在哪里？ | R1 已完成，`RG1` 本地通过 |
| 接下来去哪里？ | R2 数据模型扩展、回填和影子读 |
| 目标是什么？ | 双版本契约、生成包、兼容锁和第二版负向门禁 |
| 已了解什么？ | 见 `findings.md` 的 R1 关闭结论 |
| 已完成什么？ | `R1-01`—`R1-08` 和规定的四项 `RG1` 门禁 |

## 会话：2026-08-03 R3 实施与门禁

- **状态：** 已完成，`RG3` 通过
- **任务编号：** `R3-01`—`R3-05`
- **契约版本：** 第一版 `v1 / 1.0.0` 保持兼容；第二版 `v2 / 2.0.0`
- 已执行：
  - 新增 `V17` 手工批次迁移，建立上传会话、每项唯一逻辑任务、补偿事实、原子权限和分阶段聚合状态机。
  - 实现批次创建、增删项、上传确认、幂等提交、本人/全部查询、稳定游标及短时读取票据。
  - 复用 S3 兼容短时票据与对象头校验，并补齐对象删除、孤儿抢占清理和 `HOLD` 补偿事实。
  - 将人员安全链扩展到第二版路径，增加手工检测三项原子权限和越权隔离。
  - 按请求主版本生成标准错误；第二版使用 `error_code` 冻结形状，第一版保持原形状。
  - 增加至少 10 项逐项确认、每项唯一任务、越权、故障补偿、迁移数据不变量与真实 MinIO 删除验证。
  - 修正应用层条目标识贯穿对象键和数据库记录，避免对象与条目追踪漂移。
  - 首次最终后端门禁暴露事务服务 `final` 导致代理创建失败，明确记录失败并修复；第二次完整运行通过。

### R3 验证结果

| 测试 | 命令 | 实际 | 状态 |
|---|---|---|---|
| 业务后端与真实容器集成 | `make test-backend` | 91 项单元测试、21 项集成测试通过；PostgreSQL 18.4、MinIO、RabbitMQ 均实际启动 | 通过 |
| R3 集成边界 | `make test-integration` | 6 项通过 | 通过 |
| R3 安全与部署基线 | `make test-security` | 密钥及部署安全扫描通过，21 项安全测试通过 | 通过 |
| R3 故障注入 | `make test-faults` | 19 项通过 | 通过 |
| 数据迁移不变量 | `make verify-data` | `PASSED`；最高迁移版本 17；`errors=[]` | 通过 |
| 第二版范围 | `make verify-v2-scope` | 取消清单、生成类型、消费者表面和历史快照边界通过 | 通过 |
| 追踪冻结锁 | `.venv/bin/python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json --compact` | `valid=true`；23 个验收场景、476 条设计要求 | 通过 |
| 追踪单测 | `.venv/bin/python -m unittest tests.test_traceability` | 9 项通过 | 通过 |
| 差异格式 | `git diff --check` | 退出 0 | 通过 |

### R3 交付限制

- 功能开关默认关闭，生产启用前必须配置真实数据库和对象存储；整个存储模块关闭时能力控制器不装配。
- 本阶段止于逻辑任务创建；任务消费者、算法执行与最终回调属于后续阶段。
- 未运行全阶段汇总入口 `make verify-all`，不据此宣称全阶段严格环境通过。
# R4 启动记录（2026-08-03）

- 使用 `planning-with-files` 恢复并读取 `task_plan.md`、`findings.md`、`progress.md`。
- 已核对 DOC-17：R4 入口依赖为 `RG2` 通过、R3 批次和图片项接口在集成环境稳定、第二版推理事件和结果模式冻结；前两项已有上一阶段交付记录，事件与结果冻结状态待源码核对。
- 已把 `task_plan.md` 从已完成的 R3 切换为 R4，建立 `R4-01`—`R4-06` 与 `RG4` 验证清单。
- 已完成首轮契约与源码盘点：第二版事件契约已冻结，但推理服务当前运行编排仍是第一版多图片任务模型，R4 需要以并行适配方式新增第二版单图消费路径。
- 已盘点推理可靠性与边缘同步基础，确认可保留第一版兼容实现并增量增加第二版单图适配；下一步核对 R3 事件生产、第二版回调端点和 OpenAPI 产线表面。
- 已确认 R4 需要补齐三段断链：业务后端第二版任务发布/结果落库、推理服务第二版单图质量执行、边缘端第二版产线单项提交；同时需要以 `V18` 修正全质量拒绝聚合语义。
- 已完成 R4 入口核对和实施设计，进入 `R4-01`—`R4-06` 编码阶段；契约保持 `v2 / 2.0.0` 不变，第一版兼容代码保持原位。
- 已新增第二版单图片项事件解析、质量编排、完成/失败发布和五类版本化质量检查；首轮 `make test-inference` 共 34 项，其中 32 通过、2 项因测试调用契约校验器参数不完整而错误，已记录并进入修正。
- 修正契约校验测试接线后重跑 `make test-inference`：34 项全部通过。
- 已新增第二版产线单对象生成客户端适配、同一 `capture_id` 哈希冲突拒绝和第二版用量指标；`make test-edge`：119 项全部通过。
- 边缘测试输出明确指出真实相机、触发器、设备清单和现场批准证据缺失；模拟门禁通过不替代受控硬件验收，`RG4` 最终必须保留此限制。
- 业务后端首次增量编译在 `R4InferenceResultHandler` 的异常 multi-catch 处失败；编译器确认仓库使用的 `JacksonException` 属于 `RuntimeException`，已记录并改为单一运行时异常捕获。
- 沙箱内后端门禁因 Docker 套接字权限明确失败；沙箱外重跑后 92 项单元全部通过，真实 PostgreSQL/MinIO/RabbitMQ 均成功启动。22 项集成仅剩三处测试基线问题：迁移计数两处、测试批次号长度一处；`V18` 空库与升级迁移均已实际成功。
# 2026-08-03 R4 后端真实容器第二轮

- `make test-backend`：失败。92 个单元测试通过，22 个集成测试中剩余 1 个失败、1 个错误。
- `DatabaseMigrationIT.r4SingleItemResultsDriveSafeAggregateStates`：R4 测试夹具把图片直接置为 `AVAILABLE`，却未填写既有约束要求的宽高；已补充 `64x64`。
- `ReliableMessagingIT.confirmedOutboxRetriesMandatoryReturnAndPublishesPersistentMessage`：首次不可路由符合预期，动态补绑后的立即重试未领取到事件；将测试重试时间显式置于数据库当前时间之前，排除数据库时钟边界。
# 2026-08-03 R4 单图链路增量验证

- `make test-inference`：通过，36 项；覆盖第二版单图解码无视角属性、五项质量检查、质量拒绝跳过算法、技术失败和持久幂等回放。
- `make test-backend`：通过，92 项单元测试、22 项真实容器集成测试；PostgreSQL、MinIO、RabbitMQ 均实际启动，V18 空库/升级/备份恢复及 R4 聚合场景通过。
- 新增 R4 集成、端到端和故障用例，等待统一入口执行。
- 第二版边缘消费者状态更新为 `TESTING`，补充升级顺序、回滚方式和分版本用量指标基线；第一版保持冻结兼容。
- 首轮 `RG4`：边缘 119 项、推理 36 项、集成 8 项、端到端 39 项通过；故障 21 项中新增枚举源码断言 1 项失败，已按 V18 的真实终态模型修正测试，等待重跑。

# 2026-08-03 R4 收尾

- `make test-edge`：通过，119 项；硬件前置检查安全报告缺失项，未把模拟测试冒充硬件通过。
- `make test-inference`：通过，36 项。
- `make test-integration`：通过，8 项。
- `make test-e2e`：通过，39 项。
- `make test-faults`：通过，21 项。
- `make test-backend`：最终通过，92 项单元测试、23 项真实容器集成测试；新增产线仓储宽高与幂等用例通过。
- `make verify-contracts-source`、`make verify-v2-scope`、`make verify-data`、`make verify-p1-offline`：全部通过；数据门禁识别最新迁移为 V18。
- `R4-01`—`R4-06` 源码、契约、模拟和真实服务基础设施验证完成。
- `RG4` 关闭判定：源码与模拟门禁通过；真实相机和触发设备验收因前置缺失保持 `HOLD`，不得标为全量通过。

# 2026-08-03 R4 硬件验收豁免

- 项目所有者明确指示跳过真实硬件验收并解除 `HOLD`。
- 真实相机、触发设备、设备清单、现场配置和批准证据仍未提供；本项记录为“已豁免、未执行”，不记录为验证通过。
- `RG4` 按批准后的范围关闭，`R5` 入口解除阻断；真实硬件上线或扩大站点范围前必须在 `RG9` 恢复验收。
# R5 会话进度（2026-08-03）

- 已读取用户指定的 `planning-with-files` 技能并按其规则接管进度记录。
- 已确认三份规划文件和 `DOC-16`、`DOC-17` 均存在。
- 已把 `task_plan.md` 当前目标从已关闭的 R4 切换为 R5，并建立入口核对、设计、实施、`RG5` 验证和交付阶段。
- 当前正在提取 R5 任务编号、验收条件并盘点代码现状；尚未修改生产代码，尚未宣称任何 R5 门禁通过。
- 已提取 `R5-01`—`R5-06` 的职责概要及核心前端约束。
- 已检查工作树：检测到现有 R4 未提交改动，后续只做增量修改并在差异审查时区分本轮 R5 变更。
- 已读取 R5 完整章节、相关正式需求与前端工程清单；确认 R5 前端能力尚需新增。
- 下一步盘点第二版生成 TypeScript 类型、R3 后端接口及既有前端测试/路由模式，形成实施设计。
- 已确认 R3 主要批次接口可用，并发现快速反馈后端实现与 OpenAPI 声明之间可能存在缺口。
- 已确认 R5 必须扩展统一 API 客户端以支持稳定幂等键和第二版操作。
- 已完成 R5 入口核对与实施设计阶段，`task_plan.md` 已切换状态。
- 已确认快速反馈数据库结构和第二版生成类型可复用，不新增契约或数据库迁移。
- 已核对快速反馈冻结请求模式、前端严格编译设置和现有权限接线，开始生产代码实施。
- 已补齐快速反馈仓储、应用服务、控制器与 `PUT` 权限接线；尚待编译和集成验证。
- 当前转入契约缺口核对：批次详情缺少图片项集合，图片项类型缺少完整结果/历史展示字段。
- 已扩展批次详情返回图片项集合，并扩展前端统一 API 客户端和第二版生成类型出口。
- 下一步实现手工检测前端服务、上传状态机、页面组件与测试，再回头处理编译和契约缺口。
- 第一轮直接前端类型检查未执行：环境 Node 24.14.0 / pnpm 11.9.0 与项目锁定 Node 20.13.1 / pnpm 10.34.5 不兼容；该结果不标记为通过，下一步使用仓库统一门禁入口。
- `make test-web` 第一轮通过：类型检查通过、既有 7 个测试文件 34 项通过、生产构建通过。该轮尚未包含 R5 新增专项测试，因此只作为增量编译证据，不关闭 `RG5`。
- `make test-web` 加入 R5 专项后通过：8 个测试文件、38 项测试全部通过，类型检查与生产构建通过。
- `make test-backend` 沙箱内因 Docker 权限失败；沙箱外真实重跑通过：92 项单元测试、23 项集成测试，失败/错误/跳过均为 0。
- 新增 R5 PostgreSQL 快速反馈用例后再次完整通过：92 项单元、24 项集成，失败/错误/跳过均为 0。
- `make test-integration` 8 项、`make test-e2e` 加入 R5 后 42 项、`make test-security` 21 项及两项安全扫描均通过。
- 差异审查发现结果图/质量详情仍缺，继续实施，不关闭 `RG5`。
- 已补齐逐图质量检查、技术错误和派生结果的受控读取及页面展示；首次真实后端回归发现 PostgreSQL `numeric` 到 `Double` 的 JDBC 直接转换不受支持，已改为 `BigDecimal` 显式转换，等待重跑。
- 定向 PostgreSQL 回归通过 16 项。最终前端门禁发现客户端误声明实现完整第二版接口；已收窄为生成 `ApiClientV2` 的 R5 操作子集，保留编译期契约约束，等待重跑。
- 修正客户端实现约束后最终 `make test-web` 通过：8 个测试文件、38 项测试，类型检查和生产构建全部成功。
- 最终 `make test-backend` 通过：92 项单元测试、24 项真实容器集成测试，PostgreSQL、MinIO、RabbitMQ 均实际启动，失败、错误和跳过均为 0。
- `git diff --check` 通过；本阶段没有修改 `contracts/`、生成网络类型或数据库迁移，契约保持第二版 `v2 / 2.0.0` 与第一版 `v1 / 1.0.0` 并存。

## R5 阶段性判定

- **任务编号：** `R5-01`—`R5-05` 源码完成；`R5-06` 自动化覆盖完成、真实浏览器联合链路未执行。
- **自动化结果：** `make test-web`、`make test-backend`、`make test-integration`、`make test-e2e`、`make test-security` 全部通过。
- **门禁状态：** `RG5` 未关闭。尚无启用功能开关、真实人员会话、对象存储、队列和推理服务联合运行的浏览器验收证据。
- **功能限制：** 历史筛选目前只覆盖已加载页的来源和状态，时间、操作人员、结果的服务端筛选尚未完成；现有持久结果没有完整模型、流水线和耗时字段，管理员历史元数据展示受限。
- **范围声明：** 本轮未执行 `make verify-all`，不宣称全阶段严格门禁通过。

## R5 错误字段兼容修复（2026-08-03）

- 修复统一前端错误解析器仅读取第一版 `code`、忽略第二版 `error_code` 的问题。
- 解析顺序调整为优先使用第二版 `error_code`，缺失时兼容第一版 `code`，两者都缺失时继续生成 HTTP 状态回退码。
- 新增第二版错误码展示和第一版兼容回归测试；`make test-web` 通过，9 个测试文件共 40 项测试、类型检查和生产构建均成功。

## R5 第二版可选字段校验修复（2026-08-03）

- 复现创建批次只提交必填 `usage_stage` 时，后端把可选 `usage_stage_note` 当作必填字段并返回 `TD-API-VALIDATION-001`。
- 根因是第二版控制器复用了要求字段集合完全相等的第一版严格对象读取器；同一问题也影响不带 `supersedes_record_id` 的快速反馈请求。
- 新增第二版允许字段与必填字段分离的严格读取器；创建批次和快速反馈均拒绝未知字段、保留必填校验，同时允许省略各自的可选字段。
- 首次控制器测试因当前模拟框架不能模拟具体服务类而错误退出；改用显式记录型测试替身后，定向 4 项测试全部通过。
- `make test-backend` 最终通过：96 项单元测试、24 项真实容器集成测试，失败、错误和跳过均为 0。
- 核对前端操作表后确认提交批次路径映射正确；页面通用异常分支会丢弃已解析的服务端错误，导致只显示固定失败文案。
- 上传页现会展示服务端消息、第二版错误码和请求标识；单项上传/确认失败也保留对应错误证据。`make test-web` 通过：9 个测试文件共 41 项测试、类型检查和生产构建均成功。
# R6 进度补充（2026-08-03）

- 契约校验完成：`make verify-contracts-source` 已通过契约源、示例、兼容性自检、确定性生成及 Python/Java 契约包编译。
- 源码复核发现并修复管理员权限集合缺逗号，以及部署审批继续使用旧 `dataset:approve` 的 R6 权限收敛问题。
- 当前仍在身份迁移、前端角色与旧路由清理、迁移测试阶段；不宣称 RG5 或 RG6 通过。

## R6 实施与门禁收尾（2026-08-03）

- `R6-01`—`R6-07` 已完成：人员角色收敛为 `PRODUCTION_EMPLOYEE`、`ADMINISTRATOR`；V19 增加角色迁移预览/确认事实、未确认与冲突状态、显式原因和会话撤销；用户管理支持显示名称编辑、启停、密码重置和幂等迁移确认。
- 旧高权限角色不会自动提权：仅旧 `OPERATOR` 且无额外角色时建议生产员工，空角色保持未确认，其他旧角色进入冲突；管理员映射需要逐项确认，冲突/高权限映射需要审计原因。
- `R6-05` 收尾移除了模型页中的冻结数据集/成功训练运行选择器，数据集和训练路由、菜单及懒加载入口保持不可达；旧组件、服务和第一版客户端方法按兼容窗口保留，物理清理由 `R9` 负责。
- 模型部署审批前端、后端控制器和安全配置统一使用 `model:approve`/`model:deploy:approve`，不再通过 `dataset:approve` 旁路。
- `make test-web` 最终通过：类型检查、9 个测试文件 44 项测试和生产构建均通过；首次测试失败来自错误地把模型路由判为已删除，已收窄为数据集/训练路由不可达断言。
- `make test-backend` 最终通过：101 项单元测试、25 项真实容器集成测试，失败/错误/跳过均为 0；包含 PostgreSQL 18.4 下 V19 空库、升级库、迁移确认幂等、冲突拒绝、会话撤销和显示名称测试。
- `make test-security` 通过：密钥扫描检查 632 个文件，部署安全基线通过，21 项安全测试通过；`make test-e2e` 通过 42 项端到端静态/契约测试。
- `make verify-p1-offline` 通过：环境、布局、双版本契约、确定性生成、Python/Java 包编译、Compose、安全、21 项安全测试和 41 个 SBOM 组件检查均通过；仅保留既有冻结数据文件和 `docs` 大小写索引警告。
- `git diff --check` 通过。契约基线保持 `v1 / 1.0.0` 与 `v2 / 2.0.0`；未执行 `make verify-p1-strict`、真实浏览器联合验收、现场设备和生产外部服务验收，因此 `RG5` 与 `RG6` 继续为 `HOLD`。
