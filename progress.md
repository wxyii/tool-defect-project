# R0—R8 进度日志

## R8 会话启动：2026-08-03

- **状态：** 阶段 0 入口核对进行中；R7 仍并行运行，R8 尚未宣称完成或启用。
- 已使用 `planning-with-files` 恢复并追加仓库根目录的 `task_plan.md`、`findings.md`、`progress.md`；保留 R7 未提交改动和交接上下文。
- 已读取 DOC-16/DOC-17 的 R8 任务 `R8-01`—`R8-10`、模型包安全门禁、`RG8` 标准及规定验证命令。
- 已确认第二版契约版本为 `v2 / 2.0.0`；R7 当前改动包含契约源/生成包和 `V20__r7_sample_library_and_exports.sql`，R8 不修改同一契约文件、生成包或迁移序列。
- 已盘点 R8 现状：R2 数据库已有 `model_upload_session_v2`、`model_version.source_kind` 和外部来源互斥约束；Java 模型领域、旧控制器、前端注册器和 P6 验证脚本仍强制训练运行/数据集来源。
- 已确认 `Makefile` 尚无 `verify-model-supply`、`verify-online-without-training`；应用配置尚无 R8 隔离桶、配额、验证运行身份、固定样例和启用门槛配置。
- 已记录入口限制：`RG2` 前序记录为通过；`RG6` 因 `RG5` 真实浏览器证据缺失保持 `HOLD`；R8 专属隔离/来源/切换批准事实尚未取得，面向用户启用必须保持 `HOLD`。
- **验证：** `git diff --check` 通过；`make verify-contracts-source` 通过（契约和离线 Python/Java 包自洽）。
- **附加基线：** `make verify-models` 通过（3 个候选、0 个阻断、生产声明仍禁止）；`make verify-data` 通过（R2 迁移版本 16、错误列表为空，工具识别的最新已部署迁移为 18）。
- 已完成 R8-02 的不触碰契约/生成包/迁移序列的安全包验证子项：新增 ZIP 边界检查、清单/哈希/Ed25519 验证、安全解包和 6 个正负测试；未把它扩展为模型加载或生产启用。
- **验证：** `PYTHONPATH=src .venv/bin/python -m unittest discover -s jobs/model-supply/tests -p 'test_*.py'` 通过，6 项；相关 Python `py_compile` 和 `git diff --check` 通过。
- **下一步：** 在 R7 交接后串行补齐 R8 外部来源公共字段、数据库迁移、业务 API 和验证作业；当前先继续做不冲突的安全规则测试/隔离运行设计。
- **并发变化：** 验证期间 R7 工作树继续扩展到样本导出后端、`jobs/sample-export-worker`、Rabbit 拓扑/发件箱、权限安全链和 `application.yaml`；已重新划定边界，R8 不触碰这些路径。
- **回归：** `make verify-p1-offline` 通过；包含 21 项安全测试、41 个 SBOM 组件、契约离线编译和 Compose 静态检查。既有冻结数据/Git 大小写警告保留。
- **最终工作树检查：** R7 在回归期间继续修改 `Makefile`、数据库迁移测试、离线测试入口和数据验证器；R8 未覆盖这些并发文件，`git diff --check` 仍通过，R8 安全包单测 6 项通过。
- **R8-02 补充：** 模型策略加入 SBOM、框架/插件白名单和资源上限；供应链 CLI 正向路径已覆盖，安全包单测最终为 7 项通过。
- **最终回归：** 策略更新后再次执行 `make verify-p1-offline` 通过；Python `py_compile` 和 `git diff --check` 通过。
- **交接边界：** 最终状态检查又发现 R7 修改 `apps/web-console/src/api/client.ts`；R8 仅修改自身 `deploy/security/supply-chain-policy.json` 和新增模型供应链验证路径，未覆盖 R7 文件。

## R7 后端实施前复核：2026-08-03

- 已恢复 `planning-with-files` 的 `task_plan.md`、`findings.md`、`progress.md`，确认 R7 入口：`RG2` 通过，`RG6` 因 `RG5` 真实浏览器证据缺失保持 `HOLD`。
- 已复核契约生成源、V20 迁移、业务后端真实包路径、权限矩阵、对象存储、幂等、审计和 Outbox 边界。
- 已确认新模块使用 `com.tooldefect.business.sample` 四层结构；旧手工检测模块只提供可复用的存储/反馈读取边界，不承载管理员样本业务。
- 设计落地：管理员反馈和候选由业务 API 事务追加；导出创建时只保存候选来源快照并追加 `sample.export.requested.v2`；Python worker 逐项读取、校验 SHA-256、构建包和清单；失败项进入清单和 `FAILED`，不得伪装为成功。
- 设计落地：下载票据只持久化令牌哈希，签发事件追加到 `sample_download_event_v2`，短时 URL 由对象存储授权；外部交接仅保留手工回执表，不生成数据集/训练事实。
- 当前尚未执行 RG7 命令；下一步先补齐生成器中的 R7 模型字段，再实现 `sample` 模块、权限/config、worker 和 `verify-sample-export`。
- 已修改 `tools/generate-contracts/generate.py` 的第二版显式模型模板，补齐 `SampleExportTarget`、`SampleDownloadTicket`、反馈修订、候选来源快照、导出清单/计数等字段；重新生成后契约源哈希保持 `f72a5a3390cfe2d9530d4d12b3bb2d572a57fbbb290c10bf886c1479509935fb`。
- 增量验证 `make generate-contracts && make verify-contracts-source` 通过：模式/示例/状态机/OpenAPI/AsyncAPI、确定性生成、Python 编译、Java `-Xlint -Werror` 均通过。

## R6 会话恢复：2026-08-03

- **状态：** 入口重新核对中；暂不宣称 `RG6` 或 `R6` 完成。
- 已执行：读取 `planning-with-files` 技能说明；恢复 `task_plan.md`、`findings.md`、`progress.md`；确认目标文档存在；确认工作树有业务后端与存储适配器相关未提交改动。
- 当前约束：既有计划记录 `RG5` 未关闭，因此本轮先核对 R6 入口与 R6 任务边界；不覆盖其他未提交改动，不把缺失的真实前置条件或门禁证据标为通过。
- 已核对：`R6-01`—`R6-07`、R6 三项入口条件、`RG6` 六项通过标准及 `make test-backend`/`make test-web`/`make test-security`/`make test-e2e` 四个验证命令；DOC-16 对应 `FR-IAM-001`—`010`、`MIG-005`—`006` 和两类角色权限矩阵。
- 已初检：现有未提交改动主要属于 R5 审计摘要和对象存储安全；第二版契约角色枚举已冻结，但前端用户管理仍默认 `OPERATOR`，路由仍存在数据集/训练权限表面。
- 已盘点后端身份初始形态：`SystemRole` 仍为八类旧角色，权限矩阵和引导账号仍使用 `SYSTEM_OPERATOR`；会话过滤器当前仅下发原子权限。
- 已补充盘点：身份服务角色校验和身份响应仍以数据库旧角色为中心；前端 `/datasets`、`/training-runs` 路由及八类角色选择器仍存在。
- 已核对安全/数据层：`SecurityConfiguration` 仍开放旧数据集/训练 v1 路径，模型审批仍接受 `dataset:approve`；既有数据库迁移最高为 `V18`，后续迁移必须串行递增。
- 已核对验证资产：存在后端身份单测、前端认证/安全源码测试及通用安全/端到端测试；R5 自动化历史结果通过，但真实浏览器联合证据仍缺失，不能升级为 `RG5` 通过。
- 计划决策：按用户指令继续 R6 契约/身份/权限实施与离线回归；发布切换和 `RG6` 关闭继续保持 `HOLD`，不把缺失的真实前置标为通过。
- 工作树边界：发现其他所有者/并发改动扩展到手工检测前后端、S3、契约 v2 和验证器；本轮只审查并保留，不覆盖这些非 R6 身份文件。
- 已盘点测试基线：后端/前端测试仍大量断言旧角色和 `OPERATOR` 会话，尚未覆盖两类角色白名单、迁移预览、深链接归零与未确认高权限安全失败。
- 下一步：确认契约生成输出路径、数据库角色/权限表结构和 R6 可复用服务边界，形成实施设计后再改代码。

## R6 身份与权限实施推进：2026-08-03

- 契约源已扩展本地用户显示名称更新操作，并将本地会话、创建用户、角色更新和用户摘要的角色字段收紧为两类人员角色；等待重新生成三语言包。
- 后端已补充显示名称更新、角色迁移确认请求头、冲突状态安全失败和已确认账号角色变更；`V19` 初始迁移将旧操作员建议为生产员工，其余旧角色进入冲突或未确认清单。
- 前端已删除数据集/训练路由与懒加载注册，用户管理改为两类角色、迁移预览/确认、显示名称保存和状态/密码操作；数据集/训练兼容组件暂不物理删除，留待 R9 清理。
- 角色矩阵单测、前端旧角色解析测试、数据库迁移断言和安全源码断言已同步到 R6 语义；尚未执行业务后端/前端/安全/端到端完整门禁。
- `make test-security` 已通过：密钥扫描、部署安全基线和 21 项安全测试通过。
- 首次 `make test-web` 的类型检查通过、Vitest 44 项中 43 项通过；唯一失败是新增测试错误地把保留的模型路由也断言为不存在，已记录并准备修正测试断言。
- 首次 `make test-backend` 的 100 项单测和数据库迁移集成均通过；`OverviewAuditQueryIT` 暴露出历史夹具仍绑定已撤销权限的 `OPERATOR`，已改为 R6 的 `PRODUCTION_EMPLOYEE` 夹具，等待重跑。

## R6 会话：2026-08-03

### 阶段 0：入口条件与现状核对

- **状态：** `HOLD`
- 已执行：
  - 按 `planning-with-files` 技能恢复仓库根目录的三份持久化记录。
  - 确认用户要求开始 R6，并将 `task_plan.md` 当前阶段切换到 R6。
  - 发现 R5 自动化源码门禁已完成，但真实浏览器联合验收与完整历史筛选仍未闭合，`RG5` 当前记录为未关闭。
  - 提取 `R6-01`—`R6-07`、`RG6` 六项标准和四项验证命令。
  - 初步确认第二版公共契约已冻结两类人员角色，但尚未找到逐账号映射决定清单。
  - 读取 R0 冻结运行清单，确认运行用户为零且逐账号映射批准明确标为不适用；该项不阻断 R6。
  - 确认 `RG5` 是 R6 唯一未满足的硬入口；R5 仍缺真实全链路浏览器证据，并有完整历史筛选限制。
  - 按安全失败规则将 R6 置为 `HOLD`，未修改生产源码，也未把缺失验证标记为通过。
  - 执行 `git diff --check`，三份规划文件格式检查通过。
- 下一步：
  - 由项目所有者选择先闭合 R5（含完整历史筛选与真实联合验收），或正式修改 R6 入口条件并记录批准依据。
  - 入口解除后继续盘点 R6 身份、迁移、会话、用户管理、菜单、权限与手册实现。

### R6 入口核验结果

| 检查项 | 证据 | 结果 |
|---|---|---|
| `RG5` 通过 | `task_plan.md`、`findings.md` 的 R5 关闭判定 | 未满足，阻断 |
| `R0-05` 用户与旧角色基线完整 | `Docs/baseline/R0-v1-runtime-inventory.json` | 满足 |
| 每个旧高权限账号已有目标角色决定 | 运行用户总数为 0，`NOT_APPLICABLE_NO_RUNTIME_USERS` | 不适用，不阻断 |
| 第二版契约角色基线 | `contracts/json-schema/common-v2.schema.json` | 已有两类角色枚举 |

### 本轮验证

| 命令 | 结果 | 状态 |
|---|---|---|
| `git diff --check` | 无输出，退出 0 | 通过 |
| `make test-backend`、`make test-web`、`make test-security`、`make test-e2e` | R6 尚未获准实施，未执行 | 未执行，不计通过 |

## R3 会话：2026-08-03

### 阶段 1—5：入口、实施、门禁与交付

- **状态：** 已完成，详见本文件末尾的 R3 验证结果
- 已执行：
  - 完整重读 `planning-with-files` 技能说明并恢复三份持久化记录。
  - 确认 R0—R2 已完成且 `RG2` 已通过，可以开始 R3 核查。
  - 从 `DOC-17` 提取 `R3-01` 至 `R3-05`、`RG3` 标准和入口配置要求。
  - 确认工作树包含整改链路已有未提交改动，后续仅做增量修改。
  - 确认 R3 的四项规定门禁，并盘点现有 `detectionbatch` 与 `storage` 模块。
  - 发现存储基础能力可复用，但手工功能开关、对象前缀、批次配额、媒体类型清单和孤儿策略仍需显式配置。
  - 对齐第二版 7 个 R3 接口与写请求字段，确认手工原图无需伪造第一版采集记录。
  - 决定用 `V17` 追加 R3 持久化事实并修正草稿期聚合语义，不改写已关闭的 `V16`。
  - 首次按推测文件名查找共享幂等执行器失败；已改用源码检索定位到 `IdempotencyService`，错误已记入计划且未重复失败命令。
  - 一次跨文件合并补丁因上下文归属错误未应用；已记录并改为按文件拆分。
  - 首次编译前在业务后端子目录读取根计划文件失败；已记录路径错误，后续统一从仓库根执行验证。
  - 业务后端源码编译通过；首次 91 项单元回归有 2 项架构边界失败，已定位为 R3 新代码的跨层依赖，未标记通过。
  - 修复跨层依赖后，91 项业务后端单元测试全部通过。
  - 扩展第二版人员安全链并新增手工检测原子权限及过渡角色映射；本人/全部数据范围由 `manual-detection:read:all` 明确控制。
  - 首次 `make test-backend` 在 91 项单元测试中因旧精确权限基线缺少 R3 三项权限而失败，尚未进入集成测试；已同步测试基线并重跑通过。
  - 完成 `R3-01`—`R3-05`、四项规定门禁和附加回归，`RG3` 通过。
- 下一步：
  - 进入后续阶段的任务消费、算法执行与结果回调闭环。

## 会话：2026-08-03

### R2 实施与门禁

- **状态：** 已完成；项目所有者明确免除运行观察窗，`RG2` 已通过
- **开始时间：** 2026-08-03
- 已执行：
  - 完整重读 `planning-with-files` 技能说明并恢复三份持久化记录。
  - 确认 `R1-01` 至 `R1-08` 和 `RG1` 已完成，可以开始 R2 核查。
  - 从 `DOC-17` 提取 `R2-01` 至 `R2-08`、数据迁移不变量、`RG2` 标准和三项规定门禁。
  - 将任务计划从 R1 切换为 R2，并明确真实数据库、备份、观察窗和遥测证据不得静默跳过。
  - 确认现有迁移最高为 `V15`、无并行业务后端工作树修改，本轮独占下一序号 `V16`。
  - 盘点第一版核心表、采集仓储、模型仓储、角色权限矩阵、数据门禁与 Testcontainers 迁移测试。
  - 发现 `verify-data` 尚不覆盖 R2 数据不变量；发现沙箱访问 Docker 套接字被拒绝，已记录待受控权限复核。
  - 对齐第二版冻结契约中的统一批次、单项、质量、反馈、样本、导出、模型上传和历史来源模式。
  - 完成 R2 数据模型设计：`V16` 串行迁移、可重复回填函数、稳定失败清单、影子差异、只追加事实和模型来源互斥约束。
  - 确认第一版从未部署的冻结消费者证据可支持默认关闭数据集/训练写开关，但不等同于生产观察窗证据。
  - 新增 `V16`、统一批次领域聚合、回填与影子读仓储，以及相互独立的取消能力/产线写开关；业务后端跳过测试编译通过。
  - 首次业务后端单元测试运行 88 项，87 项通过、1 项架构边界失败；已定位为退役异常所在包错误。
  - 修复依赖方向后业务后端 88 项单元测试全部通过。
  - `make verify-data` 通过，输出 R2 迁移版本 16 且错误列表为空。
  - 受控确认 Docker 29.6.2 可用；首次 `make test-backend` 已在 PostgreSQL 18.4 上成功完成空库 `V1—V16` 迁移，但整体因 R2 摘要长度、测试版本增量、权限顺序及应用上下文级联问题失败，未标记通过。
  - 修复全部集成问题后，`make test-backend` 已通过 91 项单元测试和 19 项 PostgreSQL 18.4/Testcontainers 集成测试，包含隔离 `pg_dump`/`pg_restore` 恢复演练。
  - 最终 P1 离线回归发现 18 项安全测试中 1 项仍断言已取消的数据集/训练权限；该次运行明确记为失败，正在按 R2 权限语义修正测试后重跑。
  - 修正旧安全断言后，`make verify-p1-offline` 通过：18 项安全测试和 41 个软件物料清单组件检查通过。
  - `make test-integration` 通过 5 项测试；`make verify-data` 返回 `PASSED`、迁移版本 16、错误列表为空；`make verify-v2-scope` 通过。
  - R2 的确定性影子差异、回填幂等和失败清单测试已通过。
  - 项目所有者明确指示第一版从未部署时不考虑批准运行观察窗；已同步修订 `DOC-17` 的 `R2-08`、`RG2` 标准和开关阶段，并据此解除 `HOLD`。
  - 同步修订 DOC-16 验收场景与 ADR-0003 后，首次追踪锁验证和 9 项追踪单测因冻结摘要漂移失败；已按生成结果更新验收场景及产品文档哈希，等待复验，不把首次失败记为通过。
  - 更新冻结摘要后追踪锁验证通过，9 项追踪单测全部通过；验收场景数量保持 23，需求数量保持 476，未产生未映射需求。
- 下一步：
  - 进入以 `RG2` 为入口条件的后续阶段。

### R2 验证结果

| 测试 | 命令 | 实际 | 状态 |
|---|---|---|---|
| 业务后端与真实数据库集成 | `make test-backend` | 91 项单元测试、19 项 PostgreSQL 18.4/Testcontainers 集成测试通过；包含空库、升级、幂等回填、互斥来源和隔离恢复演练 | 通过 |
| 集成边界 | `make test-integration` | 5 项通过 | 通过 |
| 数据不变量 | `make verify-data` | `PASSED`；`r2_migration_version=16`；`errors=[]` | 通过 |
| 第二版范围 | `make verify-v2-scope` | 取消清单、生成类型、消费者表面和历史快照边界通过 | 通过 |
| P1 离线回归 | `make verify-p1-offline` | 18 项安全测试、41 个软件物料清单组件检查通过 | 通过（有既有布局警告） |
| 追踪冻结锁 | `.venv/bin/python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json --compact` | `valid=true`；23 个验收场景、476 条设计要求 | 通过 |
| 追踪单测 | `.venv/bin/python -m unittest tests.test_traceability` | 9 项通过 | 通过 |
| 运行观察窗豁免 | 项目所有者决策与冻结消费者清单 | 第一版从未部署，不要求运行观察窗 | 通过（豁免） |

### 阶段 1：需求与现状核对

- **状态：** 已完成
- **开始时间：** 2026-08-03
- 已执行：
  - 完整读取 `planning-with-files` 技能说明与模板。
  - 确认两份目标文档存在。
  - 初始化 R0 的三份持久化规划文件。
  - 提取 R0 六项任务、`RG0` 通过标准和建议验证命令。
  - 确认当前契约 `v1 / 1.0.0`、目标契约 `v2 / 2.0.0`，R0 本身不修改网络契约。
  - 初步盘点 R0 产物：`R0-01` 与 `ADR-0003` 已有基础，`R0-04` 明确尚未实现。
  - 扩展追踪生成器初版，解析出 104 条产品要求、88 条 P0 和 19 个验收场景。
  - 首次覆盖验证真实暴露 14 条 P0 无验收映射。
  - 新增 4 个 AT 场景并扩展现有映射，全部 88 条 P0 已具备任务和验收引用。
  - 新增 ADR-0004 至 ADR-0007，闭合统一批次、多视角退役、两角色映射和模型直传安全边界。
  - 扫描取消范围和第一版多视角、角色、消费者及在线训练依赖的实际源码位置。
- 已创建或修改：
  - `task_plan.md`（新建）
  - `findings.md`（新建）
  - `progress.md`（新建）

### 阶段 2：实施设计

- **状态：** 已完成

### 阶段 3：R0 实施

- **状态：** 已完成
- 已执行：
  - `R0-01`：确认索引已登记文档职责与原始需求来源。
  - `R0-02`：修订 `Docs/02`、`Docs/04` 并形成 `Docs/01—14` 冲突闭合记录。
  - `R0-03`：新增 ADR-0004 至 ADR-0007，保留并同步 ADR-0003。
  - `R0-04`：追踪生成器升级到 `2.0.0`，纳入 DOC-16 产品编号、稳定编号、任务和验收引用。
  - `R0-05`：冻结源码清单和两次一致扫描；项目所有者确认第一版从未实际使用，运行态数量闭合为零。
  - `R0-06`：冻结 7 类取消对象，每类均记录消费者、历史保留和退役任务。
  - 项目所有者确认第一版从未实际使用；新增可审计声明并将运行态基线和取消清单从 `HOLD` 更新为通过。

### 阶段 4：门禁验证

- **状态：** 已完成

### 阶段 5：交付

- **状态：** 已完成（`RG0` 已通过）

## 测试结果

| 测试 | 命令 | 期望 | 实际 | 状态 |
|---|---|---|---|---|
| 旧矩阵摘要 | `python tools/traceability/build_matrix.py --summary` | 输出可重复摘要 | 463 条要求、614 个测试链接 | 通过 |
| 旧矩阵锁验证 | `python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json` | 锁一致 | `valid: true`，退出 0 | 通过 |
| 追踪单元测试基线 | `python -m unittest tests.test_traceability` | 全部通过 | 7 项中 1 项失败：硬编码 355，实际 463 | 失败 |
| R0-04 初版语法 | `python -m py_compile tools/traceability/build_matrix.py` | 无语法错误 | 退出 0 | 通过 |
| R0-04 初版锁验证 | `python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json` | 暴露缺失映射和锁漂移 | 14 条 P0 无 AT 映射，锁为旧模式 | 预期失败 |
| R0-04 最终锁验证 | `python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json --compact` | 矩阵确定且无覆盖缺口 | `valid: true`；476 条设计要求、104 条产品要求、88 条 P0、23 个 AT；最终摘要 616 个测试链接 | 通过 |
| 追踪单元测试 | `python -m unittest tests.test_traceability` | 全部通过 | 8 项通过，退出 0 | 通过 |
| JSON 与差异格式 | `python -m json.tool ...`、`git diff --check` | 无格式错误 | 退出 0 | 通过 |
| 仓库布局 | `make verify-layout` | 布局通过 | 退出 0；有冻结数据和 Git 大小写警告 | 通过（有警告） |
| R0 源码重复扫描 | 两次相同 `shasum` 清单摘要 | 摘要一致 | 两次均为 `67bdd750...37e64a` | 通过 |
| 追踪与基线单元测试 | `python -m unittest tests.test_traceability` | 全部通过 | 9 项通过，退出 0 | 通过 |
| P1 离线源码门禁 | `make verify-p1-offline` | 所有源码前置与离线门禁通过 | 退出 0；18 项安全测试通过，SBOM 41 个组件；有 2 条既有布局警告 | 通过（有警告） |
| `RG0` 解除后锁验证 | `python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json --compact` | 矩阵与锁一致 | `valid: true`，退出 0 | 通过 |
| `RG0` 解除后追踪测试 | `python -m unittest tests.test_traceability` | 未使用声明、基线和追踪全部通过 | 9 项通过，退出 0 | 通过 |
| `RG0` 解除后离线门禁 | `make verify-p1-offline` | 离线源码门禁保持通过 | 退出 0；18 项安全测试、41 个软件物料清单组件检查通过 | 通过（有既有警告） |

## 错误日志

| 时间 | 错误 | 尝试 | 处理 |
|---|---|---:|---|
| 2026-08-03 | 追踪单测硬编码计数与当前锁漂移（355 对 463） | 1 | 纳入 R0-04，改为结构与锁一致性断言 |
| 2026-08-03 | 14 条 P0 要求无验收场景映射 | 1 | 按语义新增或扩展 AT 场景，不降低校验强度 |
| 2026-08-03 | `apply_patch` 更新追踪说明时上下文未命中 | 1 | 原子补丁未产生修改；改为按文件拆分补丁 |

## 五问恢复检查

| 问题 | 答案 |
|---|---|
| 当前在哪里？ | R1 已开始，正在盘点双版本契约与生成基础 |
| 接下来去哪里？ | 完成 `R1-01` 至 `R1-08` 并执行 `RG1` 门禁 |
| 目标是什么？ | 完成两份指定文档定义的 R1 阶段 |
| 已了解什么？ | 见 `findings.md` |
| 已完成什么？ | R0 已闭合；R1 范围、目标版本和门禁已提取 |

## R1 会话：2026-08-03

### 阶段 1：需求与现状核对

- **状态：** 已完成
- 已执行：
  - 按 `planning-with-files` 恢复三份持久化记录。
  - 确认 `RG0` 已通过，R1 入口成立。
  - 提取 `R1-01` 至 `R1-08`、`RG1` 标准和四个硬门禁。
  - 确认目标契约为 `v2 / 2.0.0`，第一版 `v1 / 1.0.0` 必须保持兼容。
  - 确认工作区包含 R0 未提交成果，后续增量实施并避让已有修改。
  - 盘点第一版契约文件、生成器、验证器、兼容检查和 Makefile 门禁。
  - 确认生成与验证流程存在第一版硬编码，兼容快照扫描范围也需先隔离主版本。
  - 提取第二版核心枚举、公共模式、25 个接口和 7 个事件的批准基线。

### 阶段 2：双版本生成与验证设计

- **状态：** 已完成
- 已决定：
  - 第一版源文件和生成文件保持原路径与内容；源哈希按主版本隔离。
  - 第二版生成类型使用独立子命名空间，避免与第一版链接时混用。
  - 第二版兼容基线独立建立，第一版快照只扫描 `*-v1`。
  - `verify-v2-scope` 使用结构化契约与生成物检查，并扫描消费者表面，不以空目标或单纯文本存在判断代替。

### 阶段 3：R1 契约优先实施

- **状态：** 已完成
- 已执行：
  - 生成器源哈希改为按主版本隔离，并新增三语言第二版子命名空间输出。
  - 新增第二版公共模式、25 个目标业务接口和错误响应表面。
  - 新增第二版 7 个异步事件及严格载荷模式。
  - 新增第二版写接口七类示例、状态转换正反例和全部事件正反例。
  - 第一版消费者清单补齐所有者、遥测、契约版本和迁移状态，并使用独立 R1 治理模式验证。
  - 已生成第二版 Python、Java、TypeScript 子命名空间并建立 `v2` 独立兼容基线。

## R1 增量测试结果

| 测试 | 命令 | 实际 | 状态 |
|---|---|---|---|
| Python 工具语法 | `python3 -m py_compile tools/generate-contracts/generate.py tools/verify-contracts/*.py` | 退出 0 | 通过 |
| 第二版 JSON 解析 | `python3 -m json.tool`（全部新增第二版 JSON） | 退出 0 | 通过 |
| 两代契约验证 | `python3 tools/verify-contracts/verify_contracts.py` | v1/v2 模式、示例、状态机、OpenAPI、AsyncAPI、消费者通过 | 通过 |
| 第一版生成冻结 | `git diff --name-only --`（8 个既有生成源） | 无输出 | 通过 |
| 第一版兼容 | `python3 tools/verify-contracts/compatibility.py --self-test` | `v1 兼容检查通过` | 通过 |
| 第二版兼容 | `python3 tools/verify-contracts/compatibility.py --major 2 --self-test` | `v2 兼容检查通过` | 通过 |

## R1 错误日志

| 时间 | 错误 | 尝试 | 处理 |
|---|---|---:|---|
| 2026-08-03 | `verify-v2-scope` 遍历依赖/构建目录导致运行超时 | 1 | 已终止；增加源码遍历剪枝后重跑 |
| 2026-08-03 | 历史数据库迁移测试因文件内存在 `V2` 字样被整体误判为第二版 | 1 | 消费者源码改为逐行识别第二版表面；契约和生成物仍全量扫描 |
| 2026-08-03 | Python 离线包门禁未递归编译第二版子包 | 1 | 改为 `rglob("*.py")` 后重跑全部契约包门禁 |
| 2026-08-03 | `planning-with-files` 完成脚本无法识别中文阶段状态，误报 0 阶段 | 1 | 遵守仓库中文规则，使用 `rg` 检查未勾选项和中文未完成状态；无命中 |

### 阶段 4：`RG1` 门禁验证

- **状态：** 已完成

| 测试 | 命令 | 实际 | 状态 |
|---|---|---|---|
| 契约源码门禁 | `make verify-contracts-source` | 两代模式/示例/接口/事件/消费者、两代兼容锁、确定性生成、Python/Java 编译通过 | 通过 |
| 严格三语言契约门禁 | `make verify-contracts` | Python、Java、TypeScript 两代生成物编译通过 | 通过 |
| 第二版范围门禁 | `make verify-v2-scope` | 取消清单、生成类型、显式第二版消费者表面、历史快照边界及负向自测通过 | 通过 |
| 离线契约包门禁 | `make test-contract-packages-offline` | Python/Java 两代递归编译通过 | 通过 |
| P1 离线回归 | `make verify-p1-offline` | 18 项安全测试、41 组件软件物料清单及其余离线门禁通过 | 通过（有既有布局警告） |
| 差异格式 | `git diff --check` | 退出 0 | 通过 |

### 阶段 5：交付

- **状态：** 已完成
- 任务编号：`R1-01`—`R1-08`。
- 契约版本：第一版 `v1 / 1.0.0`；第二版 `v2 / 2.0.0`。
- `RG1`：本地门禁通过。
- 已知限制：未执行 `make verify-p1-strict`；第二版消费者尚未运行接入；保留既有冻结数据和 Git 大小写警告。

## R1 五问恢复检查

| 问题 | 答案 |
|---|---|
| 当前在哪里？ | R1 已完成，`RG1` 本地通过 |
| 接下来去哪里？ | R2 数据模型扩展、回填和影子读 |
| 目标是什么？ | 双版本契约、生成包、兼容锁和第二版负向门禁 |
| 已了解什么？ | 见 `findings.md` 的 R1 关闭结论 |
| 已完成什么？ | `R1-01`—`R1-08` 和规定的四项 `RG1` 门禁 |

## 会话：2026-08-03 R3 实施与门禁

- **状态：** 已完成，`RG3` 通过
- **任务编号：** `R3-01`—`R3-05`
- **契约版本：** 第一版 `v1 / 1.0.0` 保持兼容；第二版 `v2 / 2.0.0`
- 已执行：
  - 新增 `V17` 手工批次迁移，建立上传会话、每项唯一逻辑任务、补偿事实、原子权限和分阶段聚合状态机。
  - 实现批次创建、增删项、上传确认、幂等提交、本人/全部查询、稳定游标及短时读取票据。
  - 复用 S3 兼容短时票据与对象头校验，并补齐对象删除、孤儿抢占清理和 `HOLD` 补偿事实。
  - 将人员安全链扩展到第二版路径，增加手工检测三项原子权限和越权隔离。
  - 按请求主版本生成标准错误；第二版使用 `error_code` 冻结形状，第一版保持原形状。
  - 增加至少 10 项逐项确认、每项唯一任务、越权、故障补偿、迁移数据不变量与真实 MinIO 删除验证。
  - 修正应用层条目标识贯穿对象键和数据库记录，避免对象与条目追踪漂移。
  - 首次最终后端门禁暴露事务服务 `final` 导致代理创建失败，明确记录失败并修复；第二次完整运行通过。

### R3 验证结果

| 测试 | 命令 | 实际 | 状态 |
|---|---|---|---|
| 业务后端与真实容器集成 | `make test-backend` | 91 项单元测试、21 项集成测试通过；PostgreSQL 18.4、MinIO、RabbitMQ 均实际启动 | 通过 |
| R3 集成边界 | `make test-integration` | 6 项通过 | 通过 |
| R3 安全与部署基线 | `make test-security` | 密钥及部署安全扫描通过，21 项安全测试通过 | 通过 |
| R3 故障注入 | `make test-faults` | 19 项通过 | 通过 |
| 数据迁移不变量 | `make verify-data` | `PASSED`；最高迁移版本 17；`errors=[]` | 通过 |
| 第二版范围 | `make verify-v2-scope` | 取消清单、生成类型、消费者表面和历史快照边界通过 | 通过 |
| 追踪冻结锁 | `.venv/bin/python tools/traceability/build_matrix.py --verify-lock Docs/traceability/matrix-lock.json --compact` | `valid=true`；23 个验收场景、476 条设计要求 | 通过 |
| 追踪单测 | `.venv/bin/python -m unittest tests.test_traceability` | 9 项通过 | 通过 |
| 差异格式 | `git diff --check` | 退出 0 | 通过 |

### R3 交付限制

- 功能开关默认关闭，生产启用前必须配置真实数据库和对象存储；整个存储模块关闭时能力控制器不装配。
- 本阶段止于逻辑任务创建；任务消费者、算法执行与最终回调属于后续阶段。
- 未运行全阶段汇总入口 `make verify-all`，不据此宣称全阶段严格环境通过。
# R4 启动记录（2026-08-03）

- 使用 `planning-with-files` 恢复并读取 `task_plan.md`、`findings.md`、`progress.md`。
- 已核对 DOC-17：R4 入口依赖为 `RG2` 通过、R3 批次和图片项接口在集成环境稳定、第二版推理事件和结果模式冻结；前两项已有上一阶段交付记录，事件与结果冻结状态待源码核对。
- 已把 `task_plan.md` 从已完成的 R3 切换为 R4，建立 `R4-01`—`R4-06` 与 `RG4` 验证清单。
- 已完成首轮契约与源码盘点：第二版事件契约已冻结，但推理服务当前运行编排仍是第一版多图片任务模型，R4 需要以并行适配方式新增第二版单图消费路径。
- 已盘点推理可靠性与边缘同步基础，确认可保留第一版兼容实现并增量增加第二版单图适配；下一步核对 R3 事件生产、第二版回调端点和 OpenAPI 产线表面。
- 已确认 R4 需要补齐三段断链：业务后端第二版任务发布/结果落库、推理服务第二版单图质量执行、边缘端第二版产线单项提交；同时需要以 `V18` 修正全质量拒绝聚合语义。
- 已完成 R4 入口核对和实施设计，进入 `R4-01`—`R4-06` 编码阶段；契约保持 `v2 / 2.0.0` 不变，第一版兼容代码保持原位。
- 已新增第二版单图片项事件解析、质量编排、完成/失败发布和五类版本化质量检查；首轮 `make test-inference` 共 34 项，其中 32 通过、2 项因测试调用契约校验器参数不完整而错误，已记录并进入修正。
- 修正契约校验测试接线后重跑 `make test-inference`：34 项全部通过。
- 已新增第二版产线单对象生成客户端适配、同一 `capture_id` 哈希冲突拒绝和第二版用量指标；`make test-edge`：119 项全部通过。
- 边缘测试输出明确指出真实相机、触发器、设备清单和现场批准证据缺失；模拟门禁通过不替代受控硬件验收，`RG4` 最终必须保留此限制。
- 业务后端首次增量编译在 `R4InferenceResultHandler` 的异常 multi-catch 处失败；编译器确认仓库使用的 `JacksonException` 属于 `RuntimeException`，已记录并改为单一运行时异常捕获。
- 沙箱内后端门禁因 Docker 套接字权限明确失败；沙箱外重跑后 92 项单元全部通过，真实 PostgreSQL/MinIO/RabbitMQ 均成功启动。22 项集成仅剩三处测试基线问题：迁移计数两处、测试批次号长度一处；`V18` 空库与升级迁移均已实际成功。
# 2026-08-03 R4 后端真实容器第二轮

- `make test-backend`：失败。92 个单元测试通过，22 个集成测试中剩余 1 个失败、1 个错误。
- `DatabaseMigrationIT.r4SingleItemResultsDriveSafeAggregateStates`：R4 测试夹具把图片直接置为 `AVAILABLE`，却未填写既有约束要求的宽高；已补充 `64x64`。
- `ReliableMessagingIT.confirmedOutboxRetriesMandatoryReturnAndPublishesPersistentMessage`：首次不可路由符合预期，动态补绑后的立即重试未领取到事件；将测试重试时间显式置于数据库当前时间之前，排除数据库时钟边界。
# 2026-08-03 R4 单图链路增量验证

- `make test-inference`：通过，36 项；覆盖第二版单图解码无视角属性、五项质量检查、质量拒绝跳过算法、技术失败和持久幂等回放。
- `make test-backend`：通过，92 项单元测试、22 项真实容器集成测试；PostgreSQL、MinIO、RabbitMQ 均实际启动，V18 空库/升级/备份恢复及 R4 聚合场景通过。
- 新增 R4 集成、端到端和故障用例，等待统一入口执行。
- 第二版边缘消费者状态更新为 `TESTING`，补充升级顺序、回滚方式和分版本用量指标基线；第一版保持冻结兼容。
- 首轮 `RG4`：边缘 119 项、推理 36 项、集成 8 项、端到端 39 项通过；故障 21 项中新增枚举源码断言 1 项失败，已按 V18 的真实终态模型修正测试，等待重跑。

# 2026-08-03 R4 收尾

- `make test-edge`：通过，119 项；硬件前置检查安全报告缺失项，未把模拟测试冒充硬件通过。
- `make test-inference`：通过，36 项。
- `make test-integration`：通过，8 项。
- `make test-e2e`：通过，39 项。
- `make test-faults`：通过，21 项。
- `make test-backend`：最终通过，92 项单元测试、23 项真实容器集成测试；新增产线仓储宽高与幂等用例通过。
- `make verify-contracts-source`、`make verify-v2-scope`、`make verify-data`、`make verify-p1-offline`：全部通过；数据门禁识别最新迁移为 V18。
- `R4-01`—`R4-06` 源码、契约、模拟和真实服务基础设施验证完成。
- `RG4` 关闭判定：源码与模拟门禁通过；真实相机和触发设备验收因前置缺失保持 `HOLD`，不得标为全量通过。

# 2026-08-03 R4 硬件验收豁免

- 项目所有者明确指示跳过真实硬件验收并解除 `HOLD`。
- 真实相机、触发设备、设备清单、现场配置和批准证据仍未提供；本项记录为“已豁免、未执行”，不记录为验证通过。
- `RG4` 按批准后的范围关闭，`R5` 入口解除阻断；真实硬件上线或扩大站点范围前必须在 `RG9` 恢复验收。
# R5 会话进度（2026-08-03）

- 已读取用户指定的 `planning-with-files` 技能并按其规则接管进度记录。
- 已确认三份规划文件和 `DOC-16`、`DOC-17` 均存在。
- 已把 `task_plan.md` 当前目标从已关闭的 R4 切换为 R5，并建立入口核对、设计、实施、`RG5` 验证和交付阶段。
- 当前正在提取 R5 任务编号、验收条件并盘点代码现状；尚未修改生产代码，尚未宣称任何 R5 门禁通过。
- 已提取 `R5-01`—`R5-06` 的职责概要及核心前端约束。
- 已检查工作树：检测到现有 R4 未提交改动，后续只做增量修改并在差异审查时区分本轮 R5 变更。
- 已读取 R5 完整章节、相关正式需求与前端工程清单；确认 R5 前端能力尚需新增。
- 下一步盘点第二版生成 TypeScript 类型、R3 后端接口及既有前端测试/路由模式，形成实施设计。
- 已确认 R3 主要批次接口可用，并发现快速反馈后端实现与 OpenAPI 声明之间可能存在缺口。
- 已确认 R5 必须扩展统一 API 客户端以支持稳定幂等键和第二版操作。
- 已完成 R5 入口核对与实施设计阶段，`task_plan.md` 已切换状态。
- 已确认快速反馈数据库结构和第二版生成类型可复用，不新增契约或数据库迁移。
- 已核对快速反馈冻结请求模式、前端严格编译设置和现有权限接线，开始生产代码实施。
- 已补齐快速反馈仓储、应用服务、控制器与 `PUT` 权限接线；尚待编译和集成验证。
- 当前转入契约缺口核对：批次详情缺少图片项集合，图片项类型缺少完整结果/历史展示字段。
- 已扩展批次详情返回图片项集合，并扩展前端统一 API 客户端和第二版生成类型出口。
- 下一步实现手工检测前端服务、上传状态机、页面组件与测试，再回头处理编译和契约缺口。
- 第一轮直接前端类型检查未执行：环境 Node 24.14.0 / pnpm 11.9.0 与项目锁定 Node 20.13.1 / pnpm 10.34.5 不兼容；该结果不标记为通过，下一步使用仓库统一门禁入口。
- `make test-web` 第一轮通过：类型检查通过、既有 7 个测试文件 34 项通过、生产构建通过。该轮尚未包含 R5 新增专项测试，因此只作为增量编译证据，不关闭 `RG5`。
- `make test-web` 加入 R5 专项后通过：8 个测试文件、38 项测试全部通过，类型检查与生产构建通过。
- `make test-backend` 沙箱内因 Docker 权限失败；沙箱外真实重跑通过：92 项单元测试、23 项集成测试，失败/错误/跳过均为 0。
- 新增 R5 PostgreSQL 快速反馈用例后再次完整通过：92 项单元、24 项集成，失败/错误/跳过均为 0。
- `make test-integration` 8 项、`make test-e2e` 加入 R5 后 42 项、`make test-security` 21 项及两项安全扫描均通过。
- 差异审查发现结果图/质量详情仍缺，继续实施，不关闭 `RG5`。
- 已补齐逐图质量检查、技术错误和派生结果的受控读取及页面展示；首次真实后端回归发现 PostgreSQL `numeric` 到 `Double` 的 JDBC 直接转换不受支持，已改为 `BigDecimal` 显式转换，等待重跑。
- 定向 PostgreSQL 回归通过 16 项。最终前端门禁发现客户端误声明实现完整第二版接口；已收窄为生成 `ApiClientV2` 的 R5 操作子集，保留编译期契约约束，等待重跑。
- 修正客户端实现约束后最终 `make test-web` 通过：8 个测试文件、38 项测试，类型检查和生产构建全部成功。
- 最终 `make test-backend` 通过：92 项单元测试、24 项真实容器集成测试，PostgreSQL、MinIO、RabbitMQ 均实际启动，失败、错误和跳过均为 0。
- `git diff --check` 通过；本阶段没有修改 `contracts/`、生成网络类型或数据库迁移，契约保持第二版 `v2 / 2.0.0` 与第一版 `v1 / 1.0.0` 并存。

## R5 阶段性判定

- **任务编号：** `R5-01`—`R5-05` 源码完成；`R5-06` 自动化覆盖完成、真实浏览器联合链路未执行。
- **自动化结果：** `make test-web`、`make test-backend`、`make test-integration`、`make test-e2e`、`make test-security` 全部通过。
- **门禁状态：** `RG5` 未关闭。尚无启用功能开关、真实人员会话、对象存储、队列和推理服务联合运行的浏览器验收证据。
- **功能限制：** 历史筛选目前只覆盖已加载页的来源和状态，时间、操作人员、结果的服务端筛选尚未完成；现有持久结果没有完整模型、流水线和耗时字段，管理员历史元数据展示受限。
- **范围声明：** 本轮未执行 `make verify-all`，不宣称全阶段严格门禁通过。

## R5 错误字段兼容修复（2026-08-03）

- 修复统一前端错误解析器仅读取第一版 `code`、忽略第二版 `error_code` 的问题。
- 解析顺序调整为优先使用第二版 `error_code`，缺失时兼容第一版 `code`，两者都缺失时继续生成 HTTP 状态回退码。
- 新增第二版错误码展示和第一版兼容回归测试；`make test-web` 通过，9 个测试文件共 40 项测试、类型检查和生产构建均成功。

## R5 第二版可选字段校验修复（2026-08-03）

- 复现创建批次只提交必填 `usage_stage` 时，后端把可选 `usage_stage_note` 当作必填字段并返回 `TD-API-VALIDATION-001`。
- 根因是第二版控制器复用了要求字段集合完全相等的第一版严格对象读取器；同一问题也影响不带 `supersedes_record_id` 的快速反馈请求。
- 新增第二版允许字段与必填字段分离的严格读取器；创建批次和快速反馈均拒绝未知字段、保留必填校验，同时允许省略各自的可选字段。
- 首次控制器测试因当前模拟框架不能模拟具体服务类而错误退出；改用显式记录型测试替身后，定向 4 项测试全部通过。
- `make test-backend` 最终通过：96 项单元测试、24 项真实容器集成测试，失败、错误和跳过均为 0。
- 核对前端操作表后确认提交批次路径映射正确；页面通用异常分支会丢弃已解析的服务端错误，导致只显示固定失败文案。
- 上传页现会展示服务端消息、第二版错误码和请求标识；单项上传/确认失败也保留对应错误证据。`make test-web` 通过：9 个测试文件共 41 项测试、类型检查和生产构建均成功。
# R6 进度补充（2026-08-03）

- 契约校验完成：`make verify-contracts-source` 已通过契约源、示例、兼容性自检、确定性生成及 Python/Java 契约包编译。
- 源码复核发现并修复管理员权限集合缺逗号，以及部署审批继续使用旧 `dataset:approve` 的 R6 权限收敛问题。
- 当前仍在身份迁移、前端角色与旧路由清理、迁移测试阶段；不宣称 RG5 或 RG6 通过。

## R6 实施与门禁收尾（2026-08-03）

- `R6-01`—`R6-07` 已完成：人员角色收敛为 `PRODUCTION_EMPLOYEE`、`ADMINISTRATOR`；V19 增加角色迁移预览/确认事实、未确认与冲突状态、显式原因和会话撤销；用户管理支持显示名称编辑、启停、密码重置和幂等迁移确认。
- 旧高权限角色不会自动提权：仅旧 `OPERATOR` 且无额外角色时建议生产员工，空角色保持未确认，其他旧角色进入冲突；管理员映射需要逐项确认，冲突/高权限映射需要审计原因。
- `R6-05` 收尾移除了模型页中的冻结数据集/成功训练运行选择器，数据集和训练路由、菜单及懒加载入口保持不可达；旧组件、服务和第一版客户端方法按兼容窗口保留，物理清理由 `R9` 负责。
- 模型部署审批前端、后端控制器和安全配置统一使用 `model:approve`/`model:deploy:approve`，不再通过 `dataset:approve` 旁路。
- `make test-web` 最终通过：类型检查、9 个测试文件 44 项测试和生产构建均通过；首次测试失败来自错误地把模型路由判为已删除，已收窄为数据集/训练路由不可达断言。
- `make test-backend` 最终通过：101 项单元测试、25 项真实容器集成测试，失败/错误/跳过均为 0；包含 PostgreSQL 18.4 下 V19 空库、升级库、迁移确认幂等、冲突拒绝、会话撤销和显示名称测试。
- `make test-security` 通过：密钥扫描检查 632 个文件，部署安全基线通过，21 项安全测试通过；`make test-e2e` 通过 42 项端到端静态/契约测试。
- `make verify-p1-offline` 通过：环境、布局、双版本契约、确定性生成、Python/Java 包编译、Compose、安全、21 项安全测试和 41 个 SBOM 组件检查均通过；仅保留既有冻结数据文件和 `docs` 大小写索引警告。
- `git diff --check` 通过。契约基线保持 `v1 / 1.0.0` 与 `v2 / 2.0.0`；未执行 `make verify-p1-strict`、真实浏览器联合验收、现场设备和生产外部服务验收，因此 `RG5` 与 `RG6` 继续为 `HOLD`。

## R6 开发账号显式迁移（2026-08-03）

- **任务编号：** `R6-02`、`R6-03`、`R6-04` 的开发环境账号落地操作。
- **账号事实：** 已核对开发数据库中 `admin` 原绑定 `SYSTEM_OPERATOR`、迁移状态为 `UNCONFIRMED`；未修改其他账号。
- **变更结果：** `admin.person_role=ADMINISTRATOR`，迁移记录为 `CONFIRMED`、版本递增至 2，保留旧 `SYSTEM_OPERATOR` 作为历史绑定证据；所有旧会话已撤销。
- **凭据处理：** 使用 Argon2 生成并写入密码哈希，未把明文密码写入仓库、配置或规划文件；沿用现有策略设置 `password_change_required=true`，首次登录后必须修改密码。
- **验证结果：** 通过本地登录和会话查询确认账号返回 `roles=[ADMINISTRATOR]`、管理员权限集合；数据库查询确认迁移状态、管理员角色和审计记录均已落库。
- **契约版本：** 未改变，仍为 `v1 / 1.0.0` 与 `v2 / 2.0.0`。

# R7 会话启动（2026-08-03）

- 已按 `planning-with-files` 恢复并继续使用仓库根目录的 `task_plan.md`、`findings.md`、`progress.md`。
- 已读取 DOC-16 的反馈、样本库、契约、权限、迁移和验收要求，以及 DOC-17 的 R7 入口、`R7-01`—`R7-07`、导出包最小内容和 `RG7` 验证命令。
- 已确认 `RG2` 已通过；`RG6` 因 `RG5` 真实浏览器证据缺失仍为 `HOLD`。本轮开始 R7 的开发核对和实现，但不把面向用户启用或阶段门禁标为通过。
- 已建立 R7 阶段计划；当前处于“入口条件与现状核对”，尚未修改生产源码，尚未执行 R7 验证命令。

## R7 入口与设计（2026-08-03）

- 入口判定：`RG2` 已通过；`RG6` 由于 `RG5` 真实浏览器证据缺失保持 `HOLD`。R7 可开发但不可面向用户启用，导出对象前缀、保留期、配额和审批阈值也尚未有批准配置。
- 已确认 R7 任务为 `R7-01`—`R7-07`，规定门禁为 `make verify-sample-export`、`make test-backend`、`make test-integration`、`make test-security`、`make test-e2e`。
- 已完成设计边界：追加 `V20`；管理员反馈和样本决策分别形成不可变事实；候选保存来源快照；导出使用队列事实和 outbox 请求事件；worker 逐候选隔离失败；下载票据只保存哈希并审计生命周期；不创建数据集/训练事实。
- 已识别并纳入设计：第二版推理结果当前未持久化模型/流水线/规则版本，worker 必须从结果对象证据或新增持久化事实取得；无法无歧义取得时该候选进入失败清单。
- 当前转入契约增量、迁移和业务后端实现；尚未修改生产源码，尚未执行 R7 门禁。
- 已完成 R7 契约增量：目标对象模式、反馈修订、候选来源快照、导出计数/清单和下载票据字段已进入 `contracts/`，三语言生成物已更新。
- `make generate-contracts` 和 `make verify-contracts-source` 最终通过；契约版本保持 `v2 / 2.0.0`，第一版生成源未修改。

## R7 实施复核（2026-08-03）

- 已核对上一轮前端 API 客户端补丁：八个 R7 操作均已出现在操作联合类型、操作表和 `ApiClient` 方法中。
- 已修正样本应用层对 JDBC 游标编码器的反向依赖，新增 `SampleCursor` 并保留非法游标的安全失败语义。
- 前端管理页、R7-06 外部交接说明、R7 专项静态测试和统一门禁仍待完成。

## R7 前端页面与生成链修正（2026-08-03）

- 新增 `SampleLibraryService`，统一调用八个 R7 API，严格解析管理员检测项、反馈、候选、导出作业和下载票据响应。
- 新增 `SampleLibraryView.vue` 并注册 `/sample-library` 管理路由；支持筛选、六类反馈、候选纳入/排除、批量选择、异步导出、失败清单和短时下载票据。
- 修正契约生成器漏生成 `v2/index.ts` 的 `JsonObject` 声明；`make generate-contracts`、`make verify-contracts-source` 通过。
- `make test-web` 最终通过：类型检查、44 项前端测试、生产构建均通过。首次模板绑定语法错误和第二次类型错误已记录在任务计划。
- 已核对 worker 实际导出布局，下一步补充 R7-06 格式/用途/手工回执说明和 R7 专项静态测试。

## R7 后端真实容器复验（2026-08-03）

- 首次 `make test-backend` 明确失败于旧迁移计数和 `final` JDBC 仓储事务代理；已分别修正为 V20 计数和可代理仓储类。
- 第二次 `make test-backend` 通过：101 项单测、25 项真实容器集成测试，失败、错误和跳过均为 0。
- 下一步执行 `make test-integration`、`make verify-data`，并核对 worker 完成事件到业务导出作业投影的剩余接线。

## R7 完成事件接线核对（2026-08-03）

- 已核对既有 RabbitMQ 拓扑、手动确认消费者、收件箱事务和 R4 推理结果处理器。
- R7 请求队列/发件箱校验已存在，但 `sample.export.completed.v2` 尚无业务后端监听队列、严格事件处理器或导出作业投影更新；worker 目录也只有纯构建/发布能力，没有数据库访问。
- 既有收件箱表允许 `detection_task_id` 为空，但仓储重复消息比较使用直接 `.equals`；R7 完成事件复用收件箱前需改为 `Objects.equals` 并覆盖空任务标识测试。
- 当前任务：实现独立样本完成消费者、严格完成事件处理和幂等投影更新；随后执行 `make test-integration`、`make verify-data` 及 R7 统一门禁。

## R7 完成事件闭环实现（2026-08-03）

- 完成事件契约增加可选 `package` 对象引用；示例同步携带压缩包和清单完整引用，保持 v2 兼容表面不删除、不新增既有必填字段。
- worker 新增确定性完成事件构造器，要求终态、压缩包/清单 SHA-256、大小、媒体类型和 `sample-exports/` 前缀；不把二进制、令牌或签名地址放入消息。
- 业务后端新增完成队列/绑定、独立手动确认消费者和严格处理器；消息收件箱使用空任务标识并已修正空值安全比较。
- `JdbcSampleLibraryRepository` 新增完成投影事务：核对请求目标、候选集合和计数，写入包/清单引用、作业 `SUCCEEDED/FAILED`、失败候选、逐项状态及成功候选的导出关联；冲突、缺失或状态未知进入非重试失败。
- 尚未运行本轮编译和门禁；下一步先执行 worker/契约验证，再执行真实后端测试。
- 已补充 `DatabaseMigrationIT` 的 R7 完成投影回归，覆盖部分失败和重复完成事件；等待 Maven 编译及真实容器测试结果。
- 强制清理增量缓存后，`make test-backend` 通过：101 项单元测试、26 项真实容器集成测试，失败/错误/跳过均为 0；PostgreSQL 18.4、MinIO、RabbitMQ 均实际启动。
- `make verify-sample-export` 通过 4 项 worker 单测及正负/部分失败/哈希/版本门禁；`make test-integration` 8 项、`make verify-data`（最新 V20）、`make test-security` 24 项、`make test-faults` 24 项、`make test-e2e` 44 项均通过。

## R7 阶段性交付（2026-08-03）

- **任务编号：** `R7-01`—`R7-07`；**契约版本：** `v1 / 1.0.0` 与 `v2 / 2.0.0`；**数据库迁移：** `V20__r7_sample_library_and_exports.sql`。
- R7 后端已形成管理员反馈、候选去重/决策、异步导出请求、完成事件消费与幂等投影、短时下载票据、清理和审计闭环；完成事件要求包与清单的完整引用，任何缺失、冲突或状态未知均失败或进入收件箱死信，不产生 `PASS`。
- R7 前端已形成筛选、六类反馈、候选加入/纳入/排除、游标分页、异步导出状态、失败项、短时票据和手工外部接收回执页面；回执登记只面向管理员和终态导出，不增加外部服务调用。
- **验证命令与结果：** `make generate-contracts`、`make verify-contracts-source`、`make verify-p1-offline`、`make verify-sample-export`、`make test-web`、`make test-backend`、`make test-integration`、`make verify-data`、`make test-security`、`make test-faults`、`make test-e2e`、`git diff --check` 全部通过。后端真实集成使用 PostgreSQL 18.4、MinIO、RabbitMQ；前端为 9 个测试文件、44 项测试；数据门禁为 V20、`PASSED`。
- **已知限制：** `make verify-p1-strict` 与 `make verify-all` 本轮未执行；真实浏览器/人员会话/现场设备和生产外部服务联合验收未执行；`RG5` 证据缺失使 `RG6` 保持 `HOLD`；导出前缀、保留期、配额和审批阈值尚未取得批准配置。因此 R7 自动化标准通过，但整体状态为 `completed_with_hold`，不面向用户启用。
# R9 进度记录

## 2026-08-03：R9 启动

- 已读取用户指定的 `planning-with-files` 技能说明，并确认规划文件应位于项目根目录。
- 已发现并保留既有 `task_plan.md`、`findings.md`、`progress.md` 中的 R0—R8 历史记录。
- 已追加 R9 入口计划、初始发现和待办；当前处于 R9 阶段 0，尚未读取 R9 具体任务内容，未修改业务源码。
- 下一步：提取 DOC-16/DOC-17 的 R9 条目，核对入口条件、工作树边界和验证要求。

## 2026-08-03：R9 需求提取

- 已读取 DOC-16/DOC-17 的 R9 相关章节。
- 已确认 R9 包含 `R9-01`—`R9-09`，主题为第一版写入退役、多视角运行语义退役、在线训练链路退役、历史通用来源快照和全量验收。
- 已记录 R9 入口条件、`RG9` 标准、`MIG-009`/`MIG-011`—`MIG-013`、`FR-GOV-010`、关键验收场景和 18 项规定验证命令。
- 当前判断：入口状态尚未核实；真实设备、外部服务、批准观察窗、备份恢复和前序 `RG3—RG8` 证据不能推测为通过。

## 2026-08-03：R9 工作树初盘

- 已检查 Git 工作树、近期提交和相关目录。
- 当前只有三份规划文件有未提交改动；最新实现基线为 R7，数据库迁移包含 `V20`。
- 已确认数据集/训练/模型评估/研究资产仍在仓库中，下一步需分类其在线入口、离线保留条件和测试依赖。
- 未修改业务源码；R9 阶段 0 继续进行。

## 2026-08-03：R9 在线退役初步扫描

- 已检查 `Makefile`、第一版/第二版消费者清单、开发启动脚本和多视角运行引用。
- 已发现 `verify-model-supply`、`verify-online-without-training` 目标缺失，`verify-all` 仍含旧 P5/P7 依赖，开发启动脚本仍启动数据集构建器。
- 已发现第二版消费者尚处 `PLANNED`/`TESTING`，以及边缘、推理、业务和存储层仍有多视角兼容代码；上述事实使 R9 入口与 RG9 关闭保持未满足。
- 下一步：读取部署/构建/配置/监控和相关业务代码，建立 R9-01—R9-09 的可执行改动清单。

## 2026-08-03：R9 退役边界复核

- 已确认后端已有 `LegacyRetiredWriteFilter` 和 `410 / TD-LEGACY-FEATURE-RETIRED` 单测，但独立数据集/训练查询表面仍存在。
- 已将“保留冻结契约/迁移/历史证据，删除在线写消费者与独立新运行表面”确定为当前实施边界。
- 尚未修改退役代码；下一步核对模型历史来源快照、数据库结构、路由和现有 R9/范围验证器。

## 2026-08-03：R9-08 快照差距

- 已核对第二版契约与范围验证器：通用历史快照契约已存在，但业务模型登记/查询实现仍以第一版来源字段为中心。
- 已记录 R9-08 的实施前置：稳定摘要、归档引用、哈希和保留期规范，以及关闭独立遗留读取后的回归证据。

## 2026-08-03：R9 配置与前端差距

- 已确认第一版生产写开关默认开启、训练配置仍在业务配置中、安全链仍注册训练回调和旧资源权限。
- 已确认前端旧页面入口已隐藏但独立旧 API/feature 文件仍存在。
- 这些内容纳入 R9-01、R9-04、R9-07 和 R9-09 的改动与验证清单；尚未修改源码。

## 2026-08-03：R9 验证资产盘点

- 已确认模型供应链测试存在但未接入根门禁；“无训练在线”验证器缺失。
- 已决定先实施不改变业务数据的 R9-07 启动链清理和 R9-09 验证入口/扫描，再评估旧模块物理删除。

## 2026-08-03：R9-07 启动链清理

- 已修改 `tools/dev/start-all.sh` 和 `tools/dev/README.md`，移除数据集构建器在线启动链及凭据注入。
- `bash -n tools/dev/start-all.sh` 通过；研究/历史目录保留。
- 下一步：清理训练监控表面并补充验证入口。

## 2026-08-03：R9-07 训练监控清理设计

- 已确认训练/数据集监控指标由 `OperationalMetrics` 的真实数据库查询提供，不能只删除仪表盘文件。
- 计划同步清理 `training-release` 仪表盘、监控清单、后端训练/数据集指标和定向单测；模型审批与生产部署指标保留。

## 2026-08-03：R9-07 训练监控清理验证

- 已删除训练发布仪表盘并同步监控清单，移除后端训练/数据集指标 SQL 与单测断言。
- 验证通过：`make verify-monitoring`、`./mvnw -q -Dtest=OperationalMetricsTest test`、`git diff --check`、监控 JSON 解析和训练指标残留扫描。
- R9-07 仍未完成：业务训练配置、训练回调安全表面及其他部署/构建/事件引用待清理。

## 2026-08-03：R9 训练在线表面核对

- 已确认业务后端仍编译训练创建、状态回调和独立查询控制器；安全配置仍有训练回调/旧资源权限匹配。
- 历史迁移和训练表保留；后续只移除在线控制器、回调配置和构建/部署引用，模型登记/历史快照暂等待 R8/R9-08 证据。

## 2026-08-03：R9 第一版产线兼容边界

- 已确认第二版产线单图片入口和边缘调用存在；第一版采集接口仍作为兼容表面保留。
- `TD_PRODUCTION_V1_WRITE_ENABLED` 没有实际消费者，只被配置/数据验证器引用；不直接修改该值，等待消费者清单与观察窗证据闭合。

## 2026-08-03：R9-01 第一版生产采集写路径核对

- 已确认第一版 `/api/v1/edge/captures` 仍由业务后端 `CaptureController` 和边缘端旧 HTTP transport 实际实现/注册；`TD_PRODUCTION_V1_WRITE_ENABLED` 仅出现在配置与验证器，不能单独作为停写开关。
- 第二版 `/api/v2/production/detection-items` 已存在；下一步需设计 v1 写路径的明确 `410`/网关退役和边缘代理切换，保留 v1 契约基线及历史回归，不在证据不足时直接删路由。

## 2026-08-03：R9-01 第一版生产采集写入退役增量

- `LegacyRetiredWriteFilter` 新增第一版生产采集独立开关；默认配置改为 `TD_PRODUCTION_V1_WRITE_ENABLED:false`，`/api/v1/edge/captures` 写入及子路径默认返回 `410 / TD-LEGACY-FEATURE-RETIRED`，显式开关仍可用于受控回滚。
- 验证通过：定向 `LegacyRetiredWriteFilterTest`、`make verify-data`、`git diff --check`。
- 已知限制：边缘旧 transport 和 v1 契约/基线仍保留；尚无消费者归零、观察窗、批准人和真实设备证据，不能关闭 R9-01 或 RG9。

## 2026-08-03：R9-07 训练回调退役实施

- 已删除内部训练状态回报控制器，移除训练回调安全匹配和训练配置绑定；历史表/仓储保留。
- 首次编译发现异常处理器残留已删除类引用，修复后通过定向后端测试。
- 最终验证：根目录残留扫描通过；`./services/business-api/mvnw -q -f services/business-api/pom.xml -Dtest=LegacyRetiredWriteFilterTest,RolePermissionMatrixTest,OperationalMetricsTest test` 通过。
- 已知限制：训练创建控制器/工作流和独立历史读取仍待 R9-08 快照替换后清理。

## 2026-08-03：R9-02 多视角语义边界核对

- 已确认第二版推理单项路径已经拒绝 `image_role` 并在解码输出中不产生该字段；旧 `decoder.py`/`materializer.py`/边缘本地队列角色字段继续作为 v1/历史资产保留。
- 未进行全仓删除；第一版消费者和观察窗证据未闭合，R9-02 仍保持待办。

## 2026-08-03：R9-08 后端实现差距复核

- 现有后端模型登记/查询仍以 v1 `training_run_id`、`dataset_version_id` 为中心，未接通通用 `LegacyProvenanceSnapshot` 历史映射。
- 未修改契约、生成物或模型数据库字段；R9-08 继续 `HOLD`，禁止以空快照或猜测来源摘要关闭独立读取。

## 2026-08-03：R9-02 增量回归

- `make verify-v2-scope` 通过。
- `make test-inference` 通过 36 项；`make test-edge` 通过 119 项。测试输出中的真实硬件配置缺失被保留为 HOLD 前置，不计入真实设备验收通过。

## 2026-08-03：R9 当前工作树复核

- 阶段 0 已完成需求、入口、并发边界和资产盘点；入口因第二版观察窗、备份恢复、真实设备及外部服务证据不足继续 `HOLD`。
- 已核对当前未提交差异，R9 本轮没有修改 `contracts/`、生成包或历史迁移；训练内部回报面、训练回调权限、训练配置绑定和训练监控面已按 R9-07 增量清理。
- 训练创建/独立历史查询、前端旧 API/组件及 R9-08 快照替换仍未完成；后续验证不得宣称 R9 或 RG9 通过。

## 2026-08-03：R9-04 前端旧表面清理

- 删除前端数据集/训练页面和服务，移除 `ApiClient` 对应第一版操作、路径及调用方法；R6 测试保留路由不可达与模型/部署回归，去除旧服务消费者。
- `make test-web` 通过：类型检查、9 个测试文件、41 项测试和生产构建均通过。
- 已知限制：业务后端旧数据集/训练查询和写过滤表面、模型历史来源字段、第一版兼容接口及 R9-08 快照替换仍未完成。

## 2026-08-03：R9-09 验证入口设计复核

- 已确认模型供应链已有严格的压缩包验证器，但缺少根 `Makefile` 的 `verify-model-supply` 接线；新增入口必须在没有外部模型包证据时返回 `HOLD`。
- 已确认无训练在线隔离验证器缺失；后续将增加静态在线拓扑/残留检查和证据文件校验，避免以单元测试或离线研究代码冒充在线验收。

## 2026-08-03：R9-09 门禁增量验证

- 已新增根 `Makefile` 目标 `verify-model-supply` 和 `verify-online-without-training`，并把两者接入 `verify-all`；模型供应链门禁执行 7 项真实单元测试后，因缺少外部证据返回退出码 2/HOLD。
- 无训练在线门禁当前返回退出码 2/HOLD，命中业务后端训练控制器、工作流、仓储、旧权限/退役路径等残留；这确认 R9-07/R9-01 尚未完成，而不是测试失败。

## 2026-08-03：R9-07 业务后端依赖边界复核

- 主源码中训练创建/查询控制器和工作流未被其他业务服务引用；`DatabaseMigrationIT` 仍通过训练 JDBC 仓储验证历史迁移和旧表事实，后续删除在线训练表面时将保留迁移证据并改为直接历史验证或快照验证。
- 安全配置仍包含数据集/训练旧权限，退役过滤器和其 `410` 测试必须保留；当前不直接删除训练仓储，先完成依赖拆分与 R9-08 历史快照设计。

## 2026-08-03：R9-07 训练删除的测试边界

- 已定位训练单测、历史迁移集成测试和权限矩阵对旧训练对象/权限的引用；这些引用属于冻结历史证据，不能在没有快照替代和回归测试时整体删除。
- 当前安全修改边界收窄为：先退役在线 HTTP 匹配和控制器/工作流，再保留历史域/仓储测试资产，最后由 R9-08 完成快照替换后清理独立读取。

## 2026-08-03：R9-07 在线训练组件退役

- 删除训练 HTTP 控制器/异常处理器；移除工作流 `@Service`、训练配置绑定、JDBC 仓储 `@Repository` 和训练 HTTP 权限匹配，保留历史域/仓储供迁移测试显式使用。
- 定向 Maven 测试通过：`TrainingWorkflowServiceTest`、`TrainingRunTest`、`LegacyRetiredWriteFilterTest`、`RolePermissionMatrixTest`、`OperationalMetricsTest`。
- 无训练在线门禁静态扫描已通过，因缺少真实隔离部署/观察窗/批准人/回滚证据返回退出码 2/HOLD；独立历史读取和 R9-08 快照替换仍未完成。

## 2026-08-03：R9 训练源码所有权纠偏

- 复核确认业务后端 `training/` 源码目录被 `.gitignore` 忽略，不把该目录的删除/修改当作可交付 Git 差异；R9-07 不能因此宣称在线训练组件已从仓库清零。
- 当前无训练门禁必须继续扫描该目录并在发现在线组件时返回 `HOLD`；最终交付以已跟踪文件、可复现命令和真实证据为准。

## 2026-08-03：R9 当前回归与门禁校正

- 第二次完整 `make test-backend` 通过：102 项单元测试、26 项真实容器集成测试，失败/错误/跳过均为 0；PostgreSQL 18.4/Testcontainers 实际完成 V1—V20 迁移，MinIO/RabbitMQ 场景通过。
- 首次完整后端回归因默认停用第一版生产写后，旧 `S3StorageIT` 仍期待 403 而失败；已给该历史续传安全测试显式注入生产 v1 回滚开关，保留默认关闭时返回 410 的独立单测，第二次完整回归通过。
- `make verify-model-supply` 的 7 项供应链单测通过，但外部模型包、声明哈希和信任根缺失，结果为 `MODEL_SUPPLY_EVIDENCE_MISSING / HOLD`。
- `make verify-online-without-training` 实际命中忽略目录中的 `TrainingController`、工作流、JDBC 仓储和异常处理器 Spring 注解，结果为 `ONLINE_TRAINING_SURFACE_REMAINS / HOLD`；此前对该忽略目录的删除/注解修改不计入交付，不能据此关闭 R9-07。
- 当前 R9 仍为 `completed_with_hold`：已跟踪的启动链、监控、配置/安全匹配、前端旧表面、第一版生产停写保护和门禁接线已完成增量；R9-07 在线训练物理退役、R9-08 快照、消费者/观察窗/备份恢复/真实设备/外部服务证据仍未闭合。

## 2026-08-03：R9 V21 物理清理迁移与后端回归

- 已新增串行迁移 `V21__r9_remove_retired_v1_sources.sql`：在删除前检查旧模型来源字段和六张数据集/训练表，发现任何旧数据即抛错进入 `HOLD`；空库/升级库删除旧表、旧模型来源列和旧权限，并仅允许 `EXTERNAL_UPLOAD` 模型来源。
- 已将 `ModelVersion`、JDBC 模型仓储、模型领域测试、数据库迁移测试和可靠消息夹具切换到外部上传来源；未修改 V1—V20 历史迁移，也未修改契约生成源。
- V21 首次后端回归只发现一处测试断言仍为 20 条迁移，修正为 21；补充负向 HOLD 测试后，`make test-backend` 最终通过 80 项单元测试和 24 项真实容器集成测试，失败/错误/跳过均为 0。
- `DatabaseMigrationIT` 实际验证了 V21 空库迁移、升级迁移、pg_dump 恢复和旧表/旧列不存在；后端 `clean test-compile` 保持 217 个主源码、36 个测试源码编译通过。
- R9 当前仍为 `completed_with_hold`：物理退役和数据库清理子项已完成，但边缘/推理/复核剩余 v1 读路径、真实设备、外部模型包、隔离观察窗和完整 RG9 证据尚未闭合。

## 2026-08-03：R9 核心、边缘、推理与前端回归

- `make test-edge` 通过 119 项，硬件验收前置缺失按 HOLD 记录；`make test-inference` 通过 36 项；`make test-web` 通过类型检查、9 个测试文件 39 项测试和生产构建。
- `make test-core` 最终运行 202 项，其中仅 9 项因旧研究模型权重缺失报错；已将手册检查改为现行“样本导出”并单测通过，不恢复旧模型权重，`test-core` 仍按缺少模型证据保持 HOLD。
- 该结果不影响业务后端 V21 回归通过，但说明完整 R9 仍缺外部模型包/哈希/信任根证据；不把缺失模型的研究测试标为通过。

## 2026-08-03：R9 集成回归迁移

- 首次 `make test-integration` 命中已物理删除的 `CaptureWorkflowService.java`；已把 P5 追踪测试迁移到现行 v2 `ProductionDetectionService` 的单项事件传播断言。
- 重跑 `make test-integration` 通过 8 项；`make test-faults` 通过 24 项，`make test-e2e` 通过 44 项。
- 运行器误传单文件的尝试已记录，改用标准 `unittest` 精确执行角色手册测试并通过；这不改变核心缺失模型证据的 `HOLD` 结论。

## 2026-08-03：R9 严格 P1 门禁

- `make verify-p1-strict` 通过：严格工具链、契约三语言生成/编译、Compose、安全扫描、24 项安全测试、SBOM 和 Compose 配置检查全部通过。
- 该门禁只证明 P1 离线/严格基础约束，不能替代模型供应链、无训练隔离观察窗、真实硬件和 RG9 全量证据。

## 2026-08-03：R9 阶段性离线门禁汇总

- `make verify-sample-export` 通过 4 项 worker 测试及正向/负向/部分失败/哈希/版本场景；`make verify-p5-offline` 通过全部子项。
- `make verify-g7` 返回 `BLOCKED`，阻断数 109，原因是既有生产环境/站点签署/模型/硬件/恢复/发布证据缺失；按规则保持阻断，不补造证据。

## 2026-08-03：R9 V21 安全失败测试补充

- 已补充 V21 迁移负向集成测试：从 V20 预置一条旧 `dataset` 数据，验证迁移抛错、旧行保留、V21 不登记为成功。
- 完整 `make test-backend` 已确认新增测试与现有空库/升级库/恢复回归共同通过：80 项单元、24 项集成，失败/错误/跳过均为 0。

## 2026-08-03：R9 本轮阶段性收口

- 本轮状态：`completed_with_hold`。已完成授权范围内的第一版运行代码/无数据测试物理删除、V21 数据库安全清理、前端/监控/启动链清理、v2 追踪测试迁移和规划/ADR 记录。
- 收尾校验通过：`make verify-data`（V21、错误为空）、`make verify-layout`、`make verify-v2-scope`、`make verify-monitoring`、`bash -n tools/dev/start-all.sh`、`git diff --check`。
- 当前未暂存/未提交，未覆盖其他智能体改动；下一轮仅应继续处理边缘/推理/复核剩余 v1 消费者与外部/现场证据，不应回退 V21 或恢复已退役运行包。

## 2026-08-03：R9 物理退役授权落地

- 项目所有者确认 `business-api/.../training/` 正式退役、没有旧模型和第一版数据残留，并授权第一版运行代码物理删除。
- 已从工作区删除业务后端训练控制器、工作流、JDBC 仓储、异常处理、训练域对象及训练单测；已删除数据库迁移测试对训练仓储的引用。无训练门禁由“在线训练残留”降为仅缺外部隔离证据的 `ONLINE_ISOLATION_EVIDENCE_MISSING / HOLD`。
- 已删除旧模型登记接口、前端登记服务和生命周期资格读取器；保留当前模型目录/部署/供应链查询实体，待后续与第二版直传实现和数据库历史列一起决定是否追加清理迁移。
- 已确认 `business.dataset` 主代码无当前 Java 主代码消费者，已开始物理删除数据集运行包及旧构建流程测试；数据库历史迁移不回写。
- 已识别并准备删除 `jobs/dataset-builder`、`jobs/training-pipeline` 的旧源码、验证入口及本地候选/模型产物；`jobs/model-evaluator` 因当前验证器仍依赖而暂保留。
- 第一次 `clean test-compile` 因 `DatabaseMigrationIT` 仍导入已删除的 `JdbcDatasetRepository` 失败；已定位为历史目录测试残留，正在移除该运行仓储调用并保留当前模型/部署迁移断言。清理动作本身已删除旧 `target/`，避免 stale training class 伪造残留。

## 2026-08-03：R9 物理删除后的编译与残留分类

- 已修复 `DatabaseMigrationIT` 对已删除数据集仓储的历史测试依赖，并完成后端全量 `clean test-compile`：217 个主源码、36 个测试源码成功编译。
- 旧训练/数据集/模型登记运行面和业务后端第一版采集命令面已不再有主源码消费者；对应本地忽略产物也按精确路径清理，无旧模型或第一版数据被迁移或保留为运行事实。
- 保留 `LegacyRetiredWriteFilter` 的 `410` 退役响应、第一版契约/兼容基线和历史迁移脚本，作为防止误调用与审计依据，不将冻结来源误判为在线能力。
- 残留扫描显示边缘端旧采集/队列/同步器、推理端旧多图回调链以及部分 v1 检测/复核读模型仍存在；它们与当前 v2 单图执行接线、历史兼容测试或现有复核事实耦合，暂不做无替代的物理删除，R9-02/R9-04 后续需逐项闭合。
- 当前无训练门禁仅剩外部隔离部署/观察窗/批准人/回滚证据缺失，仍为 `ONLINE_ISOLATION_EVIDENCE_MISSING / HOLD`；模型供应链门禁仍因缺少外部模型包证据为 `MODEL_SUPPLY_EVIDENCE_MISSING / HOLD`。
