# HTTP 写接口示例

`write-examples-v1.json` 为 OpenAPI 中每个 `POST` 或 `PUT` 操作提供：

- 成功；
- 相同幂等键和相同摘要的重复请求；
- 幂等摘要或乐观锁冲突；
- 对资源无权；
- 未知字段或语义校验失败。

错误响应集中在 `common-responses-v1.json`，但每个操作都显式引用相应场景。验证器会检查写操作集合与示例集合完全一致，并校验成功请求和响应。
