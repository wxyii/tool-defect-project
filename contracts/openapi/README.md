# HTTP 契约 v1

`tool-defect-api-v1.json` 是 OpenAPI 3.1 唯一源，覆盖：

- 采集初始化、对象确认、检测提交、查询、批量对账与心跳；
- 检测列表、详情、短时图片授权与授权重试；
- 复核任务池、认领、释放、提交与标注上传；
- 数据集、训练、模型验证、部署审批和回滚；
- 推理执行开始、结果、失败、运行时健康和模型摘要；
- 服务器发送事件。

所有写接口都要求幂等键或乐观锁，并由 `contracts/examples/http/write-examples-v1.json` 提供五类示例。
